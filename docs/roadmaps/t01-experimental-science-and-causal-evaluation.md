# T01 — Experimental Science and Causal Evaluation

**Status**: In Progress
**Last updated**: 2026-09-04
**Master**: [Program Roadmap](../roadmap.md)

## Goal

Give Petri simple, reproducible readings of ecological diversity and cognition
that show whether each closed feature moved the world toward the program goals,
and hold the falsifiable causal measures ready for the deferred proof phase.

## Track Success Criteria

- [ ] A goal profile at the standard world runs once at every feature closure, populates the first diversity and cognition indicators, and reproduces its deterministic block on a second run.
- [ ] `docs/progress.md` shows one row per closed feature with every indicator reading, so program direction is visible between features.
- [ ] Every program-level outcome has a versioned definition, uncertainty treatment, and predeclared acceptance threshold.
- [ ] Reference reactive, stateful, learning-dependent, and socially contingent controllers produce the expected causal-evaluation profiles.
- [ ] Ecological strategies are distinguished by causal resource and interaction use, not genotype or phenotype distance alone.
- [ ] Cognitive claims report a profile of effective controller size, temporal dependence, learning dependence, information integration, social contingency, and transfer breadth.
- [ ] Baseline long-running experiments quantify current collapse, persistence, novelty, and cognitive dependence across independent replicates.
- [ ] A result-blind confirmatory protocol freezes hypotheses, effect thresholds, uncertainty, replication, exposure, exclusions, and stopping rules before confirmatory runs begin.
- [ ] A single progress ledger preserves dated evaluation snapshots and makes improvement, regression, and inconclusive evidence visible for every program-level outcome.

## Executable Features

- [ ] **T01.F01 — Outcome and Evidence Contract** — Depends on: None
  - Goal: Deferred proof phase. Versioned definitions, uncertainty treatment, and acceptance thresholds for every program-level outcome, on top of the indicator definitions T01.F12 ships.
- [ ] **T01.F02 — Reference Controller and Behavior Fixtures** — Depends on: T01.F01
  - Goal: Deferred proof phase. Hand-built reactive, stateful, learning, and social controllers that the causal assays are validated against before they judge evolved creatures.
- [ ] **T01.F03 — Population, Specimen, and Lineage Sampling Model** — Depends on: T01.F01, T10.F03
  - Goal: Deferred proof phase. Which creatures, lineages, and moments get sampled from a long run so measures are unbiased.
- [ ] **T01.F04 — Behavioral Strategy Descriptors** — Depends on: T01.F03
  - Goal: Deferred proof phase. Describe what a creature does from its actions and resource use so strategies can be compared without reading genomes.
- [ ] **T01.F05 — Cognitive Comparator and Causal-Assay Protocol** — Depends on: T01.F02, T10.F05
  - Goal: Deferred proof phase. The versioned protocol for asking whether a simpler reactive controller could do what an evolved one does.
- [ ] **T01.F06 — Ecological Diversity and Persistence Measures** — Depends on: T01.F03, T01.F04
  - Goal: Deferred proof phase. Strategy count and causal distinctness measured from resource and interaction use, replacing the lineage-count tripwire.
- [ ] **T01.F07 — Adaptive Novelty and Change-Potential Measures** — Depends on: T01.F03, T01.F04, T01.F13
  - Goal: Deferred proof phase. Is the population still finding new adaptive behavior, or only churning?
- [ ] **T01.F08 — Baseline Collapse and Ablation Characterization** — Depends on: T01.F05, T01.F06, T01.F07, T01.F13, T10.F08
  - Goal: Deferred proof phase. Long replicated runs that quantify collapse, persistence, novelty, and cognitive dependence before thresholds are frozen.
- [ ] **T01.F09 — Confirmatory Protocol and Threshold Freeze** — Depends on: T01.F08, T10.F07
  - Goal: Deferred proof phase. Freeze hypotheses and thresholds under independent review before any confirmatory run.
- [ ] **T01.F10 — Program Progress Ledger** — Depends on: T01.F09, T10.F08
  - Goal: Deferred proof phase. Add campaign-grade evidence rows to the `docs/progress.md` table that T01.F12 creates.
- [x] **T01.F11 — Baseline Persistence Characterization** — Depends on: T10.F10
  - Goal: Measure how long populations persist at each world size under production defaults, so every later reading has a baseline.
- [x] **T01.F12 — Goal Profile, Basic Indicators, and Progress Table** — Depends on: T01.F11, T10.F09, T10.F11
  - Goal: Read diversity and cognition once per closed feature: a minutes-scale goal profile, a lineage-diversity indicator, a memory-sensitivity indicator, and `docs/progress.md` with one row per feature.
- [ ] **T01.F13 — Evolutionary Activity and Shadow-Model Baseline** — Depends on: T01.F12
  - Goal: Optional supplement to the diagnostic checkpoint. Is selection doing anything at all, measured as activity above a neutral shadow run with the same births and deaths?

## Notes for AI Agents

- T01.F11 runs entirely through the T10.F10 sweep profile so every number it reports is regenerable by one command; it adds no ad hoc scripts. It uses production defaults, sweeps world size (at least 128, 256, 512, and the 1600 default), density-matched founder counts, and at least three seeds for 2,000 to 5,000 ticks, and records extinction tick, peak and plateau population, births per 100 ticks, and mean energy. It may add sampled fields to the bench report but must not change simulation behavior. On 2026-09-03 every world at or below 512-by-512 went extinct by tick 300 while the default world plateaued near 5,000 creatures; T01.F11 must reproduce or supersede that result.
- T01.F12 establishes the baseline before the T11 foundation track and ecological mechanism work; its existing scope and dependencies are unchanged. It adds a `goal` profile to `v3-cli bench` on the existing sweep machinery with fixed, predeclared parameters chosen from the T01.F11 sweeps so that births and deaths both occur inside the horizon: the default 1600-by-1600 world is known to persist, and the spec predeclares seeds, horizon, and founders so one closure run takes minutes, shrinking them with a recorded reason if it does not. The gate profile is unchanged and stays seconds-scale in `make check` for the performance half; the goal profile runs once at closure through `make bench`, and its determinism is checked by a second run at closure whose `deterministic` block must match. It adds two indicator keys to the report, lineage diversity and memory sensitivity, and leaves `memory_dependence` and the other deferred keys `Undefined`, because those belong to the deferred comparator protocol. Ecological diversity: the count of surviving `lineage_id` values and the Shannon entropy of lineage abundance over the final population (lineage is founder clade identity, not ecotype, so this is a collapse tripwire and trend, not a strategy count). Cognition: memory sensitivity, the fraction of final creatures whose chosen action changes when the mesh is re-run on the same sensor snapshot with `shared_memory` intact, zeroed, and scrambled. The probe clones each creature per variant because the mesh mutates memory and may apply plasticity, scrambles with a fixed permutation or an RNG seeded from the run seed, and clones any RNG the mesh or action selection consumes, so the reading stays inside the byte-identical block; expect founders to read near zero, and a rise is the signal. It creates `docs/progress.md` as one Markdown table with one row per closed feature (feature, date, compute delta against both references, every indicator reading) and back-fills rows for T10.F10, T01.F11, T10.F09, and T10.F11 from their stored reports with goal indicators marked `Undefined`. Its spec carries a one-page indicator definitions section (what each indicator measures and what it cannot claim) that later indicator features extend; that section is the part of T01.F01 the program needs now. It changes no simulation behavior. `docs/progress/benchmark-series.json` is extended to hold two series, the existing gate series and a new goal series whose first reading is the baseline the master criteria compare against; the goal profile does not re-pin the gate epoch baseline. The closed T01.F11, T10.F09, and T10.F10 specs describe an earlier scope for T01.F12 (choosing the smallest persistent world and switching the gate profile to it); this note supersedes those forward references, and closed specs are not edited.
- T01.F13 is not in the execution order; it is available as an optional supplement to the T09.F08 diagnostic checkpoint when a second opinion on whether selection is acting is wanted. It depends on T01.F12 only and reports into the goal profile. T01.F13 adopts evolutionary activity statistics (Bedau and Packard) with a neutral shadow model, following Channon's 2024 Tokyo Type 1 procedure: report total cumulative activity, median normalized cumulative activity, and new activity for the real run against a shadow run in which births and deaths occur at the same rate without selection. The feature spec declares the heritable component definition (for example reachable-structure signatures or operator-level genome features) before any run. Its output is a per-run summary that T01.F07 extends and the ledger cites, and it declares the minimum horizon below which the T10.F10 report records its statistics as `Undefined`. A result showing no activity above the shadow model prompts examination of selection and exposure alongside the other diagnostics; it is not a metric to tune or a standalone verdict on brain capability.
- Extend Petri's applied-state telemetry and current reachability-aware functional-complexity analysis; do not treat either as sufficient evidence by itself.
- The cognitive profile is multi-axis rather than one weighted score. Report effect sizes and uncertainty for memory reset or scrambling, plasticity freeze, sensor lesions, live-versus-replayed agents, held-out transfer, and behavior-preserving controller minimization.
- T01.F05 owns the versioned comparator protocol and validates it against fixtures. It must give original and surrogate controllers observation/action parity, predeclare search and training budgets, define behavior-fidelity tolerance and held-out adaptive-performance margins, and return `Inconclusive` when the bounded search cannot establish equivalence or separation. T09 applies this protocol to evolved specimens; it does not define a second minimization method.
- T01 owns sampling and statistical policy, estimands, acceptance rules, and domain-neutral analysis definitions. T10 owns capture, serialization, replay mechanics, scheduling, provenance, and packaging; domain tracks own intervention semantics and conclusions.
- Archive and assay the inherited genome, newborn state, and mature learned state separately. Petri inherits shared memory while learned graph runtime normally resets at birth.
- Measures must be validated against constructed reference cases before they evaluate evolved organisms.
- T01.F08 is characterization evidence only. T01.F09 must seal a versioned protocol and its hashes before confirmatory data collection; an independent scientific reviewer, not the implementer, approves it using the predeclared smallest-effect, power or precision, and control-calibration rules. An unresolved scientific choice is a concrete blocker rather than permission to tune after seeing outcomes.
- The independent reviewer is defined concretely so T01.F09 cannot block forever: a separate agent session (a different model or a fresh session with no access to the implementing session's context) that receives only the sealed protocol document and its committed hash, with no result data. The user may substitute a human reviewer. The approval record names the reviewer session, the protocol hash, and the date, and is committed before any confirmatory run starts.
- T01.F10 belongs to the deferred proof phase. `docs/progress.md` itself is created by T01.F12; T01.F10 only adds campaign-grade rows to it, one compact snapshot after each designated evaluation campaign. For every program-level outcome, record the baseline, current estimate and uncertainty, direction (`Improving`, `Unchanged`, `Regressing`, or `Inconclusive`), evidence class, date, code revision, metric or protocol version, and supporting T10 result bundle. Preserve null results and regressions. Campaign rows are labeled by evidence class and sit beside the per-feature rows; the master criteria read the per-feature series. When a metric definition changes, start a new clearly labeled series rather than implying direct comparability.
- Keep the ledger as a plain Markdown document backed by compact result summaries. Do not add a dashboard, database, service, third-party tracking dependency, or composite success score.
- T01.F01 through T01.F10 are the deferred causal-proof phase named in the master roadmap. No mechanism feature depends on them, and none is planned until the user asks for that phase. T01.F01's indicator-definitions role is served by the T01.F12 spec; if the proof phase runs, T01.F01 adds uncertainty treatment and acceptance thresholds on top of those definitions.
- Research basis reviewed 2026-09-02: [MODES Toolbox](https://doi.org/10.1162/artl_a_00280), [Evolution of Integrated Causal Structures in Animats](https://doi.org/10.1371/journal.pcbi.1003966), [Simplification of genetic programs: a literature survey](https://doi.org/10.1007/s10618-022-00830-7), [Registered Reports](https://www.cos.io/initiatives/registered-reports), and [ADEMP-PreReg for simulation studies](https://doi.org/10.1037/met0000695). Added 2026-09-03 for T01.F13: Channon, "A Procedure for Testing for Tokyo Type 1 Open-Ended Evolution", Artificial Life 30(3), in the [2024 open-ended evolution special issue](https://doi.org/10.1162/artl_e_00445), and Bedau and Packard, "Measurement of evolutionary activity, teleology, and life" (Artificial Life II, 1992).
- Existing options considered were raw genome size, reachable structural size, information-theoretic brain scores, and causal behavioral assays. Use reachable size as one structural axis and causal assays as the primary evidence because architecture-specific or syntactic measures can misclassify bloat.
