# T19.F06 — Readings

Evidence for [the feature spec](../../specs/roadmap/t19-f06-retirement-and-observability.md).

## Codex challenge loop (Plan step)

Fresh read-only Codex Astra `xhigh` job per round; briefs kept outside the
worktree (session scratchpad). One table for every round.

| Round | Job | Finding | Disposition |
| --- | --- | --- | --- |
| 1 | `task-mue7225o-6ljwpt` (verdict `not-ready`) | blocking: invariants 1 and 8 overstated neutrality; the old width follows the normalized cap (`simulation.rs:1145`, `compound.rs:14`) and bounds the `sub_idx` draw (`operators.rs:183`) | Fixed: equivalence is stated exactly where the old normalized cap was 4 (defaults, both profiles, goal recipes, the seven recipes); elsewhere the width moves to 12 and mutated births remap, as the track's "held at today's four slots" instructs; a test running mutation below 4 that pins an outcome is re-pinned with the reason |
| 1 | same | blocking: the scan excluded a tracked seed record (`proptest-regressions/mutation/input_ref/tests.txt:7`) and ignored untracked files, while criterion 2 names whole directories | Fixed: `git grep --untracked` over the three directories with no exclusion; the seed record's comment is reworded and its `cc` seed kept; verification includes a newly created untracked file |
| 1 | same | blocking: invariants 5 and 6 disagreed on pass-ending routes; a route to a missing node is applied (`mesh.rs:462`), a pass cap follows an applied route (`mesh.rs:346`), a ramp-unaffordable hop is never recorded (`mesh.rs:379`) | Fixed: a hop records a route exactly when the production loop resolves one (neither exhausted nor `Decided`), as the observed mode does; missing-node and pre-cap routes kept; fixtures for all four cases; the inspector rule is "no route on a hop that applied none" |
| 1 | same | advisory: pattern omitted `ActionBid` and snake spellings | Adopted: `ActionBid`, `pop_action`, `execute_action_queue`, `write_direction_bid`, `write_world_action_meta`, `direction_bank`, and `push_action` not followed by `_` or a letter; negative controls `push_action_log` and `pushActions` in Verification |
| 1 | same | advisory: "zero delta" rests on rounded comparison output (`comparison.rs:523,549`) | Adopted: exact equality is a JSON diff of the summaries' `deterministic` sections against T19.F05's, expected to show only the new flags and the goal cases' `config_digest`; counters outside the summaries are not claimed |
| 1 | same | advisory: zero-hop passes (`mesh.rs:339`, `:353`, `:379`) would vanish from a hop-driven timeline | Adopted: one group per `tick.passes` record, empty groups kept, the recorded `hops` count shown |
| 1 | same | advisory: carried values are what was available, not proof of a read (`vm.rs:378`) | Adopted: labeled "decision-state values available to this dispatch" |
| 1 | same | advisory: the dashboard turns missing fields into zero (`index.html:371`) | Adopted in part: `mesh_summary` omits the key when a source row lacks it; the dashboard leaves scope (Non-Goals) instead of gaining an unmeasured rendering |
| 1 | same | advisory: counter equality does not exclude observation cost in the battery | Adopted: the wall row names the added battery set pass and reports a flag with host and phase, attribution pending, not dismissed |
| 2 | `task-mue7c1y6-vwmwf7` (verdict `not-ready`) | All three round-1 blocking findings confirmed fixed | — |
| 2 | same | blocking: the `deterministic`-diff rule allowed only the new flags and case digests, but the goal summary also embeds `recruitment_paths.config` (with the retired key) and its `config_digest` (`t19-f05-…-goal.json:136310`, `:136496`; `artifacts.rs:271`; `experiment.rs:1133`) | Fixed: the diff rule allows embedded config identity (every case `config_digest`, `recruitment_paths.config` losing the key, its digest); the Performance row names these and the gate's `measurement_evidence.effective_config_digest`; the recruitment-paths digest joins the before/after record. The digest is recorded only, never a seed (`experiment.rs:1133`, `recruitment.rs:281`) |
| 3 | `task-mue7hcfg-nnth2z` (verdict `ready`) | Round-2 blocking finding confirmed fixed; no new finding | — |
