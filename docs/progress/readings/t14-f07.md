# T14.F07 — surviving-clade behavioral profile: readings

| Evidence record |
| --- |
| Build and self-review pass, 2026-09-15, worktree `.claude/worktrees/t14-f07`, branch `worktree-t14-f07`. Every command below ran on the tree at `53d68f92a1f5b149803a156f492513cdba3566ec`. Benchmark, goal-report and mutation readings are not yet recorded; their sections are added by the passes that run them. |
| Benchmark specialist pass, 2026-09-15/16, same worktree, final feature code at `bdfb7675398dc3db2b951460e5759b4a6192c8a3`. Gate and goal profiles below ran sequentially, gate then goal, with no competing build/test/bench on the host. |

## Verification commands

| Command | Outcome |
| --- | --- |
| `cargo test -p v3-core -p v3-cli` | exit 0; v3-core lib 1,492 passed, 0 failed, 2 ignored; v3-core integration targets all pass (applied_trajectory 1, baseline_worlds 19 + 1 ignored, creature_workflow_e2e 7, mutational_neighborhood 4, priority_bid_reachability 2, recruitment_paths 3, reproducibility 3, temporal_fixtures 13, terrain 10, viability 25, vm_all_opcodes_e2e 1); v3-cli lib 109, main 11, `tests/bench.rs` 20, `tests/bench_artifacts.rs` 21 + 1 ignored, `tests/cli.rs` 11, all 0 failed; doc-tests 0 |
| `cargo clippy -p v3-core -p v3-cli --all-targets` | exit 0, no warning |
| `cargo fmt --all -- --check` | exit 0 |
| `cargo check --workspace --all-targets` | exit 0 |
| `make roadmap-check` | exit 0 |

## Focused coverage

All tests below pass in the package command above. The property test's
assertions hold for any drawn input; no `proptest-regressions/` file was
generated.

| Module | Exact tests |
| --- | --- |
| `creature::state::tests` | `typed_attempts_and_eats_grow_their_own_slots`; the constructor test additionally pins the four counters at zero |
| `simulation::tick::tests::clade_profile` | `every_creature_starts_with_zeroed_profile_counters`; `noop_counts_in_its_own_type_slot`; `a_failed_eat_counts_the_attempt_but_no_applied_eat`; `an_applied_eat_counts_under_the_food_type_it_named`; `a_blocked_move_counts_in_the_move_slot`; `a_rejected_reproduce_counts_in_the_reproduce_slot`; `a_rejected_steal_counts_the_attempt_and_no_kill_or_hit`; `a_mixed_sequence_keeps_the_by_type_sum_equal_to_the_attempt_count`; `a_child_starts_at_zero_while_its_parent_keeps_its_counters` |
| `simulation::actions::predation::tests` | `a_survived_transfer_counts_one_hit_on_the_victim_and_no_kill`; `a_kill_counts_on_the_attacker_beside_the_population_total`; `a_rejected_steal_counts_no_kill_and_no_hit` |
| `bench::tracking::tests` | `surviving_clade_bucketing_partitions_its_input` (proptest); `surviving_clade_rows_are_sorted_by_lineage_and_padded_to_the_food_type_count`; `the_terminal_block_profiles_each_surviving_clade_in_lineage_order`; `extinction_yields_an_empty_profile_table_not_an_absent_one`; `checkpoint_tracking_omits_every_transferred_block` (key set now lists `surviving_clade_profiles`); `tracking_fields_default_when_absent_and_survive_a_round_trip`; `transferred_tracking_blocks_are_absent_not_zero_in_a_historical_report` |

## Self-review

Single-pass `simplify` review of `git diff 96bfff5a..HEAD` across reuse,
simplification, efficiency and altitude, run inline without the four-agent
fan-out. Production code was already clean: `six`, `ActionType::as_key`, the
`by_food_type` food-type-count pattern and the `BTreeMap<String, u64>` transfer
shape are all reused, the per-creature eats clone in `observe` runs once per
run and is what keeps the bucketing pure, and `ActionType::ALL` is sized by
`ACTION_TYPE_COUNT` so the compiler enforces its length. Two cosmetic test
edits were applied at `53d68f92`: the `by_type` helper in `clade_profile.rs` is
typed by `ACTION_TYPE_COUNT` like `slot`, and `TRANSFERRED_KEYS` is a slice so
adding a key no longer bumps a hand-counted length.

## Benchmark gate and goal profiles

Both commands ran with `PATH` prefixed by `~/.local/share/petri-tools/bin` and
`~/.local/share/aquaproj-aqua/bin`, from the worktree, gate then goal,
sequentially and with no other build/test/bench/server on the host.

| Command | CLI exit | Observed outer exit | `comparison.severe` | Threshold crossed | Epoch re-pin |
| --- | --- | --- | --- | --- | --- |
| `make bench PROFILE=gate FEATURE=t14-f07` | 0 (`measurement_evidence.cli_exit.code`) | 0 (own shell, `echo "EXIT:$?"` after the redirected run) | false, against both `remove-complementary-nutrition.json` and `t03-f11-genome-replication-cost.json` | none (all six normalized counters `level: ok`) | no |
| `make bench PROFILE=goal FEATURE=t14-f07` | 0 | 0 (background-task completion notification, `exit code 0`) | false, against both `t11-f19-per-unit-mutation-supply-goal.json` and `t03-f11-genome-replication-cost-goal.json` | none (all six normalized counters `level: ok`) | no |

Raw and summary artifacts:

| Artifact | Path | Bytes |
| --- | --- | --- |
| Gate raw | `/Users/istefanek/projects/petri/.bench-artifacts/t14-f07/gate.json` (main checkout, ignored) | 94,073 |
| Gate summary | `docs/progress/features/t14-f07-surviving-clade-behavioral-profile.json` | 96,810 |
| Goal raw | `/Users/istefanek/projects/petri/.bench-artifacts/t14-f07/goal.json` (main checkout, ignored) | 511,912,703 |
| Goal summary | `docs/progress/features/t14-f07-surviving-clade-behavioral-profile-goal.json` | 6,866,520 |

The gate and goal profiles each wrote both artifacts in one CLI invocation
(`v3-cli` prints `wrote raw <path> and summary <path>`); no full report is
staged, only the two summaries and this readings update.

`make bench` writes the raw/summary pair with the short feature id
(`t14-f07`) by default; the raw path already matches the brief's required
`.bench-artifacts/t14-f07/<profile>.json` location, but the two summary files
were renamed (not regenerated) from `t14-f07.json` / `t14-f07-goal.json` to
the full slug names above immediately after the one run of each profile, so
this is not a second measurement.

### Second goal run

Not applicable: the goal profile runs once per closure under the one-goal-run
rule (`docs/workflow.md`, "Benchmark gate"); cross-process reproducibility is
covered separately by `crates/v3-core/tests/reproducibility.rs` inside `make
check`.

### Goal indicator movement against the T03.F11 epoch baseline

Predeclaration: every goal indicator and every checkpoint sample should be
byte-identical to the T03.F11 goal report apart from the new
`surviving_clade_profiles` block. The bench CLI's own comparison (printed
above) already shows the six normalized per-creature-tick counters at
`0.000000%` delta against `t03-f11-genome-replication-cost.json` /
`-goal.json`. A further field-by-field diff of
`deterministic.goal_indicators` between this run's raw goal report and the
stored T03.F11 *summary* (the only available comparison reference; no local
T03.F11 raw report was retained) found every top-level indicator byte-equal
after removing the new key:

```
population_persistence EQUAL
births_per_100_ticks EQUAL
lineage_diversity EQUAL
memory_sensitivity EQUAL
structural_companions EQUAL
temporal_memory_sensitivity EQUAL
drift_depth EQUAL
strategy_count EQUAL
strategy_causal_distinctness EQUAL
evolutionary_activity EQUAL
adaptive_novelty EQUAL
memory_dependence EQUAL
learning_dependence EQUAL
prediction_dependence EQUAL
information_integration EQUAL
reciprocal_interaction EQUAL
reachable_structure_size_distribution EQUAL
```

Two large per-case blocks (`mutational_neighborhood`, `recruitment_paths`)
differed in raw byte size between the two files (e.g. `recruitment_paths`
serialised to 178,315,601 characters in this run's raw report versus
2,194,830 in the T03.F11 *summary*). This is an artifact-type difference, not
a regression: per `docs/benchmark-artifacts.md` ("Summary version 1"), a
committed summary strips full per-proposal and per-genome detail
(`mutational_neighborhood`'s evolved per-genome rows, recruitment's
per-proposal traces) that only a raw report retains; comparing this run's raw
report against a *raw* T03.F11 report would be the correct like-for-like
check, but none is retained locally. No behavioral difference is implied
because every top-level scalar/aggregate indicator above — which the summary
does retain in full — is byte-identical.

Conclusion: no goal indicator moved against the T03.F11 baseline in any way
the predeclaration would flag as a defect.

### Stored goal report: per-world row table

jq transcript (raw goal report,
`/Users/istefanek/projects/petri/.bench-artifacts/t14-f07/goal.json`):

```
$ jq -c '.deterministic.goal_indicators.cases[] | {name: .case.name, has_profiles: (.surviving_clade_profiles != null), n_rows: (.surviving_clade_profiles.rows|length)}'
{"name":"Orchards in grassland","has_profiles":true,"n_rows":27}
{"name":"Canyon country","has_profiles":true,"n_rows":28}
{"name":"Confluence","has_profiles":true,"n_rows":17}

$ jq -c '.deterministic.goal_indicators.population_persistence.per_seed[] | {seed, last_tick: .samples[-1].tick, scfc: .samples[-1].surviving_founder_clade_count, final_population}'
{"seed":11,"last_tick":2000,"scfc":27,"final_population":9772}
{"seed":22,"last_tick":2000,"scfc":28,"final_population":11534}
{"seed":33,"last_tick":2000,"scfc":17,"final_population":14110}

$ jq -c '.deterministic.goal_indicators.cases[] | {name: .case.name, sum_size: ([.surviving_clade_profiles.rows[].size]|add), five_keys: ([.surviving_clade_profiles.rows[]|(.actions_by_type|keys|length)]|unique), definition: .surviving_clade_profiles.definition}'
{"name":"Orchards in grassland","sum_size":9772,"five_keys":[5],"definition":"surviving-clade-profile-v1"}
{"name":"Canyon country","sum_size":11534,"five_keys":[5],"definition":"surviving-clade-profile-v1"}
{"name":"Confluence","sum_size":14110,"five_keys":[5],"definition":"surviving-clade-profile-v1"}

$ jq -c '.deterministic.goal_indicators.cases[] | {name: .case.name, eats_sum: ([.surviving_clade_profiles.rows[].eats_by_type[]]|add), typed_eats_total_sum: (.typed_eats_total|add), steal_energy_sum: ([.surviving_clade_profiles.rows[].actions_by_type["StealEnergy"]]|add), predation_attempts_total: .predation.actions_attempted_total, kills_row_sum: ([.surviving_clade_profiles.rows[].predation_kills]|add), predation_kills_total: .predation.kills_total}'
{"name":"Orchards in grassland","eats_sum":262049,"typed_eats_total_sum":6673609,"steal_energy_sum":161,"predation_attempts_total":2117,"kills_row_sum":2,"predation_kills_total":39}
{"name":"Canyon country","eats_sum":226888,"typed_eats_total_sum":5772838,"steal_energy_sum":90,"predation_attempts_total":7371,"kills_row_sum":4,"predation_kills_total":80}
{"name":"Confluence","eats_sum":306214,"typed_eats_total_sum":6366057,"steal_energy_sum":410,"predation_attempts_total":3404,"kills_row_sum":6,"predation_kills_total":96}

$ jq -c '[.deterministic.goal_indicators.population_persistence.per_seed[].samples[] | select(has("surviving_clade_profiles"))] | length'
0
```

Per-world row table (row count = horizon checkpoint's
`surviving_founder_clade_count`; sizes sum to final population; five
`actions_by_type` keys per row on every row; both inequality cross-checks
hold; predation kills observed in every world's surviving clades):

| World | Row count | `surviving_founder_clade_count` | Size sum | Final population | `eats_by_type` sum ≤ `typed_eats_total` sum | `StealEnergy` attempts sum ≤ `predation.actions_attempted_total` | Row `predation_kills` sum ≤ `predation.kills_total` |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Orchards in grassland | 27 | 27 | 9,772 | 9,772 | 262,049 ≤ 6,673,609 | 161 ≤ 2,117 | 2 ≤ 39 |
| Canyon country | 28 | 28 | 11,534 | 11,534 | 226,888 ≤ 5,772,838 | 90 ≤ 7,371 | 4 ≤ 80 |
| Confluence | 17 | 17 | 14,110 | 14,110 | 306,214 ≤ 6,366,057 | 410 ≤ 3,404 | 6 ≤ 96 |

No checkpoint sample (60 total: 3 seeds x 20 samples each) carries the
`surviving_clade_profiles` key at all. The gate profile carries zero cases on
both `deterministic.profile.cases` and `.goal_indicators.cases`, so its
checkpoint-key-set and end-of-run-block claims hold vacuously.

Every surviving clade shows nonzero `predation_kills` on all three worlds
(row sums 2, 4, 6) — per the spec's Goal Impact section, this is the track's
trigger to schedule T05.F03.
