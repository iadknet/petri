# T19.F05 — Readings

Evidence for [the feature spec](../../specs/roadmap/t19-f05-decision-state-inputs.md).

## Codex challenge loop (Plan step)

Fresh read-only Codex Astra `xhigh` job per round; briefs kept outside the
worktree (session scratchpad). One table for every round.

| Round | Job | Finding | Disposition |
| --- | --- | --- | --- |
| 1 | `task-mudakb2b-zolzcp` (verdict `not-ready`) | blocking: invariants 5 and 7 and Verification claimed "one draw per reference, no sub-draw" and unchanged draw counts; `sampling.rs:48` draws again for the upstream slot and `:57` for a food type, and the remap moves which branch a state lands in | Fixed: the five new entries cost the key draw alone, existing branches keep their sub-draws, the branch-specific tests take `27` for `22`; the "unchanged count" claim deleted |
| 1 | same | advisory: V1's control also removed A's vote and V5 had no control; with A retained and the read replaced by `0`, V1 yields `[Eat, Move N]` | Adopted: every control is the same genome with the read replaced by the constant `0`; V1 control `[Eat, Move N]`, V5 control `NoOp` on both ticks |
| 1 | same | advisory: invariant 1's out-of-range rule contradicted the `HopsThisTick` row (dynamic scalars ignore `sub_idx`, `inputs.rs:50`) | Adopted: the `0.0` rule is restricted to the four compounds; the scalar ignores `sub_idx` |
| 1 | same | advisory: no case exercised the VM staging boundary (`vm.rs:423` local contribution, visible after dispatch at `mesh.rs:405`) or traced equivalence | Adopted: V6 added (reads before and after own `AddVote` are `0`, the revisit reads `1`, traced and untraced agree) |
| 1 | same | advisory: the per-hop cost claim forbade the queue-clone pattern the graph's post-evaluation contexts use (`cgp/execute.rs:229`, `:292`, `:314`) | Adopted: invariant 1 and the predeclaration allow at most two 27-value copies per graph dispatch where the queue is already cloned, no borrowing refactor |
| 2 | `task-mudaqouo-ootiey` (verdict `ready`) | All five round-1 dispositions confirmed; no new blocking finding | — |
| 2 | same | advisory: V6's "every revisit reads 1" needed a per-pass qualification (`begin_pass` clears the vector; a second pass follows the `Eat` commit) | Adopted: V6 is two 64-hop passes, both reads `0` on each pass's first dispatch and `1` thereafter, pass 1 commits `Eat`, pass 2 ends `NoDecision`, queue `[Eat]` |

## Worked cases (build commit `4eeb6a99`)

Each case runs the plain, observed, and traced executors from one state and
asserts they agree (`vote_test_support::run_tick`); each control is the same
genome with the read replaced by the constant `0`. Defaults: `max_mesh_hops`
64, `max_actions_per_turn` 10.

| Case | Test | Control | With the read |
| --- | --- | --- | --- |
| V1 | `runtime::decision_input_tests::v1_action_votes_let_a_committed_vote_inhibit_a_later_node` (VM) | `[Eat, Move N]`, commits `Eat, Move N, —` | `[Eat]`, commits `Eat, —`, `NoDecision`; B's pass-1 `Move N` vote reads `0` |
| V2 | `…::v2_previous_pass_votes_carry_the_last_pass_decision_into_the_next` (graph) | `[Eat]` | `[Eat, Move N]`; `Move N` reads `0` in pass 1 and `1` in pass 2; pass 3 `NoDecision` |
| V3 | `…::v3_commit_counts_let_a_plan_stop_after_one_commit` (graph) | `[Eat, Eat]` | `[Eat]`; pass 2 votes `Eat 1`, effective `0`; bars `[1, 0, 0, 0]` |
| V4 | `…::v4_hops_this_tick_lets_deliberation_end_the_tick` (VM) | `[Move N, Move N]`, 192 hops, `NoDecision`, `Terminate −64` | `[Move N]`, 128 hops, `TerminateVoted`; pass 1 ends `Terminate 0`, pass 2 `PassCapReached` with `Terminate 64` against `Move` effective `1` |
| V5 | `simulation::tick::tests::previous_outcome::v5_previous_outcome_moves_on_the_tick_after_a_successful_noop` | `NoOp` on both ticks, position unchanged | tick 1 `NoOp` (stores `ActionSuccess 1`), tick 2 moves north |
| V5 | `…::an_eat_on_food_is_read_next_tick_as_its_scaled_outcome` | — | `assemble_static_inputs` reads `[ΔE / max_energy, 1, 0, 0]` with `ΔE > 0` |
| V5 | `…::a_newborn_stores_zeros_and_its_parent_counts_the_birth` | — | parent stores `OffspringSuccess 1`, `ActionSuccess 1`; the newborn stores and reads zeros |
| V6 | `runtime::decision_input_tests::v6_a_dispatch_never_reads_the_vote_it_is_staging` (VM) | — | two 64-hop passes; both reads `0` on hops 0 and 64, `1` on every other hop; queue `[Eat]`, `NoDecision`; the reads summed into shared memory give `126, 126` in every mode |

Supporting tests: `runtime::inputs::tests::decision_compounds_read_their_vector_and_zero_past_the_width`
(proptest), `…::hops_this_tick_is_the_raw_hop_count_and_ignores_sub_idx`,
`sensors::static_inputs::tests::previous_outcome_read_scales_and_clamps_the_energy_channels`,
`mutation::sampling::tests::typed_sampler_draws_each_decision_state_input_with_one_draw`,
`mutation::input_ref::tests::decision_state_kinds_follow_the_swap_table`,
`creature::sensor_census::tests::each_decision_state_input_is_counted_once_per_reference`,
and in v3-cli `bench::tracking::tests::decision_input_rows_count_each_referencing_creature_once`
and `…::a_census_without_decision_rows_still_loads`.

## Pins (re-pinned after two agreeing runs)

Before is `bb4fb0a4` (the spec commit on the T19.F04 closure).

| Pin | Before | After |
| --- | --- | --- |
| `viability::founder_only_trajectory_digest_is_pinned` | green | green, unchanged |
| `baseline_worlds::legacy_default_short_run_identity` | `14378788429370811441` | `7548337837478651677` |
| `applied_trajectory::accounting_preserves_pre_feature_sampled_trajectories_and_actions` | `dcf75b04…e1e8` | `14541daa…7226` |
| `applied_trajectory::mutation_on_applied_trajectory_guard_is_pinned` | `5e094be3…4a68` | `03761998…b431` |
| Recruitment-path `InputRef.Add` seeds (cue; ring) | `1`; `68` | `201`; `1` |
| Drift walk `the_walk_dates_modules_from_their_birth_and_from_every_refresh` (dispatched; dispatch reached, median, censored present) | `5`; `7, 4, 5` | `4`; `6, 5, 6` |

With the draw still at 22 and the Phase 2.5 store in place, both viability
pins and the legacy identity held, so the store and the new snapshot field
are inert until a genome references them. `FOUNDER_GENOME_SIZE_UNITS` (97),
`config_digest`, and the v3-cli recipe pin did not move.

## Benchmark runs

Both runs measured commit `13355482` (worktree HEAD), host
`Isaacs-MacBook-Pro-2.local` (matches the T19.F04 host), against reference
`t19-f04-vote-based-action-selection(-goal).json`.

### Gate — `make bench PROFILE=gate FEATURE=t19-f05-decision-state-inputs`

- CLI exit 0; outer (`make`) exit 0. `severe=false`.
- All counters `ok` except `pass_cap_hits` (+35.11%, `flag`, below the 50%
  severe line); wall_clock `ok` (+20.64%, current 0.002059 ms/creature-tick
  vs reference 0.001707).
- Raw: `.bench-artifacts/t19-f05-decision-state-inputs/gate.json`,
  100,104 bytes,
  `sha256:94a15fbfa6593491289b4c3179470141b1becb360342e6c4c077f4804738c20b`.
- Summary: `docs/progress/features/t19-f05-decision-state-inputs.json`,
  103,004 bytes,
  `sha256:5088c5f0a6eb43746fe368e0eefaa24311fa42403bf52c3c7bede3202baca8b2`.
- `decision_inputs` census: five rows (`ActionVotes`, `PreviousPassVotes`,
  `CommitCounts`, `HopsThisTick`, `PreviousOutcome`) present in every
  goal-world sample recorded inside the gate run's own population-persistence
  block (drift-scale, tick-0 samples), all zero except `HopsThisTick`
  (17 / 24 / 33 creatures across the three seeds at that early tick).

### Goal — `make bench PROFILE=goal FEATURE=t19-f05-decision-state-inputs` (run once)

- CLI exit 3; outer (`make`) exit 2 (`error: severe work-counter regression
  against a stored reference`). `severe=true`, driven by `decided_passes`
  (+255.43% aggregate; per-world +1456.03% Orchards, −87.77% Canyon,
  −69.38% Confluence — a near-zero counter, `current`/`reference` in the
  0.00002–0.001 range per creature-tick). `pass_cap_hits` also `flag`
  (+22.68%, below severe). Every other counter `ok`; wall_clock `ok`
  (−6.61%, current 0.010677 ms/creature-tick vs reference 0.011433).
  This is recorded factually and not interpreted further; it is outside this
  spec's predeclaration (no direction was declared for `decided_passes`, and
  a mechanism cost there was declared not to fit and to be escalated).
- Raw: `.bench-artifacts/t19-f05-decision-state-inputs/goal.json`,
  660,727,787 bytes,
  `sha256:a4069268340cbd9b2bb3be242ec41eff38220ba31ac2f1b51feba8725c4d738e`.
- Summary: `docs/progress/features/t19-f05-decision-state-inputs-goal.json`,
  7,727,539 bytes,
  `sha256:f7dd28720f4d9336d89ce45c8bcafc0f5a2209e94e0e3cf815505cfa5f99c922`.
- Wall caps: founder 252.9 ms (cap 10 s), evolved-neighborhood total
  3,519.1 ms (cap 180 s), drift-depth 18,712.3 ms (world cap 30 s/world, 3
  worlds), recruitment-paths 12,004.9 ms (cap 120 s/report) — all within cap.
- `sensor_census.decision_inputs`, final checkpoint (tick 2000) per world:
  Orchards (seed 11) `ActionVotes` 2, `PreviousPassVotes` 0, `CommitCounts`
  0, `HopsThisTick` 119, `PreviousOutcome` 0; Canyon (seed 22) 3, 1, 0, 316,
  4; Confluence (seed 33) 0, 0, 0, 124, 0. All five references present with
  no direction predeclared.
- Persistence (final / minimum / plateau): Orchards 2924 / 46 / 3629.5 vs
  T19.F04 1331 / 34 / 3032.9; Canyon 3482 / 739 / 4430.4 vs 3961 / 791 /
  3736.7; Confluence 4261 / 98 / 9228.1 vs 5464 / 155 / 5014.7. No world's
  final population fell below half of T19.F04's, so the persistence
  investigation trigger was not met.
- Births probe / drift walk, per case (`founder_changed_per_all_births`,
  `founder_dead_per_all_births`, `evolved_changed_per_mutated_births`,
  `evolved_dead_per_mutated_births`, `drift_changed_per_all_births_at_2000`):
  Orchards 0.496403 / 0 / 0.313422 / 0.002950 / 0.0035; Canyon 0.489209 / 0 /
  0.299440 / 0.003731 / 0.0025; Confluence 0.496403 / 0 / 0.349233 /
  0.000852 / 0.0035 — against T19.F04's 0.496403 / 0 / 0.364964 / 0 (Orchards),
  0.489209 / 0 / 0.222506 / 0.001279 (Canyon), 0.496403 / 0 / 0.394632 / 0
  (Confluence), and drift references 0.007 / 0.004 / 0.007. Founder-half
  changed/dead unchanged; `evolved_dead_per_mutated_births` moved from zero
  to non-zero in Orchards and Confluence (recorded factually, not
  interpreted).
- `config_digest`/`inputs_changed`: `false` for all three cases (unchanged);
  `effective_config_digest` recorded in `measurement_evidence`.
