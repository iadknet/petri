# T14.F08 — sensor usage census: measured readings

| Evidence record |
| --- |
| Measurement pass, 2026-09-12, worktree `.claude/worktrees/t14-f08`, branch `worktree-t14-f08`. **Re-measured on the shipped code at `887ed8c0`:** the gate profile only — [gate](../features/t14-f08-sensor-usage-census.json) records `git_revision` `887ed8c071f4c3cfc244512de3ac09097f7c3843` (1,690 lines, 54.3 KB). **Not re-measured:** the [goal](../features/t14-f08-sensor-usage-census-goal.json) report, which still records `git_revision` `6112143fd546ee6359ca047ccc00321e9de70aee` and therefore predates the post-review refactors at `7004b1e2` and `2f9d437b`; those change no stored value, and the goal profile runs once per closure. It is 78,187 lines, 3.24 MB — 3,300 lines above T14.F04's 74,887, one census block on each of 60 checkpoint samples. Every goal figure below is from that earlier run; every gate figure is from the `887ed8c0` rerun. |

## Verification commands

| Command | Outcome |
| --- | --- |
| `cargo test -p v3-core -p v3-cli` | exit 0; 1,323 v3-core unit tests and every v3-cli target pass, 0 failed, 2 ignored |
| `cargo clippy -p v3-core -p v3-cli --all-targets` | exit 0, no warning |
| `cargo fmt --all -- --check` | exit 0 |
| `make bench PROFILE=gate FEATURE=t14-f08-sensor-usage-census` (rerun at `887ed8c0`) | exit 0; `severe=false` against both references; every compute metric `level=ok`; `wall_clock` `level=ok` too (-10.3 %, +0.6 %); no threshold crossed; wall total 598.15 ms |
| `make bench PROFILE=goal FEATURE=t14-f08-sensor-usage-census` | exit 0; `severe=false` against both references; `wall_clock` `level=flag` (+41.0 %, +29.2 %); wall total 636,820.52 ms (10.61 min); the only other non-`ok` level is the inherited `plasticity_updates` flag against T12.F04 |
| `make roadmap-check` | exit 0 |
| `MUTANTS_ITERATE=0 make rust-mutants` at `8f1c928f` | `57 mutants tested in 8m: 2 missed, 33 caught, 22 unviable`; both survivors listed below |
| `MUTANTS_ITERATE=0 make rust-mutants` at `b8399dd2` | `57 mutants tested in 8m: 35 caught, 22 unviable`; no survivor, no timeout |

The cargo commands above were rerun at `b8399dd2`, the commit that adds the
split-count test.

`make check` is not part of this pass; the user runs it at closure.

## Mutation gate

Output `~/.local/share/petri-tools/mutants/t14-f08/mutants.out`, diff against
`fc6f1f11`. The first fresh run's two survivors, both **killed**:

| Survivor | Why it survived | Resolution |
| --- | --- | --- |
| `crates/v3-cli/src/bench.rs:1720:45: replace += with *= in SensorCensus::observe` | `creatures_reading_shared_memory` starts at `0`, so `*=` pins it to `0`. The real-path test compared the horizon census against `SensorCensus::observe` of an oracle run — the same mutated code on both sides — and its other assertions are upper bounds that `0` satisfies. | `each_stateful_split_counts_the_creatures_that_qualify_for_it` asserts the split reads exactly `1` |
| `crates/v3-cli/src/bench.rs:1721:42: replace += with *= in SensorCensus::observe` | same, for `creatures_with_stateful_node`; no creature in the real-path run holds a live stateful compute node, so that count is `0` there at every checkpoint and no real run can distinguish the operators | same test, which injects one stateful-node holder |

No production code changed during triage, and no `#[mutants::skip]` or
`exclude_re` was added. The second fresh run was taken because the first run's
test set no longer matches the shipped one; `make rust-mutants` itself prints
that an incremental pass is not closure evidence.

## Tests added by this feature

| Test | Module | Binds |
| --- | --- | --- |
| `the_founder_genome_references_its_world_inputs_through_graph_sinks` | `v3-core creature::sensor_census::tests` | the founder's graph-sink reach a VM-only census would miss |
| `a_key_referenced_by_several_instructions_and_edges_is_counted_once` | same | one creature counts once per key, on both backends |
| `a_reference_on_an_unreachable_graph_compute_node_is_not_counted` | same | graph liveness |
| `a_dead_vm_read_input_is_not_counted_but_a_live_one_is` | same | VM liveness |
| `a_reference_on_a_node_outside_the_reachable_set_is_not_counted` | same | mesh reachability |
| `vm_slot_loads_are_stateful_reads_and_slot_writes_are_not` | same | the three slot loads vs the three writes |
| `a_shared_memory_edge_is_a_stateful_read_for_either_previous_flag` | same | same-tick reads are stateful too |
| `a_live_stateful_compute_node_is_stateful_and_plasticity_alone_is_not` | same | the four stateful node kinds; plasticity stays T14.F05's |
| `an_unreachable_stateful_compute_node_is_not_counted` | same | liveness applies to the stateful reading too |
| `world_inputs_reach_action_slot_and_execute_gate_edges` | same | action-bank and execute-gate wiring |
| `a_non_world_reference_is_not_a_world_input` | same | introspection references are not world inputs |
| `the_key_universe_is_seven_plus_three_per_food_type_with_labelled_families` | same | universe size and label uniqueness |
| `the_graph_census_is_exactly_the_world_keys_behind_the_live_refs` (proptest) | same | the census equals the drawn live world keys, duplication-free |
| `the_key_universe_covers_every_food_type_once` (proptest) | same | `7 + 3n` keys, all labels distinct |
| `the_real_run_path_carries_a_census_of_the_whole_key_universe_at_every_checkpoint` | `v3-cli bench::tests` | census on every checkpoint of a real `run_one_seed`, key universe in order, per-key and stateful counts bounded by population, horizon checkpoint equal to an independent oracle, founder `FoodHere:0` above zero |
| extinction checkpoint arm | same | an empty population is all-zero rows, not absent |
| legacy-wire arm | same | a report predating the block loads with `sensor_census` absent |
| checkpoint round-trip proptest arm | same | `sensor_census` survives the JSON round trip and reads absent when stripped |
| `each_stateful_split_counts_the_creatures_that_qualify_for_it` | same | each stateful split sums one per qualifying creature, over a population holding one shared-memory reader, one stateful-node holder and one creature reading neither |

## Goal comparison transcript (tail of log)

```
wrote docs/progress/features/t14-f08-sensor-usage-census-goal.json
compared against docs/progress/features/t12-f04-baseline-world-set-goal.json: severe=false
  mesh_hops: current=2.258369 reference=Some("2.247936") delta%=Some("0.464115") level=ok
  vm_steps: current=23.478709 reference=Some("23.339759") delta%=Some("0.595336") level=ok
  graph_relax_iters: current=1.024449 reference=Some("1.028540") delta%=Some("-0.397748") level=ok
  plasticity_updates: current=0.066183 reference=Some("0.046976") delta%=Some("40.886836") level=flag
  actions_applied: current=1.360568 reference=Some("1.371176") delta%=Some("-0.773642") level=ok
  births: current=0.019659 reference=Some("0.018609") delta%=Some("5.642431") level=ok
compared against docs/progress/features/t14-f01-closure-comparison-and-indicator-coverage-integrity-goal.json: severe=false
  mesh_hops: current=2.258369 reference=Some("2.258369") delta%=Some("0.000000") level=ok
  vm_steps: current=23.478709 reference=Some("23.478709") delta%=Some("0.000000") level=ok
  graph_relax_iters: current=1.024449 reference=Some("1.024449") delta%=Some("0.000000") level=ok
  plasticity_updates: current=0.066183 reference=Some("0.066183") delta%=Some("0.000000") level=ok
  actions_applied: current=1.360568 reference=Some("1.360568") delta%=Some("0.000000") level=ok
  births: current=0.019659 reference=Some("0.019659") delta%=Some("0.000000") level=ok
```

Every compute metric reads 0.000000 % against the T14.F01 goal reference. The
`plasticity_updates` flag against T12.F04 is the same inherited 0.066183 that
T14.F01, T14.F02 and T14.F04 already store; it is not a new crossing. The
harness prints no line for `wall_clock`, so the level in the stored `comparison`
block is the verdict for it — see the next section.

The predeclared direction of **none** is checked on the indicators themselves,
not only on the compute metrics and the severe flag: deleting `sensor_census`
from every checkpoint sample of this report's `deterministic.goal_indicators`
and diffing the sorted block against T14.F04's stored goal report gives an empty
diff over 73,226 lines. Lineage diversity, memory sensitivity, temporal memory
sensitivity, mutational neighborhood, drift depth, population persistence and
births per 100 ticks are unchanged value for value, as are the other eleven
indicator blocks.

```
jq -S 'del(.deterministic.goal_indicators.population_persistence
           .per_seed[].samples[].sensor_census)
       | .deterministic.goal_indicators' <f08-goal> > a.json
jq -S '.deterministic.goal_indicators' <f04-goal> > b.json
diff a.json b.json   # no output
```

## Caps and thresholds

| Cap | Limit | Gate reading | Goal reading |
| --- | --- | --- | --- |
| `environment.neighborhood_evolved_wall_clock_ms_total` | 180,000 ms | not run (`-0.0`) | 659.74 ms |
| `environment.neighborhood_founder_wall_clock_ms` | 10,000 ms | 58.32 ms | 230.65 ms |
| Total goal-profile investigation | 15 min | — | 636.82 s (10.61 min) |
| `environment.drift_depth_wall_clock_ms` | no cap | not run | 25,719.08 ms |
| Wall ms per creature-tick | normalized thresholds | 0.001399 | 0.010445 |
| `severe` on any reference | must be false | false on both | false on both |
| Epoch re-pin | none authorized | not re-pinned | not re-pinned |
| Non-`ok` levels in the stored `comparison` block | — | none: 12 metric `ok`, 2 `wall_clock` `ok` | 11 metric `ok`, 1 inherited `plasticity_updates` `flag`, 2 `wall_clock` `flag` |
| `deterministic.goal_indicators` vs T14.F04's goal report, `sensor_census` deleted | predeclared direction: none | — | byte-identical, empty `diff` |

## Wall-clock delta, and how much of it is the census

The predeclaration expects a small compute cost, larger than T14.F04's. On the
gate rerun at `887ed8c0` the wall clock stays `level=ok` against both references;
on the goal profile, measured earlier at `6112143f` on a loaded host, it crossed
`flag` and stayed below `severe`:

| Reading | T14.F01 | T14.F04 | T14.F08 | vs T14.F04 |
| --- | --- | --- | --- | --- |
| Gate wall total (rerun at `887ed8c0`) | 594.73 ms | 540.86 ms | 598.15 ms | +10.6 % |
| Gate ms per creature-tick (rerun) | 0.001391 | 0.001265 | 0.001399 | +10.6 % |
| Goal wall total (at `6112143f`) | — | 460,451.08 ms | 636,820.52 ms | +38.3 % |
| Goal ms per creature-tick (at `6112143f`) | 0.008082 (T12.F04) | 0.007552 | 0.010445 | +38.3 % |

The gate rerun replaces a superseded reading: the earlier gate run at `6112143f`
recorded 1,009.15 ms total and a `flag` on `wall_clock` (+51.3 %, +69.7 %) while
the host was under concurrent load. On a quiet host the same code measures within
0.6 % of the T14.F01 gate reference.

Most of the goal delta is not attributable to the census. The census runs only
inside checkpoint sampling, which is outside the five timed tick phases and
outside the neighborhood batteries and the drift walk entirely — yet those
untouched measurements rose by the same order:

| Measurement, census cannot run in it | T14.F04 | T14.F08 | Delta |
| --- | --- | --- | --- |
| Goal `neighborhood_founder_wall_clock_ms` | 97.82 ms | 230.65 ms | +135.8 % |
| Goal `neighborhood_evolved_wall_clock_ms_total` | 426.49 ms | 659.74 ms | +54.7 % |
| Goal `drift_depth_wall_clock_ms` | 18,170.11 ms | 25,719.08 ms | +41.5 % |

Gate rerun, per seed, timed tick phases (`actions + cognition + reward_learning +
sensor_assembly + world_update`) against the rest of the loop, which is where
checkpoint sampling and the census sit:

| Seed | Report | Total ms | Phase sum ms | Remainder ms |
| --- | --- | --- | --- | --- |
| 11 | T14.F04 | 192.26 | 179.94 | 12.32 |
| 11 | T14.F08 (`887ed8c0`) | 212.80 | 193.24 | 19.56 |
| 22 | T14.F04 | 175.44 | 164.21 | 11.22 |
| 22 | T14.F08 (`887ed8c0`) | 191.90 | 173.15 | 18.75 |
| 33 | T14.F04 | 173.17 | 161.25 | 11.91 |
| 33 | T14.F08 (`887ed8c0`) | 193.45 | 173.48 | 19.97 |

The remainder grew by 7.2, 7.5 and 8.1 ms per seed — an upper bound on the
census's cost, since the remainder also covers the rest of the sampling. The
timed phases, which contain no census work at all, grew by 13.3, 8.9 and 12.2 ms
per seed over the same runs.

The same split on the goal profile, where the census does its real work (60
checkpoints against populations up to 100,000):

| Seed | Report | Total ms | Phase sum ms | Remainder ms |
| --- | --- | --- | --- | --- |
| 11 | T14.F04 | 173,096.4 | 169,449.0 | 3,647.5 |
| 11 | T14.F08 | 241,954.9 | 235,360.7 | 6,594.1 |
| 22 | T14.F04 | 118,182.2 | 114,888.9 | 3,293.3 |
| 22 | T14.F08 | 189,115.9 | 183,042.5 | 6,073.4 |
| 33 | T14.F04 | 169,172.4 | 165,097.9 | 4,074.5 |
| 33 | T14.F08 | 205,749.8 | 199,781.1 | 5,968.7 |

Per-seed total deltas are +68,858, +70,934 and +36,577 ms; the remainder that
holds checkpoint sampling accounts for +2,947, +2,780 and +1,894 ms of them —
4.3 %, 3.9 % and 5.2 %. The census's own cost is bounded above by those figures,
about 1.2 % of the 636.8 s goal run, since the remainder also covers the rest of
the sampling. The other 95 % of the delta is in the timed tick phases and in the
neighborhood batteries and drift walk, none of which run a census.

The goal profile's flagged percentages were measured on a loaded host, so they
describe the machine more than the feature; the gate rerun on a quiet host, where
`wall_clock` reads `ok`, supports that reading. The goal profile runs once per
closure and was not re-measured.

## Checkpoint census in the stored reports

Seed-to-world mapping from `deterministic.goal_indicators.cases[]`: 11 =
Orchards in grassland (2 food types), 22 = Canyon country (1 food type), 33 =
Confluence (2 food types).

| World case (seed) | Checkpoint samples | Samples carrying `sensor_census` | `world_inputs` rows per sample | Top-level sample keys |
| --- | --- | --- | --- | --- |
| Orchards in grassland (11) | 20 | 20 | 13 | 18 |
| Canyon country (22) | 20 | 20 | 10 | 18 |
| Confluence (33) | 20 | 20 | 13 | 18 |

The row count is exactly `7 + 3 x food_type_count` in every sample of every
case: the whole key universe of that world, so a `0` reads as "no living
creature references this key" rather than as an absent key. The gate report at
`887ed8c0` carries 1 checkpoint sample per seed, each with a census of 10 rows
(the gate profile's single-food-type world), unchanged from the superseded run.

### Sample key diff against T14.F04's goal report

Union of the checkpoint sample key sets over all samples of all three cases in
each report, `diff` of the sorted lists (`<` = T14.F04, `>` = T14.F08):

```
14a15
>   "sensor_census",
```

Additions only: T14.F04's 17 keys are all present, none removed, renamed or
re-nested, 17 -> 18 keys.

### First checkpoint (tick 100) — founder-dominated population

| Seed | Population | `FoodHere:0` | `NeighborFoodRing:0` | `NearbyCreatureCore` | shared memory | stateful node | any stateful |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 11 | 100,000 | 98,313 | 98,440 | 242 | 1,819 | 330 | 2,144 |
| 22 | 45,014 | 44,406 | 44,648 | 137 | 1,078 | 186 | 1,260 |
| 33 | 100,000 | 98,163 | 98,341 | 226 | 2,016 | 269 | 2,280 |

The founder wires its world inputs straight to its graph output sinks rather
than through a compute node, so the two founder keys read 98.2–98.7 % of the
living population rather than zero — the graph-backend reach the census was
built to see. The second food type of the two-type worlds is already referenced
by a few hundred mutants at tick 100 (`FoodHere:1` 613 and 526), and the six
non-food keys sit in the low hundreds.

### Last checkpoint (tick 2000) — evolved population

| Seed | Population | `FoodHere:0` | `NeighborFoodRing:0` | `NearbyCreatureCore` | shared memory | stateful node | any stateful |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 11 | 11,313 | 7,683 | 10,845 | 511 | 3,423 | 162 | 3,474 |
| 22 | 7,090 | 6,467 | 7,031 | 37 | 2,974 | 284 | 2,984 |
| 33 | 9,995 | 7,687 | 9,622 | 2,306 | 3,616 | 212 | 3,804 |

No per-key count exceeds its checkpoint's population in any of the 60 samples,
and the combined stateful count covers each split, as the real-path test
asserts on every checkpoint of its own run.
