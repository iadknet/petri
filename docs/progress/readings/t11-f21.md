# T11.F21 — Per-Direction Motor Output readings

Test names, transcripts, and tables for the feature spec
[`docs/specs/roadmap/t11-f21-per-direction-motor-output.md`](../../specs/roadmap/t11-f21-per-direction-motor-output.md).
Implementation and self-review run from the worktree
`.claude/worktrees/t11-f21` (branch `worktree-t11-f21`, on top of main
`7c738685`). Benchmark and mutation sections are filled by their
specialists.

## Test names behind the Verification items

| Verification item | Tests (module or file) |
| --- | --- |
| Decode property tests | `runtime::action_decode::tests`: `select_direction_without_a_bank_is_the_scalar_decode`, `select_direction_all_tie_yields_the_scalar_direction`, `select_direction_unique_maximum_wins`, `select_direction_tie_prefers_the_scalar_then_the_lowest_index`, `select_direction_sanitizes_non_finite_bids`, `select_direction_single_bid_one_edge_property` (proptest); `eat_and_noop_ignore_the_bank`, `movement_actions_consult_the_bank` |
| One-edge fixtures, graph backend | `neighborhood::steering::tests`: `graph_one_positive_food_edge_hits_every_direction_exactly`, `graph_one_positive_food_edge_changes_nothing_else`, `graph_one_negative_barrier_edge_avoids_and_changes_nothing_else` |
| One-edge fixtures, VM backend (founder) | `neighborhood::steering::tests`: `vm_one_positive_food_edge_hits_every_direction_and_changes_nothing_else`, `vm_one_negative_barrier_edge_avoids_where_the_founder_led_and_changes_nothing_else` |
| VM opcode semantics | `runtime::tests::vm_io_memory`: `write_direction_bid_selects_the_winning_slot_and_persists_within_a_dispatch`, `write_direction_bid_resets_between_dispatches`, `write_direction_bid_out_of_range_leaves_the_bank_unwritten`; `tests/vm_all_opcodes_e2e.rs` (42-opcode catalog); `runtime::tests::traced_vm_tests` (trace row for the opcode) |
| Graph effects | `runtime::cgp::effects::tests::decode_action_from_kind_move_reads_the_bank_when_written` |
| Founder neutrality and determinism | `neighborhood::steering::tests::founder_reading_is_deterministic_and_writes_no_bank`; `v3-cli` `bench::run::tests::mutational_neighborhood_is_defined_only_for_the_gate_and_goal_profile_names` (founder `steering`: version, seed 9, 6 bases, 48 scenarios, `bank_written` false; pooled sums equal the per-genome sums) |
| Mutation coverage, graph | `mutation::graph::operators::tests`: `add_edge_lands_on_the_bank_surface_with_a_direction_in_range`, `edge_operators_act_on_a_bank_edge` (remove, retarget, weight, split), `raw_field_mutation_moves_a_bank_direction_by_one_unit`, `direction_moves_are_bounded_and_absent_out_of_range` |
| Mutation coverage, VM | `mutation::vm::tests`: `random_vm_instruction_covers_all_families` (42-way draw), `raw_field_mutation_visits_each_operand_field_without_changing_its_opcode` (both operands nudged), the `register_bearing_instructions` remap sweep |
| Serde | `creature::genome::cgp::tests`: `action_slot_with_a_bank_serde_roundtrip` (proptest), `action_slot_without_direction_bids_deserializes_with_an_empty_bank` (stored bytes unchanged) |
| `steering-v1` report shape | `v3-cli` `bench::schema::tests` (old reports read `Undefined` for `steering` and `steering_pooled`), `bench::artifacts::tests` (`steering_pooled` projected per seed) |
| Re-pins from the shifted uniform draws | `neighborhood::recruitment_paths::qualification` pinned seeds; `tests/applied_trajectory.rs` digest `0f21b62e…`; `tests/baseline_worlds.rs` `legacy_default_short_run_identity` `11216971115431479696`; `v3-cli` `bench::tests` occupancy-grid seed 13 -> 18 |

## Self-review verification (working tree, uncommitted, 2026-09-16)

| Command | Result |
| --- | --- |
| `cargo test -p v3-core --test viability` | 26 passed, 0 failed |
| `cargo test -p v3-core --test applied_trajectory --test baseline_worlds` | 1 passed; 19 passed, 1 ignored (pins unchanged after the simplify pass) |
| `cargo test --workspace --no-fail-fast` | 1950 passed, 0 failed, 4 ignored (pre-existing explicit-run characterizations), exit 0 |
| `cargo clippy --workspace --all-targets -- -D warnings` | clean |
| `cargo fmt --all -- --check` | clean |
| `make roadmap-check` | validation passed |

No `proptest-regressions/` file exists after the runs.

The neutrality jq transcript (founder `mesh_execution` byte-identical to the
previous closure's gate summary) and the `make check` commit hash are filled
when the gate summary and the final feature commit exist.
