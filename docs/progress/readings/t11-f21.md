# T11.F21 — Per-Direction Motor Output readings

Test names, transcripts, and tables for the feature spec
[`docs/specs/roadmap/t11-f21-per-direction-motor-output.md`](../../specs/roadmap/t11-f21-per-direction-motor-output.md).
Implementation and self-review run from the worktree
`.claude/worktrees/t11-f21` (branch `worktree-t11-f21`, on top of main
`7c738685`). Benchmark and mutation sections are filled by their
specialists.

## Test names behind the Verification items

| Verification item | Tests (module or file) |
| --- | --- |
| Decode property tests | `runtime::action_decode::tests`: `select_direction_without_a_bank_is_the_scalar_decode`, `select_direction_all_tie_yields_the_scalar_direction`, `select_direction_unique_maximum_wins`, `select_direction_tie_prefers_the_scalar_then_the_lowest_index`, `select_direction_sanitizes_non_finite_bids`, `select_direction_single_bid_one_edge_property` (proptest); `eat_and_noop_ignore_the_bank`, `movement_actions_consult_the_bank` |
| One-edge fixtures, graph backend | `neighborhood::steering::tests`: `graph_one_positive_food_edge_hits_every_direction_exactly`, `graph_one_positive_food_edge_changes_nothing_else`, `graph_one_negative_barrier_edge_avoids_and_changes_nothing_else` |
| One-edge fixtures, VM backend (founder) | `neighborhood::steering::tests`: `vm_one_positive_food_edge_hits_every_direction_and_changes_nothing_else`, `vm_one_negative_barrier_edge_avoids_where_the_founder_led_and_changes_nothing_else` |
| VM opcode semantics | `runtime::tests::vm_io_memory`: `write_direction_bid_selects_the_winning_slot_and_persists_within_a_dispatch`, `write_direction_bid_resets_between_dispatches`, `write_direction_bid_out_of_range_leaves_the_bank_unwritten`; `tests/vm_all_opcodes_e2e.rs` (42-opcode catalog); `runtime::tests::traced_vm_tests` (trace row for the opcode) |
| Graph effects | `runtime::cgp::effects::tests::decode_action_from_kind_move_reads_the_bank_when_written` |
| Founder neutrality and determinism | `neighborhood::steering::tests::founder_reading_is_deterministic_and_writes_no_bank`; `v3-cli` `bench::run::tests::mutational_neighborhood_is_defined_only_for_the_gate_and_goal_profile_names` (founder `steering`: version, seed 9, 6 bases, 48 scenarios, `bank_written` false; pooled sums equal the per-genome sums) |
| Mutation coverage, graph | `mutation::graph::operators::tests`: `add_edge_lands_on_the_bank_surface_with_a_direction_in_range`, `edge_operators_act_on_a_bank_edge` (remove, retarget, weight, split), `raw_field_mutation_moves_a_bank_direction_by_one_unit`, `direction_moves_are_bounded_and_absent_out_of_range` |
| Mutation coverage, VM | `mutation::vm::tests`: `random_vm_instruction_covers_all_families` (42-way draw), `raw_field_mutation_visits_each_operand_field_without_changing_its_opcode` (both operands nudged), the `register_bearing_instructions` remap sweep |
| Serde | `creature::genome::cgp::tests`: `action_slot_with_a_bank_serde_roundtrip` (proptest), `action_slot_without_direction_bids_deserializes_with_an_empty_bank` (stored bytes unchanged) |
| `steering-v1` report shape | `v3-cli` `bench::schema::tests` (old reports read `Undefined` for `steering` and `steering_pooled`), `bench::artifacts::tests` (`steering_pooled` projected per seed) |
| Re-pins from the shifted uniform draws | `neighborhood::recruitment_paths::qualification` pinned seeds; `tests/applied_trajectory.rs` digest `0f21b62e…`; `tests/baseline_worlds.rs` `legacy_default_short_run_identity` `11216971115431479696`; `v3-cli` `bench::tests` occupancy-grid seed 13 -> 18 |

## Self-review verification (working tree, uncommitted, 2026-09-16)

| Command | Result |
| --- | --- |
| `cargo test -p v3-core --test viability` | 26 passed, 0 failed |
| `cargo test -p v3-core --test applied_trajectory --test baseline_worlds` | 1 passed; 19 passed, 1 ignored (pins unchanged after the simplify pass) |
| `cargo test --workspace --no-fail-fast` | 1950 passed, 0 failed, 4 ignored (pre-existing explicit-run characterizations), exit 0 |
| `cargo clippy --workspace --all-targets -- -D warnings` | clean |
| `cargo fmt --all -- --check` | clean |
| `make roadmap-check` | validation passed |

No `proptest-regressions/` file exists after the runs.

## Benchmark gate and goal profiles

Both commands ran with `PATH` prefixed by `~/.local/share/petri-tools/bin` and
`~/.local/share/aquaproj-aqua/bin`, from the worktree, gate then goal,
sequentially. The user's own long-running `v3-server` (started via
`scripts/dev.sh`) blocked `scripts/bench-wait` on the first attempt; after the
user stopped it, both runs proceeded with nothing else on the host.

| Command | CLI exit | Observed outer exit | `comparison.severe` | Threshold crossed | Epoch re-pin |
| --- | --- | --- | --- | --- | --- |
| `make bench PROFILE=gate FEATURE=t11-f21-per-direction-motor-output` | 0 (`measurement_evidence.cli_exit.code`) | 0 (own shell, `echo "EXIT:$?"` after the redirected run) | false, against both `remove-complementary-nutrition.json` and `t14-f07-surviving-clade-behavioral-profile.json` | none (all six normalized counters `level: ok`) | no |
| `make bench PROFILE=goal FEATURE=t11-f21-per-direction-motor-output` | 0 | 0 (own shell, `echo "EXIT:$?"` after the redirected run) | false, against both `t11-f19-per-unit-mutation-supply-goal.json` and `t14-f07-surviving-clade-behavioral-profile-goal.json` | none (all six normalized counters `level: ok`) | no |

Raw and summary artifacts:

| Artifact | Path | Bytes | SHA-256 | Verified at |
| --- | --- | --- | --- | --- |
| Gate raw | `/Users/istefanek/projects/petri/.bench-artifacts/t11-f21-per-direction-motor-output/gate.json` (main checkout, ignored) | 94,750 | `5736b81777066cc187c9e70f088e27711c821433446eaa170db074cb099011bf` | 2026-09-16T22:13:25Z |
| Gate summary | `docs/progress/features/t11-f21-per-direction-motor-output.json` | 97,573 | (hashes `raw` above) | — |
| Goal raw | `/Users/istefanek/projects/petri/.bench-artifacts/t11-f21-per-direction-motor-output/goal.json` (main checkout, ignored) | 512,247,963 | `3cf25dde90d3f0a14ac01db99a70552638755487f15e618f8ee3f0ac11cbd936` | 2026-09-16T22:22:10Z |
| Goal summary | `docs/progress/features/t11-f21-per-direction-motor-output-goal.json` | 6,813,746 | (hashes `raw` above) | — |

No full report is staged; only the two summaries and this readings update.
Feature commit `c0e884fd` (`git_revision` in both summaries). Wall-clock caps
(`measurement_evidence.wall_caps`): founder 10 s/world (observed gate 0.055 s),
evolved 180 s/world summed (observed goal 0.535 s total, per-seed
0.112/0.228/0.196 s), goal-investigation 900 s (observed goal wall total
484.2 s) — all within cap.

### Second goal run

Not applicable: the goal profile runs once per closure under the one-goal-run
rule (`docs/workflow.md`, "Benchmark gate"); cross-process reproducibility is
covered separately by `crates/v3-core/tests/reproducibility.rs` inside `make
check`.

### Neutrality: founder `mesh_execution` and `steering`

Previous closure for the gate series is `t14-f07-surviving-clade-behavioral-profile.json`
(`docs/progress/benchmark-series.json`, `gate.closed` last entry).

```
$ jq -Sc '.deterministic.goal_indicators.mutational_neighborhood.founder.mesh_execution' docs/progress/features/t11-f21-per-direction-motor-output.json
{"backends":{"graph":{"contributing":1,"executed":1,"total":1},"vm":{"contributing":1,"executed":1,"total":1}},"executed_node_count":2,"executions_per_genome":80,"hop_cap_hits":0,"knockout_count":0,"knockout_method":"static-successor-bypass-v1","reachable_node_count":2,"route_varies_with_input":false,"snapshot_route_probes":48,"total_node_count":2,"version":"mesh-execution-v1"}

$ jq -Sc '.deterministic.goal_indicators.mutational_neighborhood.founder.mesh_execution' docs/progress/features/t14-f07-surviving-clade-behavioral-profile.json
{"backends":{"graph":{"contributing":1,"executed":1,"total":1},"vm":{"contributing":1,"executed":1,"total":1}},"executed_node_count":2,"executions_per_genome":80,"hop_cap_hits":0,"knockout_count":0,"knockout_method":"static-successor-bypass-v1","reachable_node_count":2,"route_varies_with_input":false,"snapshot_route_probes":48,"total_node_count":2,"version":"mesh-execution-v1"}

$ diff <(jq -Sc '.deterministic.goal_indicators.mutational_neighborhood.founder.mesh_execution' docs/progress/features/t11-f21-per-direction-motor-output.json) <(jq -Sc '.deterministic.goal_indicators.mutational_neighborhood.founder.mesh_execution' docs/progress/features/t14-f07-surviving-clade-behavioral-profile.json) && echo IDENTICAL
IDENTICAL
```

Founder `steering` (deterministic, `bank_written` false) is the recorded
reference for this closure:

```
$ jq '.deterministic.goal_indicators.mutational_neighborhood.founder.steering' docs/progress/features/t11-f21-per-direction-motor-output.json
{
  "avoidance_trials": 48,
  "avoided": 0,
  "bank_written": false,
  "base_count": 6,
  "exact_hits": 24,
  "moves": 48,
  "scenarios": 48,
  "seed": 9,
  "version": "steering-v1",
  "within_45": 36
}
```

`base_count` 6, `scenarios` 48, `bank_written` false, matching the spec's
predeclared founder reading (Verification item).

### Steering, evolved half (goal profile, per world)

`exact_hit_fraction`/`avoidance_fraction`/`bank_written_fraction` from
`deterministic.goal_indicators.cases[].mutational_neighborhood.evolved.per_seed[0].steering_pooled`;
chance level `exact`=0.125, `within_45`=0.375 (recorded in each `steering_pooled.chance`).

| World | seed | genomes sampled | generation median/max | exact_hit_fraction | avoidance_fraction (avoided/trials) | bank_written_fraction | final_population |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Orchards in grassland | 11 | 8 | 1 / 2 | 0.484375 | 0.000000 (0/384) | 0.000000 | 8 |
| Canyon country | 22 | 12 | 51 / 67 | 0.285012 | 0.000000 (0/407) | 0.000000 | 4,121 |
| Confluence | 33 | 12 | 60 / 69 | 0.238208 | 0.000000 (0/424) | 0.000000 | 8,105 |

All three worlds: `bank_written_fraction` is 0 — no sampled evolved genome has
written the direction-bid bank at this depth (22–50 generations), matching the
spec's predeclared "not yet exposed at this depth" case; `avoidance_fraction`
is correspondingly 0. `exact_hit_fraction` is above the 0.125 chance level in
all three worlds, but this reflects the pre-existing scalar-decode steering
(the founder's own `exact_hits`/`moves` is 20/40 or 24/48, i.e. ~50%, with no
bank), not a bank contribution — flagged for the spec owner's read, not
resolved here. No extinction in any goal world (`extinction_tick` null in all
three `population_persistence.per_seed` entries; minimum populations 8, 171,
8; final populations 8, 4121, 8105).

### Evolved per-birth `changed`/`dead` against the previous goal closure

`deterministic.goal_indicators.cases[].mutational_neighborhood.evolved.per_seed[0].pooled_births.any_events`,
compared against `t14-f07-surviving-clade-behavioral-profile-goal.json` (last
closed entry in `benchmark-series.json`'s `goal_worlds.closed`):

| World | changed_fraction (cur) | changed_fraction (prev) | direction | dead_fraction (cur) | dead_fraction (prev) | direction |
| --- | --- | --- | --- | --- | --- | --- |
| Orchards in grassland | 0.386332 | 0.354354 | up (ok) | 0.000000 | 0.004505 | down (ok) |
| Canyon country | 0.283382 | 0.328422 | **down (violates "not down")** | 0.005831 | 0.001842 | **up (violates "not up")** |
| Confluence | 0.360515 | 0.389446 | **down (violates "not down")** | 0.016452 | 0.002672 | **up (violates "not up")** |

Canyon country and Confluence both move against the spec's predeclared
direction ("evolved per-birth `changed` not down and `dead` not up against the
previous goal summary", Performance and Goal Impact). Reported as an
unexpected comparison, not interpreted into a requirement or remediated here.

### `neighborhood_read.changed_per_all_births` against the T14.F12 floors

| World | current | previous closure | T14.F12 floor | Above floor |
| --- | --- | --- | --- | --- |
| Orchards in grassland | 0.183750 | 0.207200 | 0.146400 | yes |
| Canyon country | 0.180400 | 0.196400 | 0.130000 | yes |
| Confluence | 0.190000 | 0.201400 | 0.143400 | yes |

All three worlds stay above their T14.F12 floor.

The neutrality jq transcript above is complete; the founder half
(deterministic `steering`, `bank_written` false) matches its unit-tested
reference. The `make check` commit hash on the final feature commit is filled
by the implementer/reviewer, not the benchmark specialist.

## Control run (2026-09-16, user-authorized; spec-owner ruling)

One scratch goal profile at feature commit `72845446` with an uncommitted
two-line patch restoring the pre-feature draw ranges (VM fresh-instruction
draw `0..41` instead of `0..42`; `pick_random_surface` without the `ActionBid`
entries). Founders carry no bank and no other production constructor writes
one, so the patched build is trajectory-inert apart from the draw mapping.
Raw and summary stayed in the scratchpad (not stored; the patch is
`control-draws.patch` there); the closure artifacts are untouched.

| World | control min / final | T02.F04 closure min / final | T11.F21 feature run min / final |
| --- | --- | --- | --- |
| Orchards in grassland | 80 / 10,949 | 80 / 10,949 | 8 / 8 |
| Canyon country | 490 / 5,235 | 490 / 5,235 | 171 / 4,121 |
| Confluence | 106 / 9,169 | 106 / 9,169 | 8 / 8,105 |

Exit 0, `severe=false`. With `steering`/`steering_pooled` removed, the
control's `.deterministic` matches T02.F04's byte-for-byte in
`graph_work_definition`, `per_creature_tick`, `per_seed`, `profile`, `totals`,
and every `goal_indicators` key except `cases[].surviving_clade_profiles`
(T14.F07's block, added after T02.F04's run); T14.F07 itself was benchmarked
on an earlier base. Ruling: the feature's only trajectory effect is the draw
re-mapping; the feature run's Orchards collapse is that diverged trajectory,
not the bank (`bank_written_fraction` 0). Orchards' sensitivity to a
perturbed trajectory (minimum 80 here, 8 in the feature run with
generation-1–2 survivors) is a world finding for T12/T02, not this feature's.
