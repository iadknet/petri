# T19.F03 — Readings

Evidence for [the feature spec](../../specs/roadmap/t19-f03-vote-surface-as-inert-data.md).
Base commit for every "before" record: `07086ed0` (the worktree branched from
`4137b977`, which is `07086ed0` plus the plan commit and carries identical
production code).

## Guard digest

| Record | Value |
| --- | --- |
| Before, on the untouched tree | `99ef9a142a40178c4952fe73a29978e69fa0030d9475e92ca22f106d19608273` |
| After, with the whole feature in place | `99ef9a142a40178c4952fe73a29978e69fa0030d9475e92ca22f106d19608273` |

`mutation_on_applied_trajectory_guard_is_pinned`
(`crates/v3-core/tests/applied_trajectory.rs`) digests, over 64 ticks of two
mutation-on seeds, the tick number, the reproduction and mutation-event
totals, and per creature the id, position, energy bits, and age. It carries no
genome bytes, so the catalog growth cannot move it: an unchanged value is the
evidence that no draw and no read leaked into applied behavior.

## Draw exclusions

`pick_random_surface` on the founder graph, seeds 1..=8, recorded before any
production change and asserted after it by
`pick_random_surface_on_the_founder_graph_matches_the_pre_feature_record`:

`SinkInput(54)`, `SinkInput(57)`, `SinkInput(63)`, `SinkInput(48)`,
`SinkInput(55)`, `ActionParam(3)`, `ActionParam(3)`, `SinkInput(33)`.

(`EdgeSurface::ActionParam(i)` is an action slot's parameter surface, which
predates this feature and is unrelated to the `OutputSinkKind::ActionParam`
sinks.)

## Test runs

| Command | Result |
| --- | --- |
| `cargo test -p v3-core --test viability` | 28 passed |
| `cargo test -p v3-core --test applied_trajectory` | 2 passed (guard value reproduced on a second run) |
| `cargo test -p v3-core --test baseline_worlds` | 19 passed, 1 ignored |
| `cargo test -p v3-core --test vm_all_opcodes_e2e` | 1 passed (43 discriminants) |
| `cargo test --workspace` | 24 test targets, all ok, 0 failed |
| `npm run test` (frontend) | 62 files, 329 tests passed |
| `make check` | exit 0 |

Feature tests, by name:

| Test | Where |
| --- | --- |
| `catalog_indices_are_the_pinned_order`, `from_index_round_trips_the_whole_catalog_and_stops_at_the_count`, `kind_is_absent_for_exactly_the_two_pass_control_sinks`, `vote_kind_indices_match_the_counter_order`, `serde_round_trips_every_sink_and_kind` | `crates/v3-core/src/creature/genome/vote.rs` |
| `new_with_fixed_outputs_creates_correct_catalog` (extended to the 99-sink order) | `crates/v3-core/src/creature/genome/cgp.rs` |
| `pick_random_surface_never_returns_a_vote_or_parameter_sink` (4,096 seeds), `pick_random_surface_on_the_founder_graph_matches_the_pre_feature_record` | `crates/v3-core/src/mutation/graph/operators.rs` |
| `random_vm_instruction_never_yields_add_vote_and_still_reaches_all_42_drawable` (4,096 seeds) | `crates/v3-core/src/mutation/vm/tests.rs` |
| 11 runtime tests: VM summation and commit, per-node-latest replacement on a revisit, signed two-node summation, entry and sum sanitization, a saturating VM vote, invalid-sink costed no-op, exhausted VM dispatch, exhausted graph visit, wired graph sinks, parameter overwrite, three-mode parity | `crates/v3-core/src/runtime/vote_surface_tests.rs` |
| `VoteSurfaceBlock` renders non-zero entries and the four counters (2 cases) | `frontend/src/components/inspector/VoteSurfaceBlock.test.tsx` |

## Moved pins

Both hash a genome's serde or Debug form, so both move by construction with 35
more catalog entries. Each was reproduced twice before being re-pinned.

| Pin | Before | After |
| --- | --- | --- |
| `accounting_preserves_pre_feature_sampled_trajectories_and_actions` (`tests/applied_trajectory.rs`) | `c82547de36d3cfed37a1f1db8bc61ecc4b7184a31c8e42de163681bd6daae235` | `09b9e53c23451057957fdebd135ea8dc12a49b71e8493e4a155424b527e5ff4b` |
| `legacy_default_short_run_identity` (`tests/baseline_worlds.rs`) | `14387572686062595774` | `5811834416729882562` |

No other pin moved. `founder_only_trajectory_digest_is_pinned`
(`63498f8d36…`), `FOUNDER_GENOME_SIZE_UNITS` 111, and every `config_digest`
are unchanged.

## Nothing reads the surface

`grep -rn '\.votes\|commit_counts\|action_params\|vote_contribution\|ActionVote\|ActionParam\|AddVote\|is_vote_surface\|VOTE_KINDS\|voteSink' crates frontend/src`
over non-test sources returns only:

| Kind of hit | Files |
| --- | --- |
| Catalog and type definitions | `creature/genome/vote.rs`, `creature/genome/cgp.rs`, `creature/genome/mod.rs`, `frontend/src/types/genome.ts`, `frontend/src/types/trace.ts` |
| Writers | `runtime/vm.rs` (`AddVote` arm, its cost, the commit macro), `runtime/cgp/effects.rs` (the two sink arms and the stage call), `runtime/types.rs` (staging and summation), `runtime/mesh.rs` (per-hop commit) |
| Trace and transport carriers | `runtime/trace/domain.rs`, `runtime/traced_mesh.rs`, `simulation/tick.rs`, `v3-server/transport/sample_protocol.rs`, `sample_assembler.rs` |
| Draw exclusions and classification | `mutation/graph/operators.rs` (via `OutputSinkKind::is_vote_surface`), `mutation/vm/operators.rs` (unreachable nudge and register arms), `creature/genome/analysis.rs`, `creature/genome/cgp_mesh_annotations.rs` |
| Formatters and the read-only block | `inspector/graphNodeFormatters.ts`, `inspector/vmInstructionFormat.ts`, `inspector/mesh/meshSemantics.ts`, `inspector/mesh/runtimeIoSemantics.ts`, `inspector/mesh/meshPresentation.ts`, `inspector/VoteSurfaceBlock.tsx` |

No executor branches on a vote, no input reads one, and no reading or
statistic derives from one.

The graph-internals view builds a node per *wired* sink
(`graphFullModel.ts` phase 3), and the mesh badge pass skips unwired sinks, so
the 35 new catalog entries draw nothing: the only UI addition is the read-only
vote block under the hop timeline.

## Benchmark gate and goal (benchmark specialist)

Both profiles ran from worktree `.claude/worktrees/t19-f03` at HEAD
`7ab070a0` (`worktree-t19-f03`). No host blockers (`scripts/bench-wait`
preflight clean). References for both profiles are the T19.F02 summaries, per
the spec; neither epoch was re-pinned.

Gate — `make bench PROFILE=gate FEATURE=t19-f03-vote-surface-as-inert-data
OUT=.bench-artifacts/t19-f03/gate.json
SUMMARY_OUT=docs/progress/features/t19-f03-vote-surface-as-inert-data.json`:

| Item | Value |
| --- | --- |
| CLI exit | 0 (`v3-cli` process); outer `make` exit 0 |
| `severe` | `false` |
| Work counters vs T19.F02 gate (`mesh_hops`, `vm_steps`, `graph_relax_iters`, `plasticity_updates`, `actions_applied`, `births`, `pass_cap_hits`) | All `delta%=0.000000`, `level=ok` |
| `wall_clock_ms_per_creature_tick` | current `0.0015475`, reference `0.0013469`, delta `+14.89%`, level `ok` (below the 25% flag) |
| `effective_config_digest` | `sha256:a0fedf15…` — identical to T19.F02 gate |
| Founder-neighborhood wall clock | `64.39 ms` (cap 10,000 ms) |
| Raw report | `.bench-artifacts/t19-f03/gate.json`, sha256 `324a1ea6…`, 94,762 bytes, `verified_local` |
| Summary | `docs/progress/features/t19-f03-vote-surface-as-inert-data.json`, 97,834 bytes |

Goal — `make bench PROFILE=goal FEATURE=t19-f03-vote-surface-as-inert-data
OUT=.bench-artifacts/t19-f03/goal.json
SUMMARY_OUT=docs/progress/features/t19-f03-vote-surface-as-inert-data-goal.json`
(single run per the workflow's one-goal-run policy):

| Item | Value |
| --- | --- |
| CLI exit | 0 (`v3-cli` process); outer `make` exit 0 |
| `severe` | `false` |
| Work counters vs T19.F02 goal (same seven counters) | All `delta%=0.000000`, `level=ok` |
| `goal_indicators` (persistence, diversity, recruitment, memory sensitivity, energy flows, mutational neighborhood, drift depth, cognition, mortality, all three cases) | Structural diff against T19.F02's goal summary: **0 differences** across the entire `deterministic.goal_indicators` subtree |
| `config_digest` (per case and `recruitment_paths`) | Identical to T19.F02 goal, all three cases |
| `wall_clock_ms_per_creature_tick` | current `0.010992`, reference `0.010648`, delta `+3.23%`, level `ok` (well below the 25% flag; the predeclared 35-row effects-pass cost is small relative to per-tick work here) |
| `environment.neighborhood_evolved_wall_clock_ms_total` | `622.91 ms` (cap 180,000 ms) |
| `environment.neighborhood_founder_wall_clock_ms` | `108.36 ms` (cap 10,000 ms) |
| `environment.wall_clock_ms_total` (whole goal run) | `428,728.6 ms` ≈ 7.15 min (15-minute investigation threshold not triggered — not needed since nothing severe) |
| Raw report | `.bench-artifacts/t19-f03/goal.json`, sha256 `08e9a27d…`, 662,416,918 bytes, `verified_local` |
| Summary | `docs/progress/features/t19-f03-vote-surface-as-inert-data-goal.json`, 7,668,503 bytes (T19.F02's goal summary is 7,698,308 bytes — consistent scale) |

No epoch was re-pinned; both `references` blocks point at the T19.F02 gate and
goal summaries as required. No counter, persistence/diversity/recruitment/
memory-sensitivity/energy-flow reading, or `config_digest` differed from the
T19.F02 reference. Both wall-time deltas landed under the 25% flag threshold
(gate +14.89%, goal +3.23%), so even the tolerated flag was not reached.
