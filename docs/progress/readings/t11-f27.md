# T11.F27 — Compact Action-Parameter Storage readings

Tables and transcripts for the feature spec
[`docs/specs/roadmap/t11-f27-compact-action-parameter-storage.md`](../../specs/roadmap/t11-f27-compact-action-parameter-storage.md).

## Spec challenge (Codex Astra `xhigh`, read-only)

| Round | Job | Finding | Severity | Disposition |
| --- | --- | --- | --- | --- |
| 1 | `task-mufy40md-n6lysp` | Equivalence to T11.F25 is promised, but current-format replay cannot establish cross-version equivalence; add focused checks for mapped Graph targets, opcode-25 field meaning and RNG state, keep the empty-graph no-draw invariant, or narrow the claim | blocking | Fixed: the Determinism row pins RNG use of both draws against a cloned RNG (one `gen_range(0..c + 94)` → `ComputeInput(k)`/`SinkInput(k - c)`; empty def → `None` with no RNG; opcode-25 field = `gen_range(0..3)` with equal RNG state after), and the founder-run equivalence is restated as an expectation read through the attributed re-pins, not a proof |
| 1 | `task-mufy40md-n6lysp` | No evidence that Graph writes reach the correct committed action; Graph write and mesh decode have independent mappings | blocking | Fixed: an Implementation Task requires end-to-end coverage of all three fields through both backends to the committed `WorldAction`, cross-backend last-write-wins, persistence across passes, and reset at tick start; Verification names the focused tests |
| 1 | `task-mufy40md-n6lysp` | Decoder agreement uses one existential pair for isolation | advisory | Adopted: sensitivity stays existential; off-kind independence is a property over arbitrary finite values, replacing T11.F25's `undecoded_action_params_never_change_the_commit` |
| 1 | `task-mufy40md-n6lysp` | Boundary wording alternates between all, all-graph, and affected genomes | advisory | Adopted: Goal and Boundary row reject the affected old shapes; every fixed-catalog graph is affected; an unaffected pre-F27 VM genome deserializes (test added) |
| 1 | `task-mufy40md-n6lysp` | Early divergence in every world is inferred, not guaranteed | advisory | Adopted: predeclaration states an expectation conditional on the affected draws and says the readings report where divergence was observed |
| 1 | `task-mufy40md-n6lysp` | Frontend formatter defaults missing operands to zero, so a missed rename labels every write `Eat` | advisory | Adopted: Frontend row requires serialized-shape fixtures for the three sinks and VM `field_idx` 0–3 with distinct labels |
| 2 | `task-mufy9xuh-nxgo6c` | Round-1 fixes confirmed (no rejected disposition, no new blocking finding) | — | Closed |
| 2 | `task-mufy9xuh-nxgo6c` | Opcode-29 test checks only the `0..2` bound; a constant-zero draw would pass while excluding slot 1 | advisory | Adopted: the draw test requires opcode-29 draws to cover exactly `0..2`, both slots drawn |

Round 2 verdict: `ready`. No blocking finding remains open.

## Red and green runs

| Run | Command | Result |
| --- | --- | --- |
| Red (tests written before the implementation) | `cargo test -p v3-core --lib` | exit 101: 24 compile errors, all against the missing API (`ActionParamField`, `ACTION_PARAM_FIELD_COUNT`, `WriteActionParam.field_idx`, one-field `OutputSinkKind::ActionParam`, three-cell surface) |
| Viability (first) | `cargo test -p v3-core --test viability` | 28 passed |
| Green, core lib | `cargo test -p v3-core --lib` | 1,715 passed, 0 failed, 4 ignored |
| Green, workspace | `cargo check --workspace --all-targets`; `cargo test --workspace --no-fail-fast` | check exit 0; tests exit 0, 2,152 passed, 0 failed, 6 ignored |
| Clippy and format | `cargo clippy --workspace --all-targets -- -D warnings`; `cargo fmt --all --check` | clean |
| Frontend | `npx tsc -b`; `npx biome check src/`; `npx vitest run` | tsc exit 0; biome 4 pre-existing warnings in `config-panel/startup/`, none in changed files; 67 files, 337 tests passed |

## Focused tests

| Requirement | Test |
| --- | --- |
| Catalog order, `index()`, `kind()`, serde names | `creature::genome::vote::tests::action_param_fields_are_the_decoded_catalog_in_kind_major_order` |
| Fixed catalog: 94 sinks, parameter sinks at 91..94 in `ALL` order | `creature::genome::cgp::tests::new_with_fixed_outputs_creates_correct_catalog` |
| Decoder agreement, sensitivity | `runtime::action_decode::tests::every_action_param_field_changes_its_kinds_commit` |
| Decoder agreement, independence (proptest) | `runtime::action_decode::tests::a_field_never_changes_another_kinds_commit` |
| VM addressing: field `i` writes cell `i` | `runtime::vm::tests::io_memory::write_action_param_field_i_writes_surface_cell_i` |
| VM: `field_idx` 3, 8, 255 ignored and charged as a valid write | `runtime::vm::tests::io_memory::write_action_param_ignores_and_charges_an_invalid_field`, `write_action_param_overwrites_and_ignores_an_invalid_field` |
| Graph draw RNG use: one `gen_range(0..c + 94)` on the founder def, `ComputeInput(k)`/`SinkInput(k - c)`, empty def `None` with no RNG | `mutation::graph::operators::tests::pick_random_surface_draws_uniformly_over_compute_nodes_and_every_sink` |
| Graph draw reference and `can_add_edge` (proptest) | `mutation::graph::operators::tests::pick_random_surface_matches_the_reference` |
| Fresh `AddGraphEdge` reaches all three parameter sinks | `mutation::graph::operators::tests::add_edge_reaches_every_parameter_sink` |
| Opcode-25 and opcode-29 RNG use against a cloned RNG (proptest) | `mutation::vm::tests::opcode_25_and_29_draws_match_their_pinned_rng_use` |
| Opcode-25 draws cover exactly the three fields | `mutation::vm::tests::random_vm_instruction_write_action_param_reaches_every_field` |
| Opcode-29 draws cover exactly `0..2`, both slots drawn | `mutation::vm::tests::random_vm_instruction_read_action_queue_param_draws_both_slots` |
| Boundary: current-format genome, all three fields wired by both backends, round-trips equal | `creature::genome::tests::current_format_param_genome_round_trips` |
| Boundary: old `ActionParam` sink JSON and `WriteActionParam { slot_idx, .. }` JSON rejected | `creature::genome::tests::pre_f27_param_shapes_are_rejected` |
| Boundary: unaffected pre-F27 VM genome deserializes | `creature::genome::tests::unaffected_pre_f27_vm_genome_deserializes` |
| End to end: each field from a Graph sink and from a VM write reaches its committed `WorldAction` | `runtime::pass_loop_tests::every_param_field_reaches_its_commit_from_either_backend`, `a_commit_decodes_the_parameter_surface_of_its_kind` |
| End to end: last write wins across Graph and VM nodes | `runtime::pass_loop_tests::the_last_param_write_wins_across_graph_and_vm_nodes` |
| End to end: surface persists across passes within a tick | `runtime::pass_loop_tests::the_param_surface_persists_across_passes` |
| End to end: a field no node wrote in the mesh run decodes as zero, from either backend | `runtime::pass_loop_tests::a_field_unwritten_in_the_mesh_run_decodes_as_zero` |
| Frontend: three Graph sink labels and VM `field_idx` 0–3 labels from serialized-shape fixtures, `field_idx` 3 rendered exactly `param[3] ← r1` | `frontend/src/components/inspector/actionParamLabels.test.ts` |

## Current-format replay

`recruitment::tests::a_complete_run_writes_the_record_summary_and_replay_check`
(`v3-cli`) passes, and `cargo test -p v3-core --test reproducibility` passes
(3 tests). The drift, recruitment-paths, mutational-neighborhood and
temporal-fixture pins pass unchanged.

## Re-pins

| Test | Old | New | Attribution |
| --- | --- | --- | --- |
| `applied_trajectory::accounting_preserves_pre_feature_sampled_trajectories_and_actions` | `93ba762bb64d24032df8821f5f0b3f50b5b9c3a29f340aabf7a29186f03fda65` (main after the food-fixture fix `a3f26092`) | `6ee41ec18b3f8cae35d8158e77eba4508ac1001e9208ef6891e5bae845e7a6d8` | Serialized genome bytes only (sink catalog 99 → 94, `ActionParam` shape); with genome bytes replaced by `()`, main `2a691e2e` and this tree both give `e8a9864c845c846b174b57ed4a37740b7b30698e41368a2339bf2f6aea12f229` |
| `baseline_worlds::legacy_default_short_run_identity` | `7552184245034446484` | `9485529022557498135` | Genome `Debug` text only (same cause) |

Both new values reproduced in two agreeing runs. Attribution probe: a scratch
copy of each test with genome bytes replaced by `genome_size()`, run on the
pre-feature tree (`git archive 5036b729`) and on this tree, gives identical
values on both (`2bc5e7a74a777a0209d6b2964c227918f2a47cdeefe02bb2a3e6c703277b9af3`
and `9446971795048128177`). Neither fixture's trajectory moved. The
positions-energy-age guard `mutation_on_applied_trajectory_guard_is_pinned`
holds without a re-pin. No founder-behavior pin changed.
