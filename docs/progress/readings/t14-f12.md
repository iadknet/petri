# T14.F12 — Neighborhood Read of Selected Genomes readings

Benchmark-specialist tables and transcripts for the feature spec
[`docs/specs/roadmap/t14-f12-neighborhood-read-of-selected-genomes.md`](../../specs/roadmap/t14-f12-neighborhood-read-of-selected-genomes.md).
Tested commit `71503f24`. Both profiles run from the worktree
`.claude/worktrees/t14-f12`; raw artifacts land in the main checkout's
ignored `.bench-artifacts/t14-f12-neighborhood-read-of-selected-genomes/`.

## Commands and exit status

| Profile | Command | Outer exit | CLI `cli_exit.code` | `severe` |
| --- | --- | --- | --- | --- |
| gate | `make bench PROFILE=gate FEATURE=t14-f12-neighborhood-read-of-selected-genomes` | 0 (no error output, artifact pair written) | 0 | false |
| goal | `make bench PROFILE=goal FEATURE=t14-f12-neighborhood-read-of-selected-genomes` | 0 (`echo $?` after the backgrounded wrapper) | 0 | false |

Both `measurement_evidence.cli_exit.code` fields (in the raw reports) read 0
with source `"v3-cli status on successful artifact-pair completion; output
errors instead exit 1"`. The goal raw report's `measurement_evidence.dirty`
is `true` (the gate summary/raw were already present in the worktree/main
checkout when the goal run started); the gate raw report's `dirty` is
`false`. `measurement_evidence.outer_exit` is `null` on both (no wrapper
status distinct from the CLI's).

## Raw and summary artifacts

| Profile | Raw path | Raw bytes | Raw sha256 | Summary path | Summary bytes |
| --- | --- | --- | --- | --- | --- |
| gate | `.bench-artifacts/t14-f12-neighborhood-read-of-selected-genomes/gate.json` | 94,189 | `ba355b8cf358fd4ff845d021f4db84a35cd20ef695f5c973afc9f0af9048367b` | `docs/progress/features/t14-f12-neighborhood-read-of-selected-genomes.json` | 97,039 |
| goal | `.bench-artifacts/t14-f12-neighborhood-read-of-selected-genomes/goal.json` | 511,818,289 | `41aa70e7cd5fd961b0b9444219369ced96ed859829a9ae149f07fcd693bf9cd8` | `docs/progress/features/t14-f12-neighborhood-read-of-selected-genomes-goal.json` | 6,804,498 |

Gate summary growth vs T13.F06's 96,946 bytes: +93 bytes (+0.10%) — the
gate profile is untouched, as predeclared. Goal summary growth vs T13.F06's
6,708,955 bytes: +95,543 bytes (+1.42%), from the three worlds' 50-genome
`neighborhood_read` blocks plus the five new comparison-block readings per
world; not a regression, consistent with the new per-world sample data.

`environment.generated_at`: gate `2026-09-14T18:55:08Z`, goal
`2026-09-14T19:05:01Z`.

## Wall time and threshold verdicts

**Gate** — `wall_clock_ms_total` = 744.50 ms. Compared against the epoch
baseline (`remove-complementary-nutrition.json`) and the previous closure
(`t13-f06-recruitment-and-retention-qualification.json`):

| Reference | `severe` | Six counters |
| --- | --- | --- |
| epoch `remove-complementary-nutrition.json` | false | all `level=ok`, deltas -1.32%..+1.00% |
| previous `t13-f06-recruitment-and-retention-qualification.json` | false | all `delta%=0.000000`, byte-identical |

**Goal** — `wall_clock_ms_total` = 545,940.968 ms (≈ 9.10 min).

| Reference | `severe` | Six counters |
| --- | --- | --- |
| epoch `t13-f03-mutation-target-applicability-goal.json` | false | all `level=ok`, deltas -0.38%..+3.62% |
| previous `t13-f06-recruitment-and-retention-qualification-goal.json` | false | all `delta%=0.000000`, byte-identical |

**Wall caps** (`environment`, all satisfied):

| Cap | Measured | Limit |
| --- | --- | --- |
| `neighborhood_read_wall_clock_ms_total` (this feature's predeclared cap) | 203.858 ms (67.479 / 59.712 / 76.667 ms per seed) | < 10 s |
| `neighborhood_evolved_wall_clock_ms_total` | 599.008 ms | < 180 s |
| `neighborhood_founder_wall_clock_ms` | 117.102 ms | < 10 s |
| Goal profile total | 545,940.968 ms (≈ 9.10 min) | < 15 min |

## `neighborhood_read` per world (`deterministic.goal_indicators.cases[i].neighborhood_read`, raw goal report)

`jq -c '.deterministic.goal_indicators.cases[] | .neighborhood_read as $r | {case: .case.name, sample_size: $r.sample_size, sample_size_requested: $r.sample_size_requested, birth_trials: $r.birth_trials, births_total: $r.births.births_total, sample_mean_generation: $r.mean_generation, population_size: $r.population_size}'`:

| World (seed) | `sample_size` | `birth_trials` | `births_total` | Sample mean generation | Population size |
| --- | --- | --- | --- | --- | --- |
| Orchards in grassland (11) | 50 | 100 | 5,000 | 47.000000 | 11,356 |
| Canyon country (22) | 50 | 100 | 5,000 | 48.500000 | 9,646 |
| Confluence (33) | 50 | 100 | 5,000 | 52.700000 | 9,733 |

All three worlds satisfy `sample_size == 50`, `birth_trials == 100`,
`births_total == 5,000` as predeclared.

**Population's mean generation.** T14.F04's terminal (tick 2000) checkpoint,
`jq -c '.deterministic.goal_indicators.population_persistence.per_seed[] |
{seed, population_mean_generation: .samples[-1].mean_generation}'`:

```
{"seed":11,"population_mean_generation":"46.766643"}
{"seed":22,"population_mean_generation":"48.068215"}
{"seed":33,"population_mean_generation":"52.733484"}
```

| World (seed) | Sample mean generation | Population mean generation (tick 2000) |
| --- | --- | --- |
| Orchards in grassland (11) | 47.000000 | 46.766643 |
| Canyon country (22) | 48.500000 | 48.068215 |
| Confluence (33) | 52.700000 | 52.733484 |

The 50-genome sample's mean generation tracks the population's tick-2000
mean generation closely on all three worlds (deltas +0.233357 / +0.431785 /
-0.033484).

Per-genome rows sum to the pooled tally (`jq -c` over
`.neighborhood_read.genomes[]` vs `.neighborhood_read.births.any_events`
and `.births.zero_event_births`):

| World (seed) | Σ `changed` | Σ `silent` | Σ `dead` | Σ `zero_event_births` | Pooled `changed`/`silent`/`dead`/`zero_event_births` |
| --- | --- | --- | --- | --- | --- |
| Orchards in grassland (11) | 732 | 1,390 | 17 | 2,861 | 732 / 1,390 / 17 / 2,861 (match) |
| Canyon country (22) | 650 | 1,478 | 11 | 2,861 | 650 / 1,478 / 11 / 2,861 (match) |
| Confluence (33) | 717 | 1,406 | 16 | 2,861 | 717 / 1,406 / 16 / 2,861 (match) |

Pooled fractions dividing by 5,000 (`silent_per_all_births`,
`changed_per_all_births`, `dead_per_all_births`):

| World (seed) | `silent_per_all_births` | `changed_per_all_births` | `dead_per_all_births` |
| --- | --- | --- | --- |
| Orchards in grassland (11) | 0.278000 | 0.146400 | 0.003400 |
| Canyon country (22) | 0.295600 | 0.130000 | 0.002200 |
| Confluence (33) | 0.281200 | 0.143400 | 0.003200 |

## Predeclared range check

Expected `changed_per_all_births` range: 0.104 (drift-walk depth-22) to
0.217 (depth-0 founder). Measured: 0.1464 / 0.1300 / 0.1434 — all three
worlds fall inside the range, close to the cited 12-genome evolved-half
comparison figures (0.156 / 0.130 / 0.133). No anomaly.

## Floor (new, this closure)

`neighborhood_read.changed_per_all_births` first readings, recorded as the
standing floor from the next closure on:

**Decision:** Orchards in grassland (11) = 0.146400, Canyon country (22) =
0.130000, Confluence (33) = 0.143400.

## Comparison block — `neighborhood_read_*` and `evolved_*_per_mutated_births`

`jq -c '.comparison.references[1].cases[].readings[] | select(.name | test("neighborhood_read|evolved_"))'`
against the committed goal summary, reference =
`t13-f06-recruitment-and-retention-qualification-goal.json`:

| World (seed) | `neighborhood_read_changed_per_all_births` | `neighborhood_read_silent_per_all_births` | `neighborhood_read_dead_per_all_births` | `evolved_changed_per_mutated_births` | `evolved_dead_per_mutated_births` |
| --- | --- | --- | --- | --- | --- |
| Orchards in grassland (11) | 0.146400 (ref null) | 0.278000 (ref null) | 0.003400 (ref null) | 0.340000 | 0.009091 |
| Canyon country (22) | 0.130000 (ref null) | 0.295600 (ref null) | 0.002200 (ref null) | 0.284545 | 0.012727 |
| Confluence (33) | 0.143400 (ref null) | 0.281200 (ref null) | 0.003200 (ref null) | 0.290909 | 0.007273 |

All three `neighborhood_read_*` keys carry `reference: null` on every
world, as predeclared (new keys, no prior closure to compare against).
The renamed `evolved_*_per_mutated_births` values are byte-identical to
T13.F06's stored `evolved_changed_per_all_births` /
`evolved_dead_per_all_births` values under the old names (checked via
`jq -c '.comparison.references[0].cases[].readings[] | select(.name | test("evolved"))'`
against T13.F06's own goal summary): world 1 0.340000/0.009091, world 2
0.284545/0.012727, world 3 0.290909/0.007273 — exact match on all three
worlds.

## Byte-identical `deterministic.goal_indicators` check (T14.F04-style)

`jq -S '.deterministic.goal_indicators | del(.cases[].neighborhood_read)'`
on this closure's committed goal summary vs
`jq -S '.deterministic.goal_indicators'` on T13.F06's committed goal
summary, `diff`:

```
$ diff current-gi-sum.json t13-gi-sum.json
$ echo $?
0
```

Zero-length diff, exit 0: with `neighborhood_read` deleted from each case,
`deterministic.goal_indicators` is byte-identical between this closure and
T13.F06, as predeclared (no simulation change lies between them on main).
Note: this comparison uses the *committed summaries* (both are the
projected `docs/progress/features/*-goal.json` form); the two raw reports
are not directly comparable this way because the raw-report and
summary-report shapes differ (confirmed: a first attempt diffing the raw
511.8 MB report's `goal_indicators` against T13.F06's summary produced a
12.6M-line diff purely from format/projection differences, not content
differences — discarded once the like-for-like summary comparison above
confirmed identity).

## Series entries

Added `docs/progress/features/t14-f12-neighborhood-read-of-selected-genomes.json`
to `gate.closed` and
`docs/progress/features/t14-f12-neighborhood-read-of-selected-genomes-goal.json`
to `goal_worlds.closed` in `docs/progress/benchmark-series.json`, following
T13.F06 (gate epoch `remove-complementary-nutrition.json`; goal-worlds epoch
`t13-f03-mutation-target-applicability-goal.json`, unchanged).

## Second goal run

Not applicable: per the 2026-09-05 user decision recorded in
`docs/workflow.md`'s Benchmark gate section, the goal profile runs once per
closure and a second run to re-check `deterministic` is not required (cross-
process reproducibility is covered by `crates/v3-core/tests/reproducibility.rs`
inside `make check`).

## Measured verdict summary

Gate: exit 0, `severe=false`, six counters byte-identical to T13.F06,
744.50 ms total, no wall/work flags. Goal: exit 0, `severe=false`, six
counters byte-identical to T13.F06, 545,940.968 ms total (≈9.10 min, inside
the 15-minute cap), no wall/work flags. `neighborhood_read` is defined on
all three worlds with `sample_size=50`, `birth_trials=100`,
`births_total=5,000`, per-genome rows summing exactly to the pooled tally,
and the three pooled fractions dividing by 5,000. The predeclared 10-second
`neighborhood_read_wall_clock_ms_total` cap is satisfied at 203.858 ms
(2.0% of budget); the unchanged 180-second evolved and 10-second founder
caps and the 15-minute goal total are all satisfied. The comparison block
carries the three `neighborhood_read_*` keys per world with `reference:
null` and `evolved_*_per_mutated_births` matching T13.F06's old-named values
exactly on all three worlds. The byte-identical `deterministic.goal_indicators`
check (keys deleted) holds with a zero-length diff against T13.F06 — the
predeclared direction of none is confirmed for every existing indicator.
The `changed_per_all_births` readings (0.1464 / 0.1300 / 0.1434) fall inside
the predeclared sanity range and become the standing floor per the
Decision above. The 50-genome sample's mean generation (47.0 / 48.5 / 52.7)
sits beside the population's tick-2000 mean generation from
`population_persistence.per_seed[].samples[-1].mean_generation`
(46.766643 / 48.068215 / 52.733484), tracking it closely on all three
worlds. Gate and goal summary byte growth vs T13.F06 are +93 bytes
and +95,543 bytes respectively, both consistent with the new per-world data
and no regression. No new full report staged; both raw reports remain
under the main checkout's ignored `.bench-artifacts/`.

## Mutation gate (2026-09-14)

Fresh `MUTANTS_ITERATE=0 make rust-mutants` on 411a1bbe (diff against
930ce9be), output `~/.local/share/petri-tools/mutants/t14-f12/mutants.out`,
`run-mode.txt` = `fresh`:

```
Found 41 mutants to test
ok       Unmutated baseline in 70s build + 50s test
MISSED   crates/v3-cli/src/bench/indicators.rs:679:24: replace += with *= in neighborhood_read_for_seed in 3s build + 50s test
41 mutants tested in 7m: 1 missed, 31 caught, 9 unviable
```

| Survivor | Resolution | Test |
| --- | --- | --- |
| `indicators.rs:679:24` `generation_sum += creature.generation` -> `*=` | killed | `bench::indicators::tests::neighborhood_read_samples_seeded_ranks_and_pools_its_rows_over_all_births` assigns generations `2..=7` to the six founders before the read; the sum over four sampled rows is at least 14 while the product from a zero accumulator stays 0 |

Incremental confirmation, `MUTANTS_ITERATE=1 make rust-mutants` (not closure
evidence): `1 mutant tested in 2m: 1 caught`. After the test edit:
`cargo fmt --all -- --check` clean, `cargo clippy --workspace --all-targets -- -D warnings`
clean, `cargo test -p v3-cli` exit 0 (105 unit, all integration binaries ok).
No production, test-selection, or mutation configuration changed, so no
second fresh run was required.
