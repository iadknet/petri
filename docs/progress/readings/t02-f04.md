# T02.F04 — Grazing Recovery and Overuse readings

Verification results for the feature spec
[`docs/specs/roadmap/t02-f04-grazing-recovery-and-overuse.md`](../../specs/roadmap/t02-f04-grazing-recovery-and-overuse.md).
Build pass run from the worktree `.claude/worktrees/t02-f04` on 2026-09-15.
`PATH` prefixed with `~/.local/share/petri-tools/bin` and
`~/.local/share/aquaproj-aqua/bin`.

## Build-pass commands

| Command | Result |
| --- | --- |
| `cargo test -p v3-core --test viability` (run first, on the unchanged tree) | 25 passed, 0 failed |
| `cargo test -p v3-core --test viability` (after the core change, before other verification) | 25 passed, 0 failed; 26 passed once `default_run_reports_grazing_through_sim_stats` was added |
| `cargo test -p v3-core --lib grazing` | 3 config tests passed |
| `cargo test -p v3-core --lib ordinary_food` | 29 passed (4 proptest, 4 layer, 3 state, 5 ecology fixtures among them) |
| `cargo test -p v3-core --lib record_food_growth` | 1 passed |
| `cargo test -p v3-core --no-fail-fast` (before re-pin) | only `applied_trajectory` and `baseline_worlds::legacy_default_short_run_identity` failed |
| `cargo test -p v3-core --test applied_trajectory --test baseline_worlds` (after re-pin) | 1 passed / 19 passed, 1 ignored; both new values reproduced on a second run |
| `cargo test -p v3-cli --test bench_artifacts` | 22 passed, 1 ignored, including `checked_in_goal_recipes_resolve_with_grazing_enabled_at_production_defaults` |
| `cargo test -p v3-server --test server grazing` | 3 passed |
| `cargo test --workspace --no-fail-fast` | every target ok: v3-cli 105 / 11 / 20 / 22 / 11, v3-core 1498 (2 ignored) + integration targets 1 / 19 / 7 / 4 / 2 / 3 / 3 / 13 / 10 / 25 / 1, v3-server 46 / 2 / 99 |
| `cargo fmt --all -- --check` | clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | clean (after `case_readings` gained the `per_food_type_readings` helper) |
| `cargo check --workspace --all-targets` | clean |
| `make check` | exit 0 |
| `make roadmap-check` | validation passed |
| `frontend`: `npx tsc --noEmit -p .` | exit 0 |
| `frontend`: `npx biome check src` | 4 pre-existing `noArrayIndexKey` warnings in untouched startup sections, none in touched files |
| `frontend`: `npx vitest run src/components/ConfigPanel.test.tsx src/components/ControlBar.test.tsx src/stores/startupConfig.test.ts` | 61 passed |

## Re-pinned trajectory identities

All three moved for the spec's stated reason: grazing ships enabled, so every
applied Eat halves the bitten cell's fertility modifier and the food grid
regrows differently from the first bite onward. No other pin moved
(`reproducibility`, `temporal_fixtures`, `terrain`, and the remaining
`baseline_worlds` fixtures are unchanged).

| Pin | Location | Old | New |
| --- | --- | --- | --- |
| `accounting_preserves_pre_feature_sampled_trajectories_and_actions` | `crates/v3-core/tests/applied_trajectory.rs` | `96497a4fbe59ff4fb397c54ede7dd4e93e36d790d9090f9eaa3ea25c77ae2fa9` | `1174f677c2965c38b7a039017bc6db8e4800a2bddfa8364498e335722a23d88e` |
| `legacy_default_short_run_identity` | `crates/v3-core/tests/baseline_worlds.rs` | `1397923697343438469` | `1819237026958739067` |
| `orchards-in-grassland` config digest | `crates/v3-cli/tests/bench_artifacts.rs` | `sha256:f1b9fe5ae4f1c1a1698fee18983152334500a5b531d3d0d322998a890a5295ab` | `sha256:81450ac8a7d4ed087d7f888fa992c486ecc2257d3db85fa17715619a8f1bbe80` |
| `canyon-country` config digest | same test | `sha256:9c8fcf58ed47e5307ca04cd41f4f0d8dc597f9036381c6482504c1b8da587e58` | `sha256:c8d2ab9402e2eb74a2bf7ffbdfa3b071a989680d47e39b7d5cf2ec8cb3b81b2a` |
| `confluence` config digest | same test | `sha256:a870b08a9d54e82c78d49c85e9e3c689d471c6ce4fc2dac6223fa3adb0d24250` | `sha256:a8b84ed7b3a4f2319bea8f76870a452dc5ae83582192a0110727cde872ae072d` |

The recipe digests move because the resolved config now serializes the
`grazing` block the byte-identical recipes inherit from the production default.

## Focused and property tests

| Test | Location | Checks |
| --- | --- | --- |
| `bite_stays_within_range_and_is_non_increasing` (proptest) | `crates/v3-core/src/kernel/ordinary_food/grazing.rs` | `bite_modifier` stays in `[floor, 1.0]` and never rises, for `floor` drawn first and `m` in `[floor, 1.0]` |
| `recovery_stays_within_range_and_is_non_decreasing` (proptest) | same | `recover_modifier` stays in `[floor, 1.0]` and never falls |
| `recovery_reaches_exactly_one_within_the_ceiling` (proptest) | same | from any `m`, exactly `1.0` within `ceil((1 - m) * recovery_ticks) + 1` ticks; `recovery_ticks` bounded to `1..=10_000` (see Observations) |
| `a_bite_of_one_type_leaves_the_other_bit_identical` (proptest) | same | up to five bites of one type leave every other type's grid bit-identical |
| `five_default_bites_reach_the_floor` | same | 0.5, 0.25, 0.125, 0.0625, 0.05 |
| `disabled_grazing_records_no_bite_and_reads_one` | same | disabled bite is a no-op; `multiplier_at` reads 1.0 disabled |
| `recover_holds_barriers_at_one_and_summarizes_passable_cells` | same | barrier cell reset to 1.0, mean over passable cells, grazed count |
| `reset_and_resize_start_from_one` | same | `reset` and `resize_type_storage` return every modifier to 1.0 |
| `consume_type_bites_only_the_removed_type_and_only_when_positive` | `crates/v3-core/src/kernel/ordinary_food/state.rs` | one bite per positive removal, zero-amount consume is not a bite |
| `consume_any_bites_each_type_it_removed` | same | `consume_any` bites every type it removed a positive amount from |
| `resize_type_storage_resets_grazing_to_one` | same | catalog change resets the layer |
| `bitten_cell_recolonizes_at_factor_of_the_unbitten_rate` | `crates/v3-core/src/kernel/ordinary_food/ecology.rs` | spread onto a once-bitten empty cell lands `0.5` of the unbitten delta (`Bounded` 2x1 world, fixed target) |
| `floored_cell_recolonizes_at_floor` | same | nine bites: `0.05` of the unbitten delta |
| `empty_grazed_cell_recovers_each_growth_pass` | same | an empty bitten cell recovers 0.25 per pass at `recovery_ticks` 4 and the summary reads it |
| `disabled_grazing_reads_one_and_re_enabling_starts_from_one` | same | disabled reads 1.0 everywhere; flipping `enabled` resets to 1.0; live `factor`/`floor`/`recovery_ticks` edits leave the grid alone |
| `occupancy_depletion_multiplies_beside_grazing` | same | local delta = `source * rate * 0.5 * MIN_GROWTH_MULTIPLIER` |
| `grazing_defaults_are_the_production_values`, `grazing_block_and_its_fields_are_serde_defaulted`, `normalize_grazing_clamps_ratios_and_restores_defaults` | `crates/v3-core/src/config/simulation.rs` | defaults, container-level serde default, normalization rules |
| `record_food_growth_summary_updates_food_depletion_fields` | `crates/v3-core/src/simulation/stats.rs` | per-type mean modifier and grazed-cell share reach `SimStats` and reset each tick |
| `default_run_reports_grazing_through_sim_stats` | `crates/v3-core/tests/viability.rs` | a 40-tick default-config run with applied Eats reports grazed cells and a sub-1.0 mean modifier |
| `checked_in_goal_recipes_resolve_with_grazing_enabled_at_production_defaults` | `crates/v3-cli/tests/bench_artifacts.rs` | the three recipes resolve to `GrazingConfig::default()` |
| `case_readings_follow_the_case_seed_and_observation` | `crates/v3-cli/src/bench/comparison/tests.rs` | `grazing_modifier_mean_type_{i}` and `grazed_cell_share_type_{i}` are compared readings |
| `tracking_fields_default_when_absent_and_survive_a_round_trip` | `crates/v3-cli/src/bench/tracking/tests.rs` | the two readings are on the wire beside `food_density_total` |
| `get_config_includes_food_grazing_defaults`, `patch_config_roundtrips_food_grazing_fields`, `patch_config_rejects_invalid_food_grazing_values` | `crates/v3-server/tests/server.rs` | GET carries the block; PATCH round-trips all four fields; `factor` 1.5 and `recovery_ticks` 0 are rejected as non-canonical |
| `supports grazing runtime controls` | `frontend/src/components/ConfigPanel.test.tsx` | toggle and three fields edit `world.food.shared.grazing` in the runtime draft |

## Observations

- The spec's recovery invariant cannot hold for arbitrary `u32
  recovery_ticks` in f32: past roughly `1e7` the step `1 / recovery_ticks`
  falls under half an ulp near 1.0 and `m + step == m`. The property test
  bounds `recovery_ticks` to `1..=10_000`, which covers the production 1000
  and every sensible runtime edit.
- `seed_density` resets the grazing layer to 1.0 alongside the occupancy
  depletion layer; the spec is silent on world seeding, and a freshly seeded
  world has no bites.
- The goal-report readings live on `WorldTracking` (`grazing_modifier_mean`,
  `grazed_cell_share`, per-type `Vec<String>` beside `food_density_total`)
  rather than on `TrackedFractions`, because they are final-tick state, not
  ratios of the tracking totals. Their comparison rows follow
  `typed_eat_share_type_{i}` as the spec asks.
- `docs/reference/v3-tick-orchestration-spec.md` Phase 0 sub-steps 1 and 2
  now name the grazing recovery step; the spec listed only the three other
  reference documents, but the canonical Phase 0 order would otherwise have
  been untrue.
