# T16.F01 — Gate Before Charge in Reproduction readings

Tables and transcripts for the feature spec
[`docs/specs/roadmap/t16-f01-gate-before-charge-in-reproduction.md`](../../specs/roadmap/t16-f01-gate-before-charge-in-reproduction.md).

## Focused tests

`crates/v3-core/src/simulation/actions/mod.rs` (`tests`), through
`apply_reproduce` on the founder at age `min_reproduce_age`, default config:

| Test | Branch | Fixture |
| --- | --- | --- |
| `apply_reproduce_min_energy_rejection_does_not_charge_reproduce_cost` | `energy - cost < min_reproduce_energy` | energy 30.0, request 20.0 |
| `apply_reproduce_infeasible_transfer_rejection_does_not_charge_reproduce_cost` | `energy - cost < transfer` | energy 50.0, request 100.0 |
| `apply_reproduce_non_positive_transfer_rejection_does_not_charge_reproduce_cost` | `transfer <= 0.0` | energy 80.0, request 0.0 |
| `apply_reproduce_birth_pays_cost_then_transfer_as_two_subtractions` | accepted | energy 80.0, request 20.0 |

The three rejection tests share `assert_energy_rejection_is_free`: parent
energy bit-identical, `pending_death_cause` unchanged,
`action_charges.reproduce` and `parental_transfer_debit` unchanged,
`reproduction_actions_rejected_total` +1 and the
`RejectedEnergyConstraints` reason count at 1. The accepted test computes
`cost` as the engine's product expression (`reproduce_charge`) and pins the
parent at `(energy - cost) - transfer` bit-exactly, the two flow debits as
`applied_debit` of each step, and the child at `transfer`; on the red run
(tests written, engine unchanged) it and the age precedent were the two
passes against the four rejection-side failures, so it is the
bitwise-stability witness for invariant 3.

`crates/v3-core/src/simulation/actions/reproduction.rs` (`tests`):
`energy_rejected_attempts_leave_the_reproduce_flow_at_zero` (invariant 4):
200 founders on 48x48 for 40 ticks with `min_reproduce_energy =
max_energy + 1`, so every attempt is an energy rejection; asserts
rejections > 0, spawned 0, and `action_charges.reproduce` exactly 0.0
(pre-feature value on the red run: 263.601).

`crates/v3-core/src/simulation/tick/tests/energy_accounting.rs`:
`reproduce_below_cost_rejects_free_and_records_no_crossing` replaces the
Reproduce arm of
`every_base_action_crossing_keeps_its_cause_through_the_action_floor`;
that arm (energy 1.0, cost 2.0) relied on the charge landing before the
gate, and acceptance now requires `energy - cost >= transfer > 0`, so the
reproduce charge can no longer cross zero.

## Pins that moved

| Test | Kind | Old | New | Reason |
| --- | --- | --- | --- | --- |
| `actions::tests::reproduction_rejection_charging_order_and_transfer_accounting` (energy-rejection loop) | contract pin of the removed order | energy `- cost`, charge `cost` | energy unchanged, charge 0.0 | pinned the charge-before-gate order itself |
| `tick::tests::energy_accounting::parental_transfer_counts_only_a_successful_birth_and_newborn_starts_clear` (unspawned case) | contract pin of the removed order | charge 1.0 | charge 0.0 | same |
| `tests/applied_trajectory.rs` digest | evolved trajectory (mutation probability 1.0, seeds 7 and 19, 64 ticks) | `35d2928667a5086b224a35089a66ddc1c669108b729a8ae2ae519f3bc89d954e` | `01143817f27e4b493af53cc81d680388ce260baa6bd4f42fd3445b968e8b1f57` | diverges at the first energy-gate rejection (invariant 5); two runs agreed |
| `bench::tests::the_real_run_path_carries_a_full_occupancy_grid_at_every_checkpoint` seed | evolved trajectory (32x32, 8 founders, 250 ticks) | seed 18 (population 1 on 1 cell at the horizon) | seed 1 (population 5 on 5 cells) | seed 18's trajectory now busts from 39 at tick 100 to one survivor by tick 200; 15 of seeds 0..=25 keep a spread population, 7 go extinct, 4 end on one cell, so the pin is seed-dependent |

No founder-only pin moved: the founder's charge and the T03.F11 multiplier
pins are unchanged, and `cargo test -p v3-core --test viability` passed
first, before the rest of the suite.

## Benchmark (roadmap-benchmark-specialist, 2026-09-17)

Commands, sequentially, nothing else running:

1. `make bench PROFILE=gate FEATURE=t16-f01-gate-before-charge-in-reproduction`
   — CLI arguments `bench --profile gate --feature
   t16-f01-gate-before-charge-in-reproduction`, `make` exit 0 (no error
   output; build then run completed), `measurement_evidence.cli_exit` in
   both the summary and the raw report: `{"code":0,"source":"v3-cli status
   on successful artifact-pair completion; output errors instead exit 1"}`.
   `comparison.severe` false against both references.
2. `make bench PROFILE=goal FEATURE=t16-f01-gate-before-charge-in-reproduction`
   — CLI arguments `bench --profile goal --feature
   t16-f01-gate-before-charge-in-reproduction`, `make` exit 0 (backgrounded
   and polled; no error output, both artifact files written, log ends on
   the comparison printout). `measurement_evidence.cli_exit` `{"code":0,
   "source":"v3-cli status on successful artifact-pair completion; output
   errors instead exit 1"}`. `comparison.severe` false against both
   references. `environment.neighborhood_evolved_wall_clock_ms_total`
   438.637 ms (cap 180,000 ms); no `wall_clock` comparison objects were
   populated (both `null`) and no work counter crossed the +10%/+50%
   thresholds — no wall or work flag.

Reports and sizes:

| Profile | Summary path | Summary bytes | Summary sha256 | Raw path | Raw bytes | Raw sha256 |
| --- | --- | --- | --- | --- | --- | --- |
| gate | `docs/progress/features/t16-f01-gate-before-charge-in-reproduction.json` | 97,169 | `3a4fcf02871c3b8af3744c8387f56100617142dec434621e712b2286211da7d3` | `.bench-artifacts/t16-f01-gate-before-charge-in-reproduction/gate.json` (local, not staged) | 94,342 | `81ffe4f024bbdc0c122095bf229a647b4176f6cb2909f548774ffb2e1e3b5bd8` |
| goal | `docs/progress/features/t16-f01-gate-before-charge-in-reproduction-goal.json` | 6,871,877 | `500633cecddc713aef8f6e65281020b400257938a8ade98b4beb6f2110f54c62` | `.bench-artifacts/t16-f01-gate-before-charge-in-reproduction/goal.json` (local, not staged) | 522,450,491 | `2ab7d330b1cc72c63f540af7feac90753de687723ed877d56c3b30f26f9c1bfb` |

Gate work-counter deltas (all `severe=false`):

against `remove-complementary-nutrition.json` (epoch baseline): mesh_hops
−0.293351%, vm_steps −1.579904%, graph_relax_iters +0.143785%,
plasticity_updates −56.467639%, actions_applied −0.061543%, births
−6.172517% — all `ok`.

against `t11-f22-meaning-stable-input-references.json` (latest closure,
the gate identity reference): mesh_hops +0.004948%, vm_steps +0.008799%,
graph_relax_iters +0.006522%, plasticity_updates −4.572214%,
actions_applied −0.015452%, births −0.003980% — all `ok`, none severe or
flagged, **but none is `0.000000`**.

Goal work-counter deltas (all `severe=false`, no wall flag):

against `t11-f19-per-unit-mutation-supply-goal.json` (epoch baseline):
mesh_hops −6.656506%, vm_steps −3.121340%, graph_relax_iters −5.626185%,
plasticity_updates −75.569979%, actions_applied −3.292270%, births
−8.704443%.

against `t11-f22-meaning-stable-input-references-goal.json` (latest
closure): mesh_hops +0.210205%, vm_steps −0.290198%, graph_relax_iters
−3.270277%, plasticity_updates −17.623209%, actions_applied −0.082149%,
births −1.794356%.

### (a) Gate identity check — measured, not the predeclared identity

The predeclaration expected all six `percent_delta` values against the
T11.F22 gate reference to be `0.000000` and every per-seed
`final_population`/`births` pair to match bit for bit. **This did not
hold**: all six deltas above are nonzero (largest magnitude
plasticity_updates at −4.572214%), and the per-seed pairs differ:

| Seed | final_population (this run) | final_population (T11.F22) | births (this run) | births (T11.F22) |
| --- | --- | --- | --- | --- |
| 11 | 1,896 | 1,890 | 2,968 | 2,964 |
| 22 | 1,853 | 1,849 | 3,043 | 3,032 |
| 33 | 1,874 | 1,890 | 3,031 | 3,043 |

Per the spec's own reading rule for this outcome ("Any nonzero delta means
a founder did fail the energy gate; that outcome is read under the
standard thresholds, reported as a predeclaration miss, and the track note
is corrected — it is not a blocker by itself"): this is a **predeclaration
miss**, not a threshold failure (`severe=false`, all levels `ok`). It is
consistent with invariant 6 (a founder with energy in `[30, 30 + cost)`
attempts and is rejected at Step 6): at least one founder fell in that
window during the 75-tick × 3-seed gate profile and its trajectory
diverged from T11.F22, exactly the divergence the spec's invariant 5 and
the `applied_trajectory` re-pin already document for the goal-scale
evolved case. Reported to the orchestrator as a spec-facing finding, not
remediated here.

### (b) Orchards goal-profile population trajectory — reading, not a gate

| Tick | Population before (T11.F22) | Population after (this run) |
| --- | --- | --- |
| 100 | 96,680 | 96,879 |
| 200 | 1,399 | 1,350 |
| 300 | 127 | 131 |
| 400 | 74 | 59 |
| 1,000 | 30 | 20 |
| 1,700 | 10 | 9 |
| 1,800 | 10 | 7 |
| 1,900 | 10 | 7 |
| 2,000 (final) | 10 | 7 |
| Peak | 100,000 (tick 66) | 100,000 (tick 66) |
| Minimum | 10 | 7 |
| Plateau | 10.32 | 7.988 |

The collapse trajectory persists and its floor moved slightly lower (final
population 7 vs. 10; plateau 7.988 vs. 10.32). Canyon country (seed 22)
and Confluence (seed 33), for comparison, alongside:

| World | final_population | peak_population | peak_tick | minimum_population | plateau_population |
| --- | --- | --- | --- | --- | --- |
| Orchards (seed 11) | 7 | 100,000 | 66 | 7 | 7.988 |
| Canyon country (seed 22) | 4,323 | 100,000 | 62 | 398 | 3,573.072 |
| Confluence (seed 33) | 4,341 | 100,000 | 61 | 163 | 4,515.858 |

This reading does not gate closure; it is recorded per the spec's
predeclaration.

### (c) `action_charges.reproduce / births` per world, before/after

| World | Before (T11.F22, cited in spec) | After (this run) | Direction |
| --- | --- | --- | --- |
| Orchards | 31,373.63 / 152,093 = 0.2063 | 21,018.074402 / 151,207 = 0.1390 | falls (predeclared) |
| Canyon country | 238,504.81 / 196,811 = 1.2119 | 92,478.905163 / 198,751 = 0.4653 | falls (predeclared) |
| Confluence | 190,730.72 / 261,980 = 0.7281 | 69,179.593945 / 248,102 = 0.2788 | falls (predeclared) |

All three worlds match the predeclared direction (falls), consistent with
rejected attempts no longer feeding `action_charges.reproduce` (invariant
4). `failed_action_penalty` flow this run (unchanged mechanism, moves only
with the trajectory, no predeclared direction): Orchards 3,173.248296,
Canyon country 64,860.087268, Confluence 53,940.715620.

### (d) T11.F14 readings the goal summary carries — recorded, no direction

Present in `deterministic.goal_indicators.cases[*].drift_depth` (version
`drift-depth-v3`) and `.recruitment_paths` for all three worlds, with
checkpoints `[0, 22, 250, 1000, 2000]`. Final-checkpoint (tick 2000)
backend totals, `executed`, and `contributing` counts:

| World | graph total/executed/contributing | vm total/executed/contributing |
| --- | --- | --- |
| Orchards (seed 11) | 3,698 / 102 / 5 | 3,854 / 130 / 32 |
| Canyon country (seed 22) | 3,392 / 116 / 4 | 3,628 / 124 / 27 |
| Confluence (seed 33) | 3,698 / 102 / 5 | 3,854 / 130 / 32 |

Orchards and Confluence rows are identical because the drift-depth battery
is a fixed-seed founder walk (`walk_seed_formula: 90000 + lineage_index`),
world-independent for worlds with the same food-type count; T11.F22's goal
report shows the same coincidence.

`lineage_diversity` is `null` in all three cases this run (not computed by
this goal-indicator pass). Sensor-census and recruitment-cohort fields are
present per checkpoint but not reproduced here in full (raw report is
522 MB local-only, not staged); consistent with the spec's instruction to
record that these T11.F14 readings are carried, with no direction claimed.

## Predeclaration misses / exceptions to report

- Gate identity (item a above): nonzero `percent_delta` on all six work
  counters against T11.F22 and mismatched per-seed final_population/births
  — a predeclaration miss under the spec's own reading rule, not severe,
  not a wall flag. The track note ("founder gate unchanged") needs
  correction per the spec's own instruction; routed to the orchestrator.
- No severe flag and no wall flag on either profile.

## Mutation gate (roadmap-mutation-specialist, 2026-09-17)

Fresh `MUTANTS_ITERATE=0 make rust-mutants` on the reviewed HEAD
(`11eafd65`), diff against merge base `61ca4009`:

    17 mutants tested in 3m: 1 missed, 15 caught, 1 unviable

Output: `~/.local/share/petri-tools/mutants/t16-f01/mutants.out`, run mode
`fresh`. No timeouts. The later `MUTANTS_ITERATE=1` pass rotated that
directory to `mutants.out.old` (its `missed.txt` holds the one survivor
below) and left `mutants.out/previously_caught.txt` with the 16 accumulated
caught/unviable entries plus `caught.txt` with the kill.

Survivor and resolution:

- `crates/v3-core/src/simulation/actions/reproduction.rs:182:19: replace <
  with <= in apply_reproduce` — the step-6 energy gate
  (`after_cost < min_reproduce_energy`). No test pinned the boundary where
  the post-charge energy lands exactly on the floor. **Killed** by
  `apply_reproduce_accepts_post_charge_energy_exactly_at_min_reproduce_energy`
  in `crates/v3-core/src/simulation/actions/mod.rs`: a parent at 80.0 with
  `min_reproduce_energy` pinned to the engine's own f32 `energy - cost`
  must still spawn and land bit-exactly on `after_cost - transfer`.
  `MUTANTS_ITERATE=1 make rust-mutants` -> "1 mutant tested in 76s: 1
  caught". Test-only change; no production, config, or selection edits, so
  no second fresh run is required under the one-fresh-run rule.

Focused checks after the test edit: `cargo test -p v3-core` -> 1561 lib +
89 integration passed, 0 failed; `cargo fmt --all -- --check` -> exit 0;
`cargo clippy --workspace --all-targets -- -D warnings` -> exit 0.
