# T19.F04 — Readings

Evidence for [the feature spec](../../specs/roadmap/t19-f04-vote-based-action-selection.md).
Base commit for every "before" record: `f4835da5` (the spec commit on top of
`3a063f56`, identical production code).

## Founder port

The founder's decision node is a graph voting `Eat`, the cardinal `Move` and
`Reproduce` sinks, and `Terminate` (`creature/cgp_founder.rs`,
`build_cgp_founder_decision_graph`). Its measured `genome_size()` is 97 for
V3Alpha1, against 111 for the VM decision node it replaces.

Size is the only lever the port moves in the founder-only run. With node 1
padded by 14 unread input references to the old 111 units, the
`founder_only_trajectory_digest_is_pinned` run reproduces the old birth count
exactly (185 births, final population 0); only the digest moves, through the
energy bits of the compute charge (two or three graph passes per tick instead
of one graph and one VM dispatch). Without the padding the smaller genome pays
14 fewer carrying units per tick and the run reaches 323 births.

| Run (seed 2026, 2,000 ticks, mutation off) | Births | Final population |
| --- | --- | --- |
| Before (`f4835da5`) | 185 | 0 |
| Vote founder padded to 111 units (scratch, not committed) | 185 | 0 |
| Vote founder, 97 units | 323 | 0 |

One founder tick at the defaults: forage with food (`Eat, Move E`) runs three
passes and six hops at graph cost 0.00039; no food (`Move E`) runs two passes
and four hops at 0.00026; `NoDecision` ends both.

### Founder per-tick compute cost, before and after

The same founder-only run, read from `stats.energy_flows` divided by
`creature_ticks_total` at tick 2,000. A throwaway integration test (not
committed) was run in this worktree and in a `git archive f4835da5` copy:
`cargo test -p v3-core --test founder_cost_probe -- --nocapture`.

| Per creature-tick | Before (`f4835da5`) | After |
| --- | --- | --- |
| Creature-ticks; births | 9,912; 185 | 17,925; 323 |
| `vm_compute` | 2.64e-6 | 0 |
| `graph_compute` | 3.018e-5 | 2.932e-4 |
| `mesh_ramp`, `hebbian_learning` | 0, 0 | 0, 0 |
| Compute total | 3.28e-5 | 2.93e-4 |
| `genome_carrying` | 1.1259e-2 | 9.826e-3 |
| Compute plus carrying | 1.1292e-2 | 1.0119e-2 |
| `lifecycle_decay` | 0.50716 | 0.50647 |

Compute rises about ninefold (two or three graph passes per tick instead of
one graph and one VM dispatch) but stays under 3% of the carrying cost. The
carrying cost falls by 1.43e-3 per tick (97 units against 111), which is
the larger move: the birth rise comes from the smaller genome, as the
111-unit padding run above shows, not from cheaper cognition.

## Moved pins (re-pinned after two agreeing runs)

| Pin | Before (`f4835da5`) | After |
| --- | --- | --- |
| `viability::founder_only_trajectory_digest_is_pinned` (births, final population, digest) | `(185, 0, 63498f8d…f2d0be)` | `(323, 0, 54db30ad…e529c)` |
| `baseline_worlds::legacy_default_short_run_identity` | `5811834416729882562` | `14378788429370811441` |
| `applied_trajectory::accounting_preserves_pre_feature_sampled_trajectories_and_actions` | `09b9e53c…5ff4b` | `dcf75b04…e1e8` |
| `applied_trajectory::mutation_on_applied_trajectory_guard_is_pinned` | `99ef9a14…8273` | `5e094be3…4a68` |
| `FOUNDER_GENOME_SIZE_UNITS`; per-unit expected events (`0.005 ×`) | 111; 0.555 | 97; 0.485 |
| v3-cli `tick_sample` founder `mean_genome_size` | 111 | 97 |

No `config_digest` and no v3-cli recipe digest pin moved.

## Recruitment paths (T13 fixtures, re-expressed as votes)

A blank module now reaches a `Move(E)` vote in one edge (Graph) or one
`AddVote` (VM), so all nine forms qualify: `graph_blank` and `vm_blank` join
the seven that qualified before, and no form records a growth gap. Every
qualified path keeps the payload reading of the T13 table: earlier steps
score 4 with no loss; the last scores 8 with bypass loss 4 and ancestral loss
4 (6 on the unprepared forms).

Fixture sites `[input_refs, route_targets, vm_instructions, vm_constants,
graph_compute_nodes, graph_edges]` and genome size:

| Form | Before | After |
| --- | --- | --- |
| graph_blank | `[1,2,1,0,2,3]` 14 | `[1,2,1,0,1,1]` 9 |
| graph_copy | `[2,2,1,0,4,6]` 22 | `[2,2,1,0,2,2]` 12 |
| graph_split | `[2,2,1,0,5,7]` 24 | `[2,2,1,0,3,3]` 14 |
| graph_unprepared, graph_prepared | `[2,2,1,1,4,8]` 25 | `[2,2,1,1,2,4]` 17 |
| vm_blank | `[1,2,13,2,0,0]` 21 | `[1,2,7,1,0,0]` 14 |
| vm_copy | `[2,2,23,4,0,0]` 34 | `[2,2,11,2,0,0]` 20 |
| vm_unprepared, vm_prepared | `[2,2,21,5,0,0]` 33 | `[2,2,9,3,0,0]` 19 |

Production lineage pins (`experiment.rs`; proposal discovery, retained,
viable retained, retained useful; discard counts stay `[0, 0]`):

| Arm | Before | After |
| --- | --- | --- |
| graph_prepared Drift | `[19,15,13,7]` | `[18,13,13,1]` |
| graph_prepared Selection | `[20,20,20,20]` | `[18,18,18,17]` |
| vm_prepared Drift | `[19,16,15,9]` | `[19,16,16,8]` |
| vm_prepared Selection | `[19,19,19,18]` | `[19,19,19,19]` |
| graph_prepared CostSelection (censored) | `[21,21,21,21]` (11) | `[16,16,16,16]` (16) |
| vm_prepared CostSelection (censored) | `[20,20,20,20]` (12) | `[21,21,21,21]` (11) |

Known lineages, found again by search on the vote founder (same arms where
one exists):

| Test | Before | After |
| --- | --- | --- |
| specializing, retained at +64 / censored at +16 | arm 22 b1 l13, generation 192 | arm 22 b0 l3, generation 106 (129 specialized proposals) |
| deleting retention | arm 12 b0 l2, discovery 5, deleted at depth 94 | arm 12 b2 l2, discovery 9, deleted at depth 51 |
| generation-0 eligibility | arm 25 b1 l11 | arm 25 b3 l8 |

Payload table (`recruitment_paths_print_payload_readings_table`):

| Form | Steps before the last (score 4) | Last step (score, bypass, ancestral) |
| --- | --- | --- |
| graph_blank | cue_added, vote_edge_added | activated (8, 4, 4) |
| graph_copy, graph_split | vote_edge_added | activated (8, 4, 4) |
| vm_blank | cue_added, read_cue, vote_east | activated (8, 4, 4) |
| vm_copy | leading_halt_removed | activated (8, 4, 4) |
| graph_unprepared | ring_added, cue_edge_retargeted, north_vote_removed, vote_edge_added | activated (8, 4, 6) |
| vm_unprepared | ring_added, read_ref_idx_1, read_sub_idx_1, read_sub_idx_2, vote_to_east | activated (8, 4, 6) |
| graph_detour | cue_added | vote_edge_added (8, 4, 4) |
| vm_detour | cue_added, read_cue | vote_east (8, 4, 4) |

## One-edge census (founder, node 1)

Every unit-weight edge from each of node 1's 18 input-leaf sub-values into
each of the 27 vote sinks, classified on `neighborhood-v1`
(`one_edge_census_print`). `Reordered` and `Recount` are tallies inside
`Changed`.

| Sinks | Silent | Changed | Reordered | Recount | Dead |
| --- | ---: | ---: | ---: | ---: | ---: |
| Eat | 9 | 9 | 0 | 3 | 0 |
| Move N, E, S, W (each) | 9 | 9 | 0 | 3 | 0 |
| Move NE, SE, SW, NW (each) | 8 | 10 | 0 | 2 | 0 |
| Reproduce, each direction | 8 | 10 | 0 | 0 | 0 |
| StealEnergy, each direction | 8 | 10 | 0 | 0 | 0 |
| Terminate | 12 | 6 | 0 | 0 | 0 |
| Decide | 18 | 0 | 0 | 0 | 0 |
| Total (486 edges) | 235 | 251 | 0 | 23 | 0 |

No single edge kills the founder; a `Decide` edge is always silent on it
(node 1 ends the chain, so a `Decided` pass end there changes nothing).

## Test fixtures adapted to the pass loop

- `temporal_fixtures` E1 and E2 run with `max_actions_per_turn = 1` (a
  labeled deviation): a voting node is otherwise dispatched again in the
  pass after its commit, doubling the per-tick trace credit they pin.
- `reproducibility` routes each founder's decision node into an injected VM
  node holding the paired slot groups; the founder has no VM node.
- `mutational_neighborhood`: `VmCopyConstantBlock` never applies on the
  all-Graph founder (0 applied), where the audit recorded 100% silent.
- v3-cli occupancy-grid pin moves from seed 3 (now extinct before tick 250)
  to seed 1 (2 creatures on 2 cells).
- v3-cli severe-regression fixtures use `mesh_hops`, since the founder's
  `vm_steps` is zero.
- `gate_profile_has_no_severe_regression_against_series_references` fails
  until the closing commit re-pins the gate epoch: gate `mesh_hops` per
  creature-tick is 4.829716 against the T19.F02 epoch's 2.040238 (+136.7%,
  the predeclared severe rise).

## Grep proof

```sh
grep -rn -e PushAction -e PopAction -e ExecuteActionQueue -e WriteDirectionBid \
  -e ActionSlot -e ExecuteGate -e action_bank -e execute_gate \
  -e MutateActionSlotBehavior -e ActionEmitted -e DirectionBank \
  crates frontend/src --exclude-dir=target --exclude-dir=node_modules
```

Result (rerun after the guard change): one line,
`crates/v3-core/src/mutation/graph/tests.rs:31`. Exception: that line is the
enforcement test itself, asserting that no `GraphOperator` name contains
`ActionSlot` (the catalog holds 21 operators). No production or fixture code
names a deleted surface.

## Decide guard, both clauses

`Decide` ends a pass when some kind's effective vote is positive, or when the
queue is non-empty and `Terminate` is positive (`runtime/mesh.rs`). W17 keeps
its reading (pass 2 `PassCapReached`, tick `NoDecision`, queue `Move W`);
W17b adds a static `Terminate 1.0`, so pass 2 ends `Decided` after one hop and
the tick ends `NoDecision` (the pass end's `E <= 0` check precedes the
`Terminate` check), with two decided passes and no cap hit. A pass ended by the
`Terminate` clause counts in `decided_passes`. The founder wires no `Decide`
vote: `cargo test -p v3-core --test viability` (28 passed) and the v3-core
suite pass with no pin moved. `MESH_EXECUTION_VERSION` is `mesh-execution-v2`;
`drift::EXECUTED_SOURCE` names it, while `drift::VERSION` stays
`drift-depth-v3`.

## Benchmark gate and goal (benchmark specialist)

### Gate

`make bench PROFILE=gate FEATURE=t19-f04-vote-based-action-selection`
(host `Isaacs-MacBook-Pro-2.local`, git revision `31d47ecc`). `v3-cli bench`
exit status 3 (`measurement_evidence.cli_exit`: "successful artifact-pair
completion" with `severe=true`); `make` propagated the same code
(`make: *** [bench] Error 3`). Raw report
`.bench-artifacts/t19-f04-vote-based-action-selection/gate.json`, 97,764
bytes, sha256 `d8f2482153d6fc10bafb106f1c7ffec3c5387b9919d0de763ccfe751b1c9c939`.
Summary `docs/progress/features/t19-f04-vote-based-action-selection.json`,
100,680 bytes. Compared against the pinned gate epoch
`t19-f02-live-internal-state-and-legal-cycles.json` only (`benchmark-series.json`
`gate.epoch_baseline`); T19.F03's gate summary was never added as a reference.

| Counter (per creature-tick) | Current | Reference (T19.F02) | Delta % | Level | Matches predeclaration? |
| --- | ---: | ---: | ---: | --- | --- |
| `mesh_hops` | 4.829716 | 2.040238 | +136.72 | severe | Yes (up, severe expected; ceiling is goal-only, ≤5× T19.F03's, not evaluated at gate) |
| `graph_relax_iters` | 4.757310 | 0.999831 | +375.81 | severe | Yes (up, severe expected) |
| `vm_steps` | 0.042477 | 22.532262 | -99.81 | ok | No — predeclared "severe expected"; the comparison tool does not flag large decreases as severe ("no floor") |
| `plasticity_updates` | 0.025565 | 0.006946 | +268.05 | severe | Yes ("no direction... a severe is reported to the user with the re-pin") |
| `actions_applied` | 1.352599 | 1.352060 | +0.04 | ok | Yes (no direction predeclared) |
| `births` | 0.026281 | 0.025954 | +1.26 | ok | Yes (up expected from carrying-cost fall; not severe here) |
| `pass_cap_hits` | 0.000752 | 0.000000 | n/a (ref 0) | severe | Tool-flagged severe on any nonzero change from a zero reference; the spec's own ceiling (≤10% of creature-ticks / passes) is met (0.075%). Reported, not remediated. |
| `passes` | 2.351203 | — | — | new | Yes (level `new`; ≤4 predeclared, satisfied) |
| `decided_passes` | 0.000000 | — | — | new | Yes (level `new`, no direction) |

Overall `comparison.severe = true` (`mesh_hops`, `graph_relax_iters`,
`plasticity_updates`, `pass_cap_hits` at `level: severe`). This matches the
predeclaration's expectation of a severe gate result pending the re-pin,
except `vm_steps` (predeclared severe, measured `ok`) and `pass_cap_hits`
(measured severe on a zero-reference artifact, but within its own stated
ceiling). Both exceptions are reported here, not resolved.

### Goal — converted from the existing raw report, stored

The original `make bench PROFILE=goal FEATURE=t19-f04-vote-based-action-selection`
run at `94ea0af9`: `v3-cli bench` wrote both artifacts (raw
`measurement_evidence.cli_exit` = `{code: 3, source: "v3-cli status on
successful artifact-pair completion; output errors instead exit 1"}`,
`severe=true`) but its own post-write read-back then failed with `error:
invalid full report: data did not match any variant of untagged enum
Indicator at line 16416992 column 7` and the outer process exited status 1
(`make: *** [bench] Error 1`, `MAKE_EXIT=2` from the wrapping shell); no
summary was produced at that time. The reader was fixed at `e1a6919d`
(`crates/v3-cli/src/bench/schema.rs`, `goal_reader_*` tests in
`crates/v3-cli/tests/bench_artifacts.rs`) without touching simulation or
measurement code, so the raw report already on disk still reflects the
`94ea0af9` measurement. Per `docs/benchmark-artifacts.md` ("Convert an
existing full report"), this benchmark run used the deterministic
`bench-summarize` conversion instead of repeating the goal run (the
goal-profile-runs-once rule): it never reruns simulation, assays, comparisons
or confidence intervals, only re-projects the stored bytes, so it is a valid
substitute for a second `make bench` invocation once the code identity gap is
limited to the reader.

Command: `target/release/v3-cli bench-summarize --input
.bench-artifacts/t19-f04-vote-based-action-selection/goal.json --out
<summary> --provenance <provenance>` (binary built at worktree HEAD
`e1a6919d`+docs commits, i.e. the current tip), exit status 0. Provenance
records `supplied_evidence.cli_exit=1`/`outer_exit=2` for the original failed
read-back and `inheritance`: raw bytes unchanged since `94ea0af9`, converter
run at the reader-fix revision.

Raw report: `.bench-artifacts/t19-f04-vote-based-action-selection/goal.json`,
663,778,358 bytes, sha256
`99083de1c92aee6dd73597a086320569de5ce32fd2289c03fc9e3401cb23eb08` (matches
the bytes written by the original run; `raw.availability: verified_local`).
Summary: `docs/progress/features/t19-f04-vote-based-action-selection-goal.json`,
7,688,756 bytes. Compared against the pinned goal-worlds epoch
`t19-f02-live-internal-state-and-legal-cycles-goal.json` (`benchmark-series.json`
`goal_worlds.epoch_baseline`); T19.F03 never produced its own goal summary
(0 counter/indicator differences recorded at its closure), so this is also
the correct T19.F03 comparison point. `comparison.severe = true`.

Wall caps (`measurement_evidence.wall_caps`): `evolved_seconds_per_world` 180,
`founder_seconds_per_world` 10. Measured
`neighborhood_evolved_wall_clock_ms_per_seed`: seed 11 1,905.708 ms, seed 22
2,003.501 ms, seed 33 1,160.092 ms — all ≤ 2.1 s, far inside the 180 s cap.
`neighborhood_founder_wall_clock_ms` (total, not per seed): 293.431 ms,
inside the 10 s cap. Host: `Isaacs-MacBook-Pro-2.local` (gate host also);
the epoch's host is `MacBookPro.lan`, so `comparison.references[0].wall_clock`
is `null` and the spec's "4× T19.F03's per world" cognition-wall ceiling is
not evaluated — cognition wall per creature-tick is reported as a bare
measurement (below), not gated.

Pooled counters (per creature-tick, all three worlds combined) against the
goal-worlds epoch:

| Counter | Current | Reference (T19.F02) | Delta % | Level | Matches predeclaration? |
| --- | ---: | ---: | ---: | --- | --- |
| `mesh_hops` | 8.934548 | 2.203591 | +305.45 | severe | Yes (up, severe expected) |
| `vm_steps` | 1.024805 | 22.999081 | -95.54 | ok | No — predeclared "severe expected", tool has no floor (same mismatch as gate) |
| `graph_relax_iters` | 7.637247 | 1.015213 | +652.28 | severe | Yes (up, severe expected) |
| `plasticity_updates` | 0.316498 | 0.086461 | +266.06 | severe | Yes (no direction predeclared; severe reported with the re-pin) |
| `actions_applied` | 2.548610 | 1.327288 | +92.02 | severe | Yes (no direction predeclared) |
| `births` | 0.017596 | 0.015632 | +12.56 | flag | Yes (no direction; flag not severe here) |
| `pass_cap_hits` | 0.001111 | 0.000020 | +5455.00 | severe | Tool-flagged severe on a near-zero reference; per-world ceiling check below |
| `passes` | 3.475969 | — | — | new | Ceiling breached in one world — see per-world table |
| `decided_passes` | 0.000092 | — | — | new | Yes (level `new`, no direction) |

Overall `comparison.severe = true` (`mesh_hops`, `graph_relax_iters`,
`plasticity_updates`, `actions_applied`, `pass_cap_hits` at `level: severe`,
pooled). This matches the gate's severe set plus `actions_applied`
(pooled-level severe; not flagged at gate scale), and the same `vm_steps`
mismatch as the gate (predeclared severe, measured `ok`, no floor).

Per-world reading (against the same epoch), the readings the predeclaration
table names directly:

| Reading | Orchards | Canyon | Confluence | Predeclaration check |
| --- | ---: | ---: | ---: | --- |
| `mesh_hops`/creature-tick (ref) | 10.487580 (2.141471) | 8.970517 (2.204542) | 7.597087 (2.246735) | Ceiling ≤5× ref: 10.7074 / 11.0227 / 11.2337 — all met (Orchards closest, 98%) |
| `vm_steps`/creature-tick | -95.18% | -93.79% | -97.04% | No floor; "ok" everywhere |
| `passes`/creature-tick | **4.106617** | 3.236254 | 3.104272 | Ceiling ≤4 — **Orchards breaches it** (predeclaration mismatch, escalated, not remediated here) |
| `decided_passes`/creature-tick | 0.000060 | 0.000170 | 0.000065 | Level `new`, no direction — met |
| `pass_cap_hits`/creature-tick | 0.000917 | 0.001306 | 0.001145 | Ceiling ≤10% of creature-ticks — met (all ≤0.13%) |
| `pass_cap_hits`/`passes` (capped share) | 0.000223 | 0.000404 | 0.000369 | Ceiling ≤10% of passes — met |
| `births`/creature-tick (ref) | 0.017953 (0.015218) | 0.016996 (0.015683) | 0.017699 (0.015892) | No direction; up in all three, consistent with the gate's carrying-cost story |
| Final population (ref) | **1,331 (5,111)** | 3,961 (3,349) | 5,464 (8,909) | "Below half of reference is investigated" — **Orchards is 1,331 < 2,555.5 (half of 5,111); triggers investigation, escalated, not investigated here** |
| Minimum population (ref) | 34 (34) | 791 (667) | 155 (268) | Reported, no direction |
| Plateau population (ref) | 3,032.874 (2,758.520) | 3,736.694 (3,195.540) | 5,014.708 (6,704.010) | Reported, no direction |
| Lineage Shannon entropy, nats (ref) | 0.657144 (0.699411) | 1.045617 (1.482583) | 1.172887 (1.746097) | Reported, no direction |
| Surviving founder clades (ref) | 2 (4) | 6 (7) | 7 (10) | Reported, no direction |
| `evolved_dead_per_mutated_births` (ref) | 0.000000 (0.011628) | 0.001279 (0.004042) | 0.000000 (0.014164) | Ceiling ≤2× ref: 0.023256 / 0.008084 / 0.028328 — all met |
| `mesh_ramp` energy flow (ref) | 10,885.16 (4.44) | 15,170.11 (11.14) | 8,456.93 (25.24) | Predeclared up "where passes push a creature past 32 hops" — matches (large rise everywhere) |
| `priority_bid` energy flow (ref) | 3,225.47 (47,733.15) | 6,249.54 (47,103.87) | 2,341.87 (60,077.58) | No direction predeclared; down sharply in all three |
| Cognition wall, ms/creature-tick | 0.0019828 (seed 11) | 0.0018363 (seed 22) | 0.0017513 (seed 33) | Ceiling not evaluated (host mismatch, `wall_clock: null`); reported only |
| `config_digest` (goal) | `sha256:d9a4dc8c...` unchanged | `sha256:d1aff3f5...` unchanged | `sha256:b24c1225...` unchanged | `inputs_changed: false` in all three cases, as predeclared |

**Two predeclaration mismatches found, escalated rather than remediated:**
(1) Orchards `passes`/creature-tick (4.106617) exceeds the predeclared ≤4
ceiling. (2) Orchards final population (1,331) is below half of the
goal-worlds epoch's Orchards final population (5,111 → half 2,555.5),
triggering the predeclared investigation clause. Both are reported here for
the orchestrator/spec owner to resolve; no remediation attempted by the
benchmark specialist.

Births probe (evolved half, `mutational_neighborhood.evolved.per_seed[0].pooled_births.any_events`),
new classes against the founder-half table above:

| Class | Orchards | Canyon | Confluence |
| --- | ---: | ---: | ---: |
| `trials` / `applied` | 1,370 / 1,370 | 1,564 / 1,564 | 1,006 / 1,006 |
| `changed` (`changed_fraction`) | 500 (0.364964) | 348 (0.222506) | 397 (0.394632) |
| `silent` (`silent_fraction`) | 870 (0.635036) | 1,214 (0.776215) | 609 (0.605368) |
| `dead` (`dead_fraction`) | 0 (0.000000) | 2 (0.001279) | 0 (0.000000) |
| `changed_only_in_sequences` | 13 | 8 | 10 |
| `mean_fraction_differing` | 0.385100 | 0.345321 | 0.327834 |
| `recount` | 21 | 16 | 12 |
| `reordered` | 1 | 3 | 2 |

All three worlds meet the predeclared ≤2× T19.F02/T19.F03 `evolved_dead_per_mutated_births`
ceiling (table above); `reordered`/`recount` reported, no direction predeclared.

Drift walk (`drift_depth.readings[].births.any_events`, depths 0/22/250/1,000/2,000),
against the goal-worlds epoch (Orchards and Confluence share a battery and
are identical; Canyon differs):

| Depth | Orchards/Confluence changed / silent / dead (current) | ref (T19.F02) | Canyon changed / silent / dead (current) | ref (T19.F02) |
| --- | --- | --- | --- | --- |
| 0 | 0.468208 / 0.531792 / 0.000000 | 0.475917 / 0.524083 / 0.000000 | 0.458092 / 0.541908 / 0.000000 | 0.464450 / 0.535550 / 0.000000 |
| 22 | 0.253633 / 0.735799 / 0.010568 | 0.261272 / 0.738728 / 0.000000 | 0.229465 / 0.767927 / 0.002608 | 0.249711 / 0.750289 / 0.000000 |
| 250 | 0.060879 / 0.936866 / 0.002255 | 0.047938 / 0.946488 / 0.005574 | 0.053512 / 0.943144 / 0.003344 | 0.031215 / 0.960981 / 0.007804 |
| 1,000 | 0.020737 / 0.979263 / 0.000000 | 0.008065 / 0.991935 / 0.000000 | 0.017281 / 0.982719 / 0.000000 | 0.019585 / 0.980415 / 0.000000 |
| 2,000 | 0.016185 / 0.983815 / 0.000000 | 0.000000 / 1.000000 / 0.000000 | 0.009249 / 0.990751 / 0.000000 | 0.001156 / 0.997688 / 0.001156 |

No floor predeclared (withdrawn 2026-09-14); reported for record, not gated.

T11.F14/steering evolved half (`mutational_neighborhood.evolved.per_seed[0].mesh_summary`):

| Field | Orchards | Canyon | Confluence |
| --- | ---: | ---: | ---: |
| `total` reachable-from-genome | 62 | 80 | 46 |
| `reachable` | 51 | 46 | 38 |
| `executed` | 39 | 43 | 36 |
| `execs` | 960 | 960 | 960 |
| `genomes` | 12 | 12 | 12 |
| `knockout` | 11 | 16 | 10 |
| `passes` (battery) | 6,091 | 8,021 | 4,408 |
| `capHits` | 0 | 0 | 0 |
| `routeVaries` (steering) | 5 | 2 | 1 |
| `routeDestinationVaries` (steering) | 4 | 2 | 1 |

Reported, no direction predeclared; the survey's contributing-node median (2)
and route-variation share (3.3%) referenced by the predeclaration are read
from the survey note, not recomputed here (T11.F10 owns that hypothesis).

Goal persistence/lineage/recruitment/memory (no direction predeclared,
reported):

| Reading | Orchards | Canyon | Confluence |
| --- | ---: | ---: | ---: |
| `memory_different_from_either_fraction` | 0.000000 | 0.000000 | 0.000000 |
| `temporal_memory_persisted_outputs_different_from_either_fraction` | 0.008264 | 0.004039 | 0.006040 |
| `temporal_memory_operator_state_different_from_either_fraction` | 0.444027 | 0.000757 | 0.000549 |
| `founder_changed_per_all_births` | 0.496403 | 0.489209 | 0.496403 |
| `neighborhood_read_changed_per_all_births` | 0.209200 | 0.150400 | 0.156000 |

Recruitment paths (`recruitment_paths`, S0 fixture reproduced in-line by the
goal profile, not the separate `v3-cli recruitment` S0 harness): 27 arms,
`total_proposals` 82,944, `opportunities.attempted` 46,089,
`opportunities.applied` 41,322 across four domains (`Graph` 8,280, `InputRef`
12,147, `Topology` 9,318, `Vm` 11,577). Not compared to a reference (first
time this shape has appeared in a goal report at this scale); reported for
record.

`mutation_supply` operator mix: case-level field records `"legacy"` (the
pre-T11.F19 per-unit rate identifier persisted from the recruitment fixture
context, not a live supply reading in this report); reported as-is, no
further breakdown available in the summary.

Energy flows and mortality, `mesh_ramp`/`priority_bid` (already tabulated
above); `mortality.deaths_total`: Orchards 260,156 (ref 184,329), Canyon
195,810 (ref 170,752), Confluence 297,743 (ref 267,240) — up in all three, no
direction predeclared, consistent with the higher `births` and larger
population swings.

### Orchards seed 12 to 2,000 ticks (`v3 run`, independent of the goal profile)

`./scripts/bench-wait target/release/v3-cli run --ticks 2000 --seed 12
--config experiments/worlds/orchards-in-grassland.json`, exit status 0.
Config digest `sha256:d9a4dc8c530aa1c110c34ef40debd8d6c63bc662b4f1fa3ac026689b19260bd7`.

| Reading | Value |
| --- | --- |
| Final population (tick 2,000) | 4,828 (`run_completed.final_population`) |
| Minimum population | 34, at tick 308 |
| Tick reasons | Not measured — `v3 run`'s `tick_sample` events carry population/energy/genome/mutation counters only, no termination-reason breakdown; the goal report's per-seed fields carry `extinction_tick` only (null for all three T19.F04 goal worlds — no extinction before tick 2,000), not a full reason enum |

Beside seed 11's own T19.F04 goal-worlds Orchards run (this closure, final
population 1,331, minimum 34) and the T19.F02 pin (final population 5,111):
seed 12's final population (4,828) sits between the two and its minimum (34)
matches seed 11's T19.F04 minimum exactly, consistent with "seed-dependent"
small/medium-population trajectories already on record
(`petri-baseline-collapse-2026-09.md`). No direction is predeclared for this
reading; reported as measured, not gated.

### Run-half readings — now obtained from the converted goal summary

All items below were "not measured" pending the goal report; the conversion
in "Goal — converted from the existing raw report, stored" above fills them.

| Reading | Status |
| --- | --- |
| Births probe (evolved half) | Measured — table above ("Births probe (evolved half)") |
| Drift walk (T19.F03 comparison) | Measured — table above ("Drift walk") |
| T11.F14 evolved half (tick reasons, passes/`decided_passes` totals) | Measured — `mesh_summary` table above; tick reasons remain unavailable beyond `extinction_tick` (null in all three worlds) |
| Steering evolved half | Measured — `routeVaries`/`routeDestinationVaries` columns in the `mesh_summary` table above |
| Passes and `Decided` against capped passes (goal scale, per world) | Measured — per-world table above (`passes`, `decided_passes`, `pass_cap_hits` rows); Orchards breaches the ≤4 passes/creature-tick ceiling (flagged, escalated) |
| Cognition wall per creature-tick (goal, per world, host-gated ceiling) | Measured — per-seed values 0.0017513–0.0019828 ms above; ceiling not evaluated (host `Isaacs-MacBook-Pro-2.local` ≠ epoch host `MacBookPro.lan`, `wall_clock: null`) |
| Goal persistence/lineage/recruitment/memory | Measured — tables above; Orchards final population 1,331 is below half the T19.F02 reference (5,111), triggering the predeclared investigation clause (flagged, escalated) |
| `mutation_supply` operator mix | Measured — `"legacy"` identifier only, no operator-level breakdown in this report shape (reported above) |
| Energy flows and mortality (`mesh_ramp`, `priority_bid`, `deaths_total`) | Measured — table above |
| `config_digest` goal check | Measured — `inputs_changed: false` in all three cases (table above) |

## Verification commands (after the guard change and the simplify pass)

| Command | Result |
| --- | --- |
| `cargo test -p v3-core --test viability` | 28 passed |
| `cargo fmt --all --check` | clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | clean |
| `cargo test --workspace --no-fail-fast` | 2,093 passed, 1 failed: `gate_profile_has_no_severe_regression_against_series_references` (gate `mesh_hops` 4.829716 against 2.040238, +136.7%, the predeclared severe before the re-pin) |
| `npx vitest run` (frontend) | 62 files, 327 passed |
| `make roadmap-check` | passed |

## Orchards investigation (spec owner, from stored summaries)

The predeclared clause "a world whose final population falls below half of
T19.F03's is investigated before the result is presented" fired on Orchards
seed 11 (1,331 against 5,111). Read from the committed goal summaries'
`population_persistence.per_seed[0].samples` (no new run).

| Tick | 200 | 300 | 400 | 500 | 600 | 700 | 800 | 900 | 1000 | 1100 | 1200 | 1300 | 1400 | 1500 | 1600 | 1700 | 1800 | 1900 | 2000 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| T19.F04 | 2,070 | 34 | 167 | 1,181 | 2,129 | 1,567 | 1,363 | 1,989 | 2,176 | 4,101 | 8,354 | 8,221 | 1,895 | 1,621 | 1,487 | 3,593 | 5,419 | 3,087 | 1,331 |
| T19.F03 | 2,705 | 38 | 206 | 454 | 1,157 | 1,467 | 1,241 | 764 | 1,120 | 1,080 | 839 | 1,406 | 1,760 | 2,049 | 1,938 | 2,242 | 2,563 | 3,581 | 5,111 |

| Indicator | T19.F04 | T19.F03 |
| --- | ---: | ---: |
| Minimum population (tick) | 34 (300) | 34 (300) |
| Plateau population | 3,032.874 | 2,758.520 |
| Final population | 1,331 | 5,111 |
| Orchards seed 12 (`v3 run`, 2,000 ticks) | final 4,828, min 34 at 308 | not run |

Finding: no extinction and no sterile decline; the population recovers from
the same trough to 8,354 and cycles (boom-bust) where T19.F03's recovery was
monotone, and the plateau indicator is higher. The final snapshot is a trough
of that cycle. Recorded with no predeclared direction; the mechanism behind
the stronger cycle (a 13% lower founder carrying cost, more actions per tick)
is not attributed here.

## Mutation gate (mutation specialist)

Aborted attempt (not a gate run): the first fresh `MUTANTS_ITERATE=0` run
stopped at the unmutated baseline, zero mutants tested, because
`gate_profile_has_no_severe_regression_against_series_references` was red
against the T19.F02 epoch; the user accepted the severes and both epochs were
re-pinned at `bf2ca325` before this run.

Fresh run (the only one that tested mutants), at `bf2ca325`, diffed against
merge base `e5eeacf0`: `454 mutants tested in 75m: 26 missed, 314 caught, 114
unviable`, 0 timeouts. Output
`~/.local/share/petri-tools/mutants/t19-f04/mutants.out` (moved to
`mutants.out.old` by the incremental pass; preserved copy with the wrapper log
in `mutants.out.fresh`). One `MUTANTS_ITERATE=1` feedback pass after the test
additions: `26 mutants tested in 8m: 2 missed, 24 caught` (the two
equivalents). No second fresh run: triage added tests only (one new
`#[cfg(test)]` module in `qualification.rs`); no production code, test
selection, or tool configuration changed.

| Survivor(s) | Resolution |
| --- | --- |
| `bench/schema.rs:232` `Indicator` visitor `expecting` → `Ok(Default)` | Killed: `indicator_rejects_a_non_string_non_object_value_naming_both_accepted_shapes` |
| `neighborhood/classify.rs:49` `==` → `!=` in `execution_shape`'s `count` | Equivalent: `count` is only called after `base.len() == candidate.len()`, so counting non-matches (`len - matches`) compares exactly as counting matches |
| `neighborhood/mesh_execution.rs:75`–`77` `TickReasonCounts::add` (5: `*=`, `-=`) and `:83` `total` (7: `0`, `-`, `*`) | Killed: `tick_reason_counts_add_sums_each_reason` |
| `runtime/action_decode.rs:36` `sink_direction` `- 1` → `/ 1`, `+ 1` | Killed: `out_of_range_sink_direction_clamps_to_the_last_direction` |
| `runtime/vote_select.rs:57` `bars * VOTE_UNIT` → `/` | Equivalent: `VOTE_UNIT` is the constant `1.0`, and `x * 1.0 == x / 1.0` exactly for every `f32` |
| `simulation/tick.rs:608`–`609` `record` and `:655`–`656` `commit` `passes`/`decided_passes` (6: `*=`, `-=`) | Killed: `tick_compute_passes_and_decided_passes_accumulate_across_creatures_and_ticks` |
| `creature/genome/mesh_annotations.rs:105` delete `WriteInternalPayload` arm | Killed: `classifies_write_internal_payload_as_a_payload_write` |
| `recruitment_paths/qualification.rs:511` `edge.weight > 0.0` → `>=` | Killed: `east_vote_edge_added_rejects_a_zero_weight_edge` |
| `recruitment_paths/qualification.rs:593` `&&` → `\|\|` in `north_vote_removed` | Killed: `north_vote_removed_rejects_a_changed_compute_node` |

Focused checks: the seven new tests pass (`cargo test -p v3-core --lib` and
`cargo test -p v3-cli --lib`, filtered by name), `cargo fmt --all --check`
clean, `make rust-clippy` clean.
