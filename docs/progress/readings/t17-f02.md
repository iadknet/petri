# T17.F02 — Unit-Scale Introspection readings

Tables and transcripts for the feature spec
[`docs/specs/roadmap/t17-f02-unit-scale-introspection.md`](../../specs/roadmap/t17-f02-unit-scale-introspection.md).

## Founder-only digest pin (invariant 6)

`crates/v3-core/tests/viability.rs::founder_only_trajectory_digest_is_pinned`:
the viability scenario (32×32, 10 V3Alpha1 founders, full coverage, seed
2026, 2,000 ticks, `mutation_probability` 0, `per_unit_supply_enabled`
false). SHA-256 over every creature's slot id, position, energy bits, age and
generation after every tick, then the primary food grid after tick 2,000;
births and final population asserted beside it.

| Measured on | Births | Final population | Digest |
| --- | ---: | ---: | --- |
| pre-feature code (b6bd2a87 + the pin) | 177 | 0 | `5ad9e8e1484792ab566c8ecac7466fffac3bb2b92bb4dd72e9ce7cb49ade36a9` |
| T17.F02 code | 177 | 0 | `5ad9e8e1484792ab566c8ecac7466fffac3bb2b92bb4dd72e9ce7cb49ade36a9` |

The founder-only world on this small grid is extinct by tick 2,000; the
per-tick sampling makes the digest cover the whole 177-birth trajectory, not
only the empty end state.

## Gate scans and property tests (invariants 6 and 8)

Founder, `crates/v3-core/src/creature/founder.rs` (`tests`):

| Test | Claim |
| --- | --- |
| `founder_energy_gates_scan_identically_on_the_unit_scale` | for each profile, `unit == raw / 200` bit-exact, and over every f32 energy in [raw − 1, raw + 1] the mismatch set of `e > raw` vs `e / 200 > unit` is empty for V3Alpha1, ForageFirstSparse, RichOffspring and Balanced, and exactly `[60.000004]` for Conservative |
| `founder_age_gate_is_exact_at_every_integer_age` | for reference spans 500, 1000, 1024, 2000, 2048, 4096 and `min_reproduce_age` 0, 1, 19, 20, 25, 100: `age_fraction(age, S) > age_gate_threshold(min_age, S)` equals `age >= min_age` at every integer age in [0, S + 10] |
| `founder_reproduce_policy_matches_the_profile_table` | policy thresholds 0.16/0.16/0.30/0.20/0.25 |
| `founder_profiles_execute_energy_age_boundaries_and_priorities` | mesh at raw `threshold` (no attempt) and `threshold + 1` (attempt) for every profile, energies from `unit × 200` |

Sensors, `crates/v3-core/src/sensors/static_inputs.rs` (`tests`):

| Test | Claim |
| --- | --- |
| `energy_fraction_is_bounded_and_monotone` (proptest) | for `lo` in [−1e6, 1e6], `step` in [0, 1e6], `max_energy` in [1, 1e6]: both reads in [0, 1] and `f(lo) <= f(lo + step)` |
| `age_fraction_is_bounded_and_monotone` (proptest) | for `age`, `step` in [0, 1e6), `reference` in [1, 1e6): both reads in [0, 1] and non-decreasing |
| `energy_fraction_clamps_negative_and_overfull_reads` | −5 → 0, 0 → 0, 200 → 1, 260 → 1, 32 → 0.16 |
| `age_fraction_saturates_at_the_reference_span` | 0 → 0, 500 → 1, 10,000 → 1, 20 → 0.04 |
| `static_introspection_age_is_a_fraction_of_the_reference_span` | age 42 on `age_reference_ticks` 400 reads 0.105; `max_energy` 150 rides on the snapshot |

No `proptest-regressions/` file appeared for these.
`crates/v3-core/proptest-regressions/simulation/actions/mod.txt` holds the
red run of `founder_profiles_are_accepted_at_every_energy_above_their_gate`
(`profile = V3Alpha1, unit = 0.0, age = 20`) from before that proptest
multiplied the unit threshold back by `max_energy`; the case passes now.

Resolver, `crates/v3-core/src/runtime/inputs.rs` (`tests`):
`dynamic_energy_current_is_a_fraction_of_max_energy` (42.5 → 0.2125, −3 → 0,
250 → 1, 200 → 1) and
`dynamic_energy_consumed_this_tick_is_a_fraction_of_max_energy` (5.5 →
0.0275, 0 → 0, 400 → 1). VM mid-dispatch semantics:
`runtime/tests/vm_step_ramp.rs::{a_mid_dispatch_energy_read_sees_the_effective_energy,
a_mid_dispatch_consumption_read_includes_what_the_dispatch_owes}` read
`effective / 200` and `(consumed + debt) / 200`.

Config, `crates/v3-core/src/config/simulation.rs` (`tests`):
`normalize_zero_age_reference_ticks_falls_back_to_default`,
`normalize_age_reference_ticks_keeps_configured_value`,
`energy_lifecycle_serde_defaults_min_reproduce_age_when_missing` (also
asserts the 500 default). Seeding:
`simulation::seeding::tests::seeding_threads_min_reproduce_age_and_reference_span_into_founder_gate`
(`min_reproduce_age` 25 on span 1,000 → `Threshold(0.0245)`).

## Pins that moved (invariant 9)

Every mutation-on trajectory moves from the first birth that draws an
introspective reference or parameter (the catalog lost `Generation`, so every
input-reference draw shifts). Each digest was measured after two runs agreed.

| Test | Kind | Old | New |
| --- | --- | --- | --- |
| `tests/applied_trajectory.rs` digest | evolved trajectory (mutation probability 1.0, seeds 7 and 19, 64 ticks) | `112d9b4e0106fece34f47a87246bf658f838b56376ddad58e42530bda393266f` | `1d1878c9c15b5f1527e21a10af92db75faf079630e3246d21baf4895997a8709` |
| `tests/baseline_worlds.rs::legacy_default_short_run_identity` | default-mutation trajectory (48×48, 100 founders, seed 11, 30 ticks) | `17387818010249764947` | `17519359726252727600` |
| `neighborhood::recruitment_paths::experiment::tests::production_prepared_lineages_match_the_recorded_baseline_and_metadata` | prepared-lineage counts | graph drift `[21, 13, 11, 7]`, VM drift `[21, 14, 12, 8]` | graph drift `[21, 13, 11, 6]`, VM drift `[21, 14, 12, 7]`; both selection rows and all discard counts unchanged |
| `v3-cli/tests/bench_artifacts.rs::checked_in_goal_recipe_identities_are_unchanged_by_json_precision` | resolved-config digests (the resolved config now serializes `age_reference_ticks`) | `81450ac8…`, `c8d2ab94…`, `a8b84ed7…` | `b065b220…`, `bd3fa787…`, `289a95bd…` (Orchards, Canyon, Confluence) |

Fixtures re-expressed on the unit scale (no pinned value moved, the
expectation was rescaled by 1/200 or 1/500): `runtime/plasticity/tests.rs::activity_uses_evaluation_energy_before_pure_hebbian_cost`
(8 → 8/200), `runtime/traced_mesh.rs::traced_and_untraced_meshes_receive_live_energy`
(100 → 0.5), `tests/creature_workflow_e2e/{flow_and_vm,graph_inputs}.rs`
(`Generation` ref removed and indices shifted; `AgeTicks` 3 → 3/500 and 4 →
4/500), `tests/temporal_fixtures.rs` B2/C1/C2 genomes (age targets 1 and 2
and the half-tick tolerance divided by `age_reference_ticks`),
`neighborhood/battery.rs` (`AGE_CHOICES` carried as `age / 500`; the founder
gate decision at each age is unchanged), `mutation::input_ref::tests::swap_alternatives_follow_the_kind_table`
(introspection kind has 3 members, 2 alternatives), `mutation/sampling.rs::typed_sampler_retains_both_energy_introspection_keys`
(catalog indices 5 and 6 of 22). Frontend: the `Generation` input-reference
fixtures in `inputRefUtils.test.ts`, `useMeshDerivation.test.ts` and
`meshAnalysis.test.ts` name `AgeTicks`; the trace `static_inputs.generation`
field left `StaticInputsSnapshotPayload`, `types/trace.ts` and five sampler
fixtures.
