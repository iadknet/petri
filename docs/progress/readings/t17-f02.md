# T17.F02 — Unit-Scale Introspection readings

Tables and transcripts for the feature spec
[`docs/specs/roadmap/t17-f02-unit-scale-introspection.md`](../../specs/roadmap/t17-f02-unit-scale-introspection.md).

## Founder-only digest pin (invariant 6)

`crates/v3-core/tests/viability.rs::founder_only_trajectory_digest_is_pinned`:
the viability scenario (32×32, 10 V3Alpha1 founders, full coverage, seed
2026, 2,000 ticks, `mutation_probability` 0, `per_unit_supply_enabled`
false). SHA-256 over every creature's slot id, position, energy bits, age and
generation after every tick, then the primary food grid after tick 2,000;
births and final population asserted beside it.

| Measured on | Births | Final population | Digest |
| --- | ---: | ---: | --- |
| pre-feature code (b6bd2a87 + the pin) | 177 | 0 | `5ad9e8e1484792ab566c8ecac7466fffac3bb2b92bb4dd72e9ce7cb49ade36a9` |
| T17.F02 code | 177 | 0 | `5ad9e8e1484792ab566c8ecac7466fffac3bb2b92bb4dd72e9ce7cb49ade36a9` |

The founder-only world on this small grid is extinct by tick 2,000; the
per-tick sampling makes the digest cover the whole 177-birth trajectory, not
only the empty end state.

## Gate scans and property tests (invariants 6 and 8)

Founder, `crates/v3-core/src/creature/founder.rs` (`tests`):

| Test | Claim |
| --- | --- |
| `founder_energy_gates_scan_identically_on_the_unit_scale` | for each profile, `unit == raw / 200` bit-exact, and over every f32 energy in [raw − 1, raw + 1] the mismatch set of `e > raw` vs `e / 200 > unit` is empty for V3Alpha1, ForageFirstSparse, RichOffspring and Balanced, and exactly `[60.000004]` for Conservative |
| `founder_age_gate_is_exact_at_every_integer_age` | for reference spans 500, 1000, 1024, 2000, 2048, 4096 and `min_reproduce_age` 0, 1, 19, 20, 25, 100: `age_fraction(age, S) > age_gate_threshold(min_age, S)` equals `age >= min_age` at every integer age in [0, S + 10] |
| `founder_reproduce_policy_matches_the_profile_table` | policy thresholds 0.16/0.16/0.30/0.20/0.25 |
| `founder_profiles_execute_energy_age_boundaries_and_priorities` | mesh at raw `threshold` (no attempt) and `threshold + 1` (attempt) for every profile, energies from `unit × 200` |

Sensors, `crates/v3-core/src/sensors/static_inputs.rs` (`tests`):

| Test | Claim |
| --- | --- |
| `energy_fraction_is_bounded_and_monotone` (proptest) | for `lo` in [−1e6, 1e6], `step` in [0, 1e6], `max_energy` in [1, 1e6]: both reads in [0, 1] and `f(lo) <= f(lo + step)` |
| `age_fraction_is_bounded_and_monotone` (proptest) | for `age`, `step` in [0, 1e6), `reference` in [1, 1e6): both reads in [0, 1] and non-decreasing |
| `energy_fraction_clamps_negative_and_overfull_reads` | −5 → 0, 0 → 0, 200 → 1, 260 → 1, 32 → 0.16 |
| `age_fraction_saturates_at_the_reference_span` | 0 → 0, 500 → 1, 10,000 → 1, 20 → 0.04 |
| `static_introspection_age_is_a_fraction_of_the_reference_span` | age 42 on `age_reference_ticks` 400 reads 0.105; `max_energy` 150 rides on the snapshot |

No `proptest-regressions/` file appeared for these.
`crates/v3-core/proptest-regressions/simulation/actions/mod.txt` holds the
red run of `founder_profiles_are_accepted_at_every_energy_above_their_gate`
(`profile = V3Alpha1, unit = 0.0, age = 20`) from before that proptest
multiplied the unit threshold back by `max_energy`; the case passes now.

Resolver, `crates/v3-core/src/runtime/inputs.rs` (`tests`):
`dynamic_energy_current_is_a_fraction_of_max_energy` (42.5 → 0.2125, −3 → 0,
250 → 1, 200 → 1) and
`dynamic_energy_consumed_this_tick_is_a_fraction_of_max_energy` (5.5 →
0.0275, 0 → 0, 400 → 1). VM mid-dispatch semantics:
`runtime/tests/vm_step_ramp.rs::{a_mid_dispatch_energy_read_sees_the_effective_energy,
a_mid_dispatch_consumption_read_includes_what_the_dispatch_owes}` read
`effective / 200` and `(consumed + debt) / 200`.

Config, `crates/v3-core/src/config/simulation.rs` (`tests`):
`normalize_zero_age_reference_ticks_falls_back_to_default`,
`normalize_age_reference_ticks_keeps_configured_value`,
`energy_lifecycle_serde_defaults_min_reproduce_age_when_missing` (also
asserts the 500 default). Seeding:
`simulation::seeding::tests::seeding_threads_min_reproduce_age_and_reference_span_into_founder_gate`
(`min_reproduce_age` 25 on span 1,000 → `Threshold(0.0245)`).

## Pins that moved (invariant 9)

Every mutation-on trajectory moves from the first birth that draws an
introspective reference or parameter (the catalog lost `Generation`, so every
input-reference draw shifts). Each digest was measured after two runs agreed.

| Test | Kind | Old | New |
| --- | --- | --- | --- |
| `tests/applied_trajectory.rs` digest | evolved trajectory (mutation probability 1.0, seeds 7 and 19, 64 ticks) | `112d9b4e0106fece34f47a87246bf658f838b56376ddad58e42530bda393266f` | `1d1878c9c15b5f1527e21a10af92db75faf079630e3246d21baf4895997a8709` |
| `tests/baseline_worlds.rs::legacy_default_short_run_identity` | default-mutation trajectory (48×48, 100 founders, seed 11, 30 ticks) | `17387818010249764947` | `17519359726252727600` |
| `neighborhood::recruitment_paths::experiment::tests::production_prepared_lineages_match_the_recorded_baseline_and_metadata` | prepared-lineage counts | graph drift `[21, 13, 11, 7]`, VM drift `[21, 14, 12, 8]` | graph drift `[21, 13, 11, 6]`, VM drift `[21, 14, 12, 7]`; both selection rows and all discard counts unchanged |
| `v3-cli/tests/bench_artifacts.rs::checked_in_goal_recipe_identities_are_unchanged_by_json_precision` | resolved-config digests (the resolved config now serializes `age_reference_ticks`) | `81450ac8…`, `c8d2ab94…`, `a8b84ed7…` | `b065b220…`, `bd3fa787…`, `289a95bd…` (Orchards, Canyon, Confluence) |

Fixtures re-expressed on the unit scale (no pinned value moved, the
expectation was rescaled by 1/200 or 1/500): `runtime/plasticity/tests.rs::activity_uses_evaluation_energy_before_pure_hebbian_cost`
(8 → 8/200), `runtime/traced_mesh.rs::traced_and_untraced_meshes_receive_live_energy`
(100 → 0.5), `tests/creature_workflow_e2e/{flow_and_vm,graph_inputs}.rs`
(`Generation` ref removed and indices shifted; `AgeTicks` 3 → 3/500 and 4 →
4/500), `tests/temporal_fixtures.rs` B2/C1/C2 genomes (age targets 1 and 2
and the half-tick tolerance divided by `age_reference_ticks`),
`neighborhood/battery.rs` (`AGE_CHOICES` carried as `age / 500`; the founder
gate decision at each age is unchanged), `mutation::input_ref::tests::swap_alternatives_follow_the_kind_table`
(introspection kind has 3 members, 2 alternatives), `mutation/sampling.rs::typed_sampler_retains_both_energy_introspection_keys`
(catalog indices 5 and 6 of 22). Frontend: the `Generation` input-reference
fixtures in `inputRefUtils.test.ts`, `useMeshDerivation.test.ts` and
`meshAnalysis.test.ts` name `AgeTicks`; the trace `static_inputs.generation`
field left `StaticInputsSnapshotPayload`, `types/trace.ts` and five sampler
fixtures.

## Benchmark (roadmap-benchmark-specialist, 2026-09-18)

Commands, sequentially from the worktree at `ae5eef80` (final feature
code), nothing else running (`pgrep` before the first run showed no
`v3-*`/`cargo`/`cargo-mutants` processes; `scripts/bench-wait` printed no
blocking-PID lines in either log); logs in the session scratchpad
(`gate.log`, `goal.log`), each ending on an explicit `outer exit: <status>`
line written by the calling shell.

1. `make bench PROFILE=gate FEATURE=t17-f02-unit-scale-introspection` — CLI
   arguments `bench --profile gate --feature t17-f02-unit-scale-introspection`;
   `make` exit 0 (observed, `outer exit: 0`); `measurement_evidence.cli_exit`
   `{"code":0,"source":"v3-cli status on successful artifact-pair
   completion; output errors instead exit 1"}`; `outer_exit` null; `dirty`
   false. `comparison.severe` false against both references; every counter
   and both wall readings `ok`.
2. `make bench PROFILE=goal FEATURE=t17-f02-unit-scale-introspection` — CLI
   arguments `bench --profile goal --feature t17-f02-unit-scale-introspection`;
   `make` exit **2** (observed, `outer exit: 2`, after `make: *** [bench]
   Error 3` and the CLI line `error: severe work-counter regression against
   a stored reference`); `measurement_evidence.cli_exit` `{"code":3,...}`
   (same source string; 3 is the documented completed-severe-run status);
   `outer_exit` null; `dirty` **true** (worktree state at run time: tracked
   files clean, the step-1 gate summary present as an untracked file). One
   comparison printed because the goal-worlds-v1 epoch and latest closure
   are the same file (`t11-f23-scale-relative-vm-constant-steps-goal.json`).
   `comparison.severe` **true**: `plasticity_updates` +78.944503% (level
   `severe`, above the 50% severe threshold); the other five counters `ok`;
   wall `ok` (−3.777692%). Both artifacts were written. Run once; the
   second determinism run is closed by the 2026-09-05 user decision (no
   Verification item lists it for this feature).

Reports and sizes:

| Profile | Summary path | Summary bytes | Summary sha256 | Raw path | Raw bytes | Raw sha256 |
| --- | --- | --- | --- | --- | --- | --- |
| gate | `docs/progress/features/t17-f02-unit-scale-introspection.json` | 97,641 | `2f06c62c8aaa3ba73824057ad2f6d6fbc43a2dd1dcbe2c685cc6ecc4556648ea` | `.bench-artifacts/t17-f02-unit-scale-introspection/gate.json` (main checkout, local, not staged) | 94,822 | `d6e867c02328fd939b1aa7a57e76040776b780112d30abfc96aa0103aaf816e1` |
| goal | `docs/progress/features/t17-f02-unit-scale-introspection-goal.json` | 6,873,055 | `5fc69539a2b17454f801d09f50786b8e39efaff7e8fb54f68021d87f71ae86ef` | `.bench-artifacts/t17-f02-unit-scale-introspection/goal.json` (main checkout, local, not staged) | 534,284,606 | `f6dcd19f220c6e9b46ca45ad36daf7efe470a7ecde8a2c320440f4018d32079f` |

Raw `availability` `verified_local` in both summaries (`verified_at`
2026-09-19T06:32:23Z gate, 2026-09-19T06:41:40Z goal; `generated_at`
2026-09-19T06:32:23Z / 2026-09-19T06:41:38Z, `git_revision`
`ae5eef809af00ef41d4dd0d9d7be8ce6b6e99cf4`, host Apple M1 Pro, 8 threads);
raw sha256 and byte counts re-checked with `shasum -a 256` and `ls -l`
after the runs and match the summaries' `raw` blocks. Thresholds recorded
in `measurement_evidence`: work flag 10% / severe 50%, wall flag 25% /
severe 100%, wall not fatal; caps founder 10 s per world, evolved 180 s,
drift 30 s per world, recruitment 120 s per report, goal investigation
900 s.

Gate work-counter deltas (per creature-tick; `severe=false`, every level
`ok`):

| Counter | Current | vs `remove-complementary-nutrition.json` (epoch) | vs `t11-f23-scale-relative-vm-constant-steps.json` (latest closure) |
| --- | --- | --- | --- |
| mesh_hops | 2.030869 | +0.177974% | +0.143296% |
| vm_steps | 22.461237 | −1.274999% | −0.148449% |
| graph_relax_iters | 1.000081 | +0.487021% | +0.036610% |
| plasticity_updates | 0.006812 | −39.020679% | −18.536235% |
| actions_applied | 1.350924 | +5.911213% | +0.111900% |
| births | 0.025998 | −2.920090% | −0.168958% |

Gate wall: 0.001831 ms per creature-tick; vs epoch +17.401067% `ok`; vs
latest closure +19.273047% `ok` (both below the 25% flag).
`wall_clock_ms_total` 498.9 ms; `neighborhood_founder_wall_clock_ms`
59.393 ms against the 10,000 ms cap. Gate per seed (before = the T11.F23
gate summary; 75 ticks each; no `extinction_tick`):

| Seed | births (T17.F02) | births (before) | final_population (T17.F02) | final_population (before) | creature_ticks (T17.F02) | creature_ticks (before) |
| --- | --- | --- | --- | --- | --- | --- |
| 11 | 2,352 | 2,360 | 2,109 | 2,122 | 90,778 | 90,566 |
| 22 | 2,349 | 2,395 | 2,117 | 2,135 | 90,261 | 91,207 |
| 33 | 2,382 | 2,361 | 2,162 | 2,107 | 91,402 | 91,482 |

Gate founder T11.F14 rows
(`deterministic.goal_indicators.mutational_neighborhood.founder.births.any_events`,
210 applied of 500 births): changed 87 (0.414286), silent 123 (0.585714),
dead 0 (0.000000); before (T11.F23 gate summary) changed 83 (0.395238),
silent 127 (0.604762), dead 0 (0.000000). Gate
`operator_rows[VmConstantMutation]` changed 6/50 (unchanged from T11.F23).

Goal work-counter deltas (per creature-tick; epoch and latest closure are
both `t11-f23-scale-relative-vm-constant-steps-goal.json`):

| Counter | Current | Reference | Delta | Level |
| --- | --- | --- | --- | --- |
| mesh_hops | 2.220186 | 2.210847 | +0.422417% | ok |
| vm_steps | 23.114328 | 23.093048 | +0.092149% | ok |
| graph_relax_iters | 1.041916 | 1.035677 | +0.602408% | ok |
| plasticity_updates | 0.088735 | 0.049588 | **+78.944503%** | **severe** |
| actions_applied | 1.358706 | 1.337863 | +1.557932% | ok |
| births | 0.015684 | 0.016219 | −3.298600% | ok |

Goal `plasticity_updates` totals 3,531,132 over 39,794,254 creature-ticks
(before 1,802,010 over 36,339,553). Per world
`plasticity_updates_per_creature_tick`: Orchards 0.101430 (before
0.030519), Canyon 0.099151 (0.027569), Confluence 0.070998 (0.082386).

Goal wall: 0.012671 ms per creature-tick vs 0.013168, −3.777692%, `ok`.
Goal wall budgets (closure checks): `wall_clock_ms_total` 504,217.8 ms
(seeds 11/22/33: 192,858.1 / 119,532.3 / 191,827.4);
`neighborhood_evolved_wall_clock_ms_total` 1,503.298 ms (478.933 /
609.885 / 414.481 per seed) against the 180,000 ms summed cap;
`neighborhood_founder_wall_clock_ms` 224.504 ms against the 10,000 ms cap;
`neighborhood_read_wall_clock_ms_total` 374.762; `drift_depth_wall_clock_ms`
27,118.9; `recruitment_paths_wall_clock_ms` 10,968.0. All within cap. No
`extinction_tick` in any world (null in `per_seed` and every world's
`case_readings`). Recruitment `total_proposals` 82,944 (before 82,944).
Every goal case carries `inputs_changed: true` against the reference
(resolved-config digests moved because `age_reference_ticks` now
serializes; the new digests are in the "Pins that moved" table above).

Goal per seed (`deterministic.per_seed`, 2,000 ticks; before = T11.F23
goal summary): seed 11 births 197,547 (189,888), final_population 7,055
(7,208), creature_ticks 12,857,652 (11,872,179); seed 22 births 175,258
(168,585), final_population 3,741 (5,683), creature_ticks 11,172,435
(10,509,345); seed 33 births 251,335 (230,909), final_population 12,011
(4,648), creature_ticks 15,764,167 (13,958,029).

Goal per world, T11.F23 goal summary beside this run
(`comparison_inputs.case_readings`):

| World | Indicator | T11.F23 | T17.F02 |
| --- | --- | --- | --- |
| Orchards in grassland | `final_population` | 7,208 | 7,055 |
| Orchards in grassland | `minimum_population` | 99 | 149 |
| Orchards in grassland | `peak_population` | 100,000 | 100,000 |
| Orchards in grassland | `plateau_population` | 3,536.944 | 4,318.060 |
| Orchards in grassland | `births` | 189,888 | 197,547 |
| Orchards in grassland | `mean_energy` | 26.049613 | 41.698212 |
| Orchards in grassland | `extinction_tick` | null | null |
| Orchards in grassland | `births_per_creature_tick` | 0.015994 | 0.015364 |
| Orchards in grassland | `vm_steps_per_creature_tick` | 22.824975 | 22.912170 |
| Orchards in grassland | `plasticity_updates_per_creature_tick` | 0.030519 | 0.101430 |
| Orchards in grassland | `founder_changed_per_all_births` | 0.433333 | 0.457143 |
| Orchards in grassland | `founder_dead_per_all_births` | 0 | 0 |
| Orchards in grassland | `evolved_changed_per_mutated_births` | 0.270939 | 0.394668 |
| Orchards in grassland | `evolved_dead_per_mutated_births` | 0.043147 | 0.022757 |
| Orchards in grassland | `neighborhood_read_changed_per_all_births` | 0.1694 | 0.2156 |
| Orchards in grassland | `neighborhood_read_dead_per_all_births` | 0.0278 | 0.0068 |
| Orchards in grassland | `neighborhood_read_silent_per_all_births` | 0.461 | 0.3714 |
| Orchards in grassland | `drift_changed_per_all_births_at_2000` | 0.001 | 0.0015 |
| Orchards in grassland | `lineage_shannon_entropy_nats` | 0.519970 | 1.429177 |
| Orchards in grassland | `surviving_founder_clade_count` | 9 | 10 |
| Orchards in grassland | `reachable_structure_size_median` | 83 | 87 |
| Canyon country | `final_population` | 5,683 | 3,741 |
| Canyon country | `minimum_population` | 612 | 806 |
| Canyon country | `peak_population` | 80,263 | 79,825 |
| Canyon country | `plateau_population` | 4,366.686 | 4,177.842 |
| Canyon country | `births` | 168,585 | 175,258 |
| Canyon country | `mean_energy` | 29.707365 | 30.704367 |
| Canyon country | `extinction_tick` | null | null |
| Canyon country | `births_per_creature_tick` | 0.016041 | 0.015687 |
| Canyon country | `vm_steps_per_creature_tick` | 23.120488 | 23.097642 |
| Canyon country | `plasticity_updates_per_creature_tick` | 0.027569 | 0.099151 |
| Canyon country | `founder_changed_per_all_births` | 0.395238 | 0.414286 |
| Canyon country | `founder_dead_per_all_births` | 0 | 0 |
| Canyon country | `evolved_changed_per_mutated_births` | 0.269611 | 0.308634 |
| Canyon country | `evolved_dead_per_mutated_births` | 0.000581 | 0.016315 |
| Canyon country | `neighborhood_read_changed_per_all_births` | 0.171 | 0.2048 |
| Canyon country | `neighborhood_read_dead_per_all_births` | 0.0034 | 0.0094 |
| Canyon country | `neighborhood_read_silent_per_all_births` | 0.4474 | 0.357 |
| Canyon country | `drift_changed_per_all_births_at_2000` | 0.004 | 0.0035 |
| Canyon country | `lineage_shannon_entropy_nats` | 1.436572 | 1.432460 |
| Canyon country | `surviving_founder_clade_count` | 6 | 9 |
| Canyon country | `reachable_structure_size_median` | 83 | 75 |
| Confluence | `final_population` | 4,648 | 12,011 |
| Confluence | `minimum_population` | 178 | 191 |
| Confluence | `peak_population` | 100,000 | 100,000 |
| Confluence | `plateau_population` | 5,692.592 | 9,101.302 |
| Confluence | `births` | 230,909 | 251,335 |
| Confluence | `mean_energy` | 30.476261 | 36.881574 |
| Confluence | `extinction_tick` | null | null |
| Confluence | `births_per_creature_tick` | 0.016543 | 0.015943 |
| Confluence | `vm_steps_per_creature_tick` | 23.300402 | 23.291039 |
| Confluence | `plasticity_updates_per_creature_tick` | 0.082386 | 0.070998 |
| Confluence | `founder_changed_per_all_births` | 0.433333 | 0.457143 |
| Confluence | `founder_dead_per_all_births` | 0 | 0 |
| Confluence | `evolved_changed_per_mutated_births` | 0.331953 | 0.356962 |
| Confluence | `evolved_dead_per_mutated_births` | 0 | 0 |
| Confluence | `neighborhood_read_changed_per_all_births` | 0.1732 | 0.2442 |
| Confluence | `neighborhood_read_dead_per_all_births` | 0.0012 | 0.0008 |
| Confluence | `neighborhood_read_silent_per_all_births` | 0.4384 | 0.4732 |
| Confluence | `drift_changed_per_all_births_at_2000` | 0.001 | 0.0015 |
| Confluence | `lineage_shannon_entropy_nats` | 0.008127 | 1.027931 |
| Confluence | `surviving_founder_clade_count` | 5 | 4 |
| Confluence | `reachable_structure_size_median` | 100 | 90 |

Goal T11.F14 rows per world, changed / silent / dead (before in
parentheses). Founder:
`cases[].mutational_neighborhood.founder.births.any_events`, of 210
applied: Orchards 96/114/0 (91/119/0), Canyon 87/123/0 (83/127/0),
Confluence 96/114/0 (91/119/0). Evolved:
`cases[].mutational_neighborhood.evolved.per_seed[0].pooled_births.any_events`,
of 2,400 births per world:

| World | Applied (before) | Changed (before) | Silent (before) | Dead (before) | changed / silent / dead fractions (before) |
| --- | --- | --- | --- | --- | --- |
| Orchards in grassland | 1,538 (1,576) | 607 (427) | 896 (1,081) | 35 (68) | 0.394668 / 0.582575 / 0.022757 (0.270939 / 0.685914 / 0.043147) |
| Canyon country | 1,471 (1,721) | 454 (464) | 993 (1,256) | 24 (1) | 0.308634 / 0.675051 / 0.016315 (0.269611 / 0.729808 / 0.000581) |
| Confluence | 1,580 (1,690) | 564 (561) | 1,016 (1,129) | 0 (0) | 0.356962 / 0.643038 / 0.000000 (0.331953 / 0.668047 / 0.000000) |

Evolved `mesh_summary` per world (executed / reachable / total /
routeVaries; before): Orchards 30/41/58/1 (30/56/70/0), Canyon 39/47/58/6
(36/53/80/0), Confluence 30/37/59/0 (35/63/82/0).

Sensor census `Generation` references: the summary's
`population_persistence.per_seed[].samples[].sensor_census.world_inputs`
lists world keys only (no introspection rows), so the count is not a
census field; the string `Generation` occurs 0 times in the raw goal
report (`grep -c` over 534,284,606 bytes) and 0 times in the summary
(`AgeTicks` occurs 90 times in the raw report). The T11.F23 summary also
contains 0 occurrences.

Predeclaration table against the measurements (verdict only; no
interpretation):

| Indicator | Predeclared | Measured | Verdict |
| --- | --- | --- | --- |
| Gate `births` per creature-tick, per-seed `births`/`final_population` | move; no sign; standard thresholds | 0.026042 → 0.025998 (−0.168958% vs latest closure, `ok`); every per-seed value moved (table above) | met |
| Gate `vm_steps`, `mesh_hops`, `graph_relax_iters`, `plasticity_updates`, `actions_applied` | no direction; standard thresholds | all `ok`, `severe=false` (largest −18.536235% plasticity vs latest closure, −39.020679% vs epoch) | met |
| Founder-only (mutation-off) digest | unchanged | `5ad9e8e1…de36a9` before and after (test table above) | met |
| Goal `births` per creature-tick, `final_population`, `plateau_population`, `mean_energy` per world | move; no sign; extinction is a blocker | all moved in every world; no extinction in any world | met |
| Goal T11.F14 founder and evolved changed / silent / dead | move; no sign | founder changed rose in all three worlds (dead stays 0); evolved changed rose in all three, dead fell in Orchards, rose in Canyon, 0 in Confluence (table above) | recorded |
| Goal `neighborhood_read_silent`, drift depth, lineage diversity, recruitment | no direction | table above; recruitment `total_proposals` 82,944 | recorded |
| Goal sensor census `Generation` references | 0 by construction | 0 occurrences of `Generation` in the raw goal report and summary (no census row exists for it) | met (by string count, not a census field) |
| Goal work counters | standard thresholds; severe is a user decision | `plasticity_updates` +78.944503% **severe**; the other five `ok`; `comparison.severe=true`; CLI exit 3, `make` exit 2 | **severe — user decision under the blocker rule** |
| Wall caps | flag-only; caps are closure checks | gate wall +17.4%/+19.3% `ok`; goal wall −3.8% `ok`; evolved 1,503 ms of 180,000; founder 59 ms (gate) and 225 ms (goal) of 10,000 | met |
