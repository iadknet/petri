# T11.F20 readings

## Pre-feature references (T19.F06 goal summary, production-identical to `2c09fd76`)

Drift walk (`drift-depth-v3`, legacy per-birth rule), per checkpoint, 2,000 births each:

| Depth | Orchards / Confluence changed/all | Canyon changed/all | Orchards / Confluence dead/all | Canyon dead/all | Mean executed nodes (O/C, Canyon) |
| --- | --- | --- | --- | --- | --- |
| 0 | 0.162000 (324) | 0.158500 (317) | 0.000000 | 0.000000 | 2.00, 2.00 |
| 22 | 0.101500 (203) | 0.095000 (190) | 0.004000 | 0.003000 | 2.56, 2.56 |
| 250 | 0.025500 (51) | 0.024000 (48) | 0.001000 | 0.000000 | 4.36, 4.74 |
| 1,000 | 0.013000 (26) | 0.008000 (16) | 0.001500 | 0.002000 | 7.42, 6.98 |
| 2,000 | 0.003500 (7) | 0.002500 (5) | 0.000000 | 0.002500 | 9.60, 10.06 |

Pinned-draw expectations: legacy mean requested 0.44 × 1.25 = 0.55, event-bearing 0.44; pinned Binomial(97, 0.005) mean 0.485, P(0) = 0.6149, event-bearing 0.3851 (ratio 0.875).

Other references: goal `drift_depth_wall_clock_ms` 24,370.5; `recruitment_paths_wall_clock_ms` 12,609.0; `recruitment_paths.total_proposals` 82,944; host Isaacs-MacBook-Pro-2.local (Apple M1 Pro). Goal vs T19.F04 epoch: `decided_passes` +255.434783% severe (accepted 2026-09-22, T19.F05), `pass_cap_hits` +22.682268% flag. Gate vs T19.F04: `pass_cap_hits` +35.106383% flag, not severe.

Comparison-input check (2026-09-23): every `epoch_baseline` and `closed` path in `docs/progress/benchmark-series.json` is a summary (`kind` present; 0 full reports); no committed full report under `docs/progress/features/` embeds a retired key or `recruitment_paths`; `grep -l mutation_probability` over the 97 `.bench-artifacts/historical/blobs` reports finds none.

Pre-feature `by_requested_events` at Canyon depth 2,000 (legacy): 0:1135, 1:701, 2:126, 3:33, 4:3, 5:2. Binomial(97, 0.005) expectation per 2,000: 0:1230, 1:600, 2:145, 3:23, 4:3.

## Build verification (2026-09-23)

Viability, `cargo test -p v3-core --test viability`:

| Run | Result |
| --- | --- |
| Pre-change baseline, first test command | 28 passed, 0 failed |
| First run after `MutationConfig` changed shape | 27 passed; `mutation_accounting_invariant_in_viability` still running after 10 min and killed. At `per_unit_rate = 0.03` on its genome's own size, a genome mutated cumulatively over 1,000 seeds compounds its own exposure. Re-expressed as rate 1.0 on 3 units through `apply_mutations_on_units`, which is exactly three events per call, as in the old 1–5 range. |
| Rerun | 28 passed, 0 failed. No assertion weakened. `founder_only_trajectory_digest_is_pinned` passes unchanged. |

Focused tests, all passing. Config: `retired_per_birth_supply_keys_are_rejected`. Recipe: `recipe_rejects_the_retired_per_birth_supply_keys` and `root_world_recipes_resolve`. Runtime patch: `patch_config_rejects_the_retired_per_birth_supply_keys` (v3-server). Instrument constant: `the_walk_draws_on_the_founder_unit_count_whatever_the_walked_size`, where at rate 1.0 every walk and checkpoint birth from a 111-unit subject requests exactly 97 events and `supply_rule` is pinned. Also `recruitment_paths_panels_enforce_their_size_caps` (`Supply::FounderUnits.units` is 97 whatever the parent's size) and `a_supplied_unit_count_ignores_the_genome_size`. Production path: `production_supply_draws_on_the_genome_s_own_size`. Unchanged pins: `legacy_default_short_run_identity` (hash `7548337837478651677`), `the_canonical_founder_genome_is_ninety_seven_units`, and `default_per_unit_rate_keeps_the_founder_near_the_legacy_supply`.

Re-pins of test-only fixtures (legacy-rule values; no production pin moved):

| Test | Before | After |
| --- | --- | --- |
| `production_prepared_lineages_match_the_recorded_baseline_and_metadata`: graph Drift, graph Selection, VM Drift, VM Selection | `[18,13,13,1]`, `[18,18,18,17]`, `[19,16,16,8]`, `[19,19,19,19]` | `[20,14,13,7]`, `[19,19,19,19]`, `[25,18,18,12]`, `[25,25,25,25]`; discards `[0,0]` in all four |
| `production_cost_selection_lineages_pin_the_first_reading`: graph, VM (censored) | `[16,16,16,16]` (16), `[21,21,21,21]` (11) | `[19,19,19,19]` (13), `[23,23,23,23]` (9) |
| `the_walk_dates_modules_from_their_birth_and_from_every_refresh`: created, dispatched; Dispatch; InternalChange | 12, 4; reached 6, median 5, censored 0/6; reached 6, median 5, censored 1/5 | 9, 7; reached 7, median 7, censored 0/2; reached 3, median 1, censored 0/6 |
| `accounting_preserves_pre_feature_sampled_trajectories_and_actions` (fixture moved from 2–4 events to `per_unit_rate = 0.03`; two agreeing runs) | `14541daa…307226` | `c04e289e…eafc95` |
| `checked_in_goal_recipe_identities_are_unchanged_by_json_precision`: orchards, canyon, confluence (two agreeing runs) | `eabc5096…cd261f`, `f9d82656…72c4a1`, `790b73ae…f34436` | `0a805638…75a890`, `4edca49b…133cbc`, `09398394…b341f6` |
| `recruitment_projection_keeps_estimates_counts_and_pairing_but_no_trace_payloads`: compact `supply` | `"legacy"` | `"founder_units"` |

Retirement scan: `rg -n "mutation_probability|per_birth_mutation_events_min|per_birth_mutation_events_max|per_birth_mutation_event_continuation_probability|per_unit_supply_enabled|with_legacy_supply|Per-Unit Supply" crates frontend/src docs/reference world-recipe-*.json` finds only the three rejection tests: `crates/v3-core/src/config/simulation.rs`, `crates/v3-core/src/config/mod.rs`, and `crates/v3-server/tests/server.rs`.

`make check` (worktree, after the last code change): exit 0. Its two `[bench] Error 1` lines are the Makefile's expected-failure self-tests (`FEATURE is required for PROFILE=gate`, `FEATURE or OUT is required for PROFILE=sweep`). `cargo clippy --workspace --all-targets` and `cargo check --workspace --all-targets` are clean, and `make roadmap-check` passes. After the self-review cleanup (the trivial `proposal_mutation_config` test helper inlined; no pinned value moved), viability is 28 passed, `cargo test -p v3-core --lib recruitment_paths` is 68 passed and 1 ignored, and `make check` exits 0 again.

## Codex challenge loop

| Round | Job ID | Finding | Severity | Disposition |
| --- | --- | --- | --- | --- |
| 1 | task-muezswbu-18bgvz | Historical full-report comparison breaks when fields are deleted and `Supply::Legacy` is renamed (`artifacts.rs:580`) | blocking | Rebutted with evidence, and the spec now states the reason (invariant 4): every series reference is a summary read through `comparison_inputs`; no committed full report and none of the 97 historical blobs carries a retired key; the local post-T15.F01 raws are not comparison inputs. |
| 1 | task-muezswbu-18bgvz | Viability must run before any other implementation test, not only first after the shape change | blocking | Fixed: viability runs as the first test command of implementation (a pre-change baseline) and again first after the shape change (Implementation Tasks, Verification). |
| 1 | task-muezswbu-18bgvz | "All wall readings report-only on another host" waives the absolute observation budgets | blocking | Fixed: the absolute budgets (10 s, 180 s, 15 min, drift under 30 s) apply on any host; only the relative comparisons are host-bound. |
| 1 | task-muezswbu-18bgvz | The drift 0.875 scaling and the executed-node direction do not follow; Canyon depth 2,000 has a zero tripwire | advisory | Adopted: the scaling is labeled a heuristic; the executed-node direction is removed; a powered `by_requested_events` check is added; a checkpoint below half its pre-feature count gets an attribution before its row is adopted. |
| 1 | task-muezswbu-18bgvz | 24.37 s is an aggregate over the three worlds, not a per-case figure | advisory | Adopted: the drift wall is stated as one aggregate. |
| 1 | task-muezswbu-18bgvz | The production-identity wording is internally inconsistent | advisory | Adopted: rewritten as one exhaustive list of allowed differences. |
| 1 | task-muezswbu-18bgvz | Reuse the existing `FOUNDER_GENOME_SIZE_UNITS` (`founder.rs:16`, already pinned in `state.rs`) | advisory | Adopted (invariant 2). |
| 2 | task-muf029m2-vz7gr8 | Round-1 rebuttal rejected: `bench-summarize` and full-report references still decode `Report` typed, and T19.F06's goal raw carries the retired keys and `"supply": "legacy"`. Require historical decoding and a fixture | blocking | Rebutted again with code evidence, now stated in invariant 4. Since `02d33611` (T19.F06) removed `action_queue_cap` from the strict `MutationConfig`, main already cannot typed-decode any goal raw older than T19.F06 (the T19.F05 raw carries `"action_queue_cap": 4`). A shim would rescue only T19.F06's goal raw, which is already summarized. The T15.F02 historical set is unaffected. The row delegates reject-or-ignore to this spec. |
| 2 | task-muf029m2-vz7gr8 | Round-1 fixes: viability ordering, wall budgets, drift qualification, aggregate timing, identity list, founder anchor | advisory | Confirmed by Codex. |
| 3 | task-muf09o1p-anr3lc | Round-2 rebuttal (historical decoding) | advisory | Accepted by Codex: `action_queue_cap` sat inside the strict `MutationConfig` (`02d33611^`, line 858), and the T19.F05 raw carries it. All 94 series references are summaries. No new blocking findings. `verdict: ready`. |

## Benchmark gate (2026-09-23, benchmark specialist)

Worktree HEAD `96bc5b03` (clean). `PATH` prefixed with
`~/.local/share/petri-tools/bin` and `~/.local/share/aquaproj-aqua/bin`. Both
commands run once, sequentially, nothing else running on the host.

**Gate.** `make bench PROFILE=gate FEATURE=t11-f20-per-birth-supply-rule-retirement`,
CLI and `make` exit 0. Raw
`/Users/istefanek/projects/petri/.bench-artifacts/t11-f20-per-birth-supply-rule-retirement/gate.json`
(102,646 bytes, sha256 `ae36055107a51f4f18ede63307d68d78edd80c7324fdb4bb473e91dee9d10e81`).
Summary `docs/progress/features/t11-f20-per-birth-supply-rule-retirement.json`
(105,579 bytes, sha256 `066057e40237851a19ffb453b1b1c894177cfae852c556031e34eb9022397c47`).
Vs T19.F06 (production-identity check): `severe=false`, every counter
`delta%=0.000000`. Vs T19.F04 epoch: `severe=false`, `pass_cap_hits` flag
+35.106383% (matches T19.F06's own comparison to the same epoch exactly; not
remediated, per predeclaration).

**Goal.** `make bench PROFILE=goal FEATURE=t11-f20-per-birth-supply-rule-retirement`,
run once. CLI exits 3 ("error: severe work-counter regression against a
stored reference"), `make` exits 2 (`make: *** [bench] Error 3`, captured
outer status 2). Raw
`/Users/istefanek/projects/petri/.bench-artifacts/t11-f20-per-birth-supply-rule-retirement/goal.json`
(654,040,575 bytes, sha256
`4a5837d458b08f814cee75d0bac0fa1d25510dedd4e1f3ce61a2b5c55198db72`). Summary
`docs/progress/features/t11-f20-per-birth-supply-rule-retirement-goal.json`
(7,813,592 bytes, sha256
`d25fd20bc375deb3010017018218713611ceab3e4e1e7ed2c4855c1124f05dc1`). Vs
T19.F06: `severe=false`, every counter `delta%=0.000000`. Vs T19.F04 epoch:
`severe=true` on `decided_passes` +255.434783% and `pass_cap_hits` flag
+22.682268% — exactly T19.F06's own comparison to the same epoch, the severe
accepted by the user on 2026-09-22 at T19.F05 and not remediated here, per
predeclaration.

**Production-identity diff (invariant 3).** A recursive structural diff of
`deterministic` between the new raws and T19.F06's raws
(`.bench-artifacts/t19-f06-retirement-and-observability/{gate,goal}.json`)
rolled up to the top-level container once inside a permitted subtree. Gate:
0 differing paths (byte-identical). Goal: 10 differing paths, all within the
spec's exhaustive permitted list and none outside it —
`goal_indicators.cases[0..2].case.config_digest` (3),
`goal_indicators.cases[0..2].drift_depth` (3, the whole subtree, expected
because the instrument draw changed), `goal_indicators.recruitment_paths` (1,
the whole subtree, same reason), and `profile.cases[0..2].config_digest` (3).
No other `deterministic` path differs. Both comparisons load the T19.F04 and
T19.F06 summaries (confirmed in the `make bench` output above: each run
prints one comparison block against
`docs/progress/features/t19-f04-vote-based-action-selection{,-goal}.json` and
one against `docs/progress/features/t19-f06-retirement-and-observability{,-goal}.json`).

**Predeclaration table, measured against T11.F20's own goal raw:**

| Reading | Predeclared | Measured | Fit |
| --- | --- | --- | --- |
| Work counters, persistence, founder neighborhood, T14.F12 read, `mutation_supply` vs T19.F06 | Identical exact integers | `delta%=0.000000` on every counter, gate and goal | Fits |
| Same counters vs T19.F04 epoch | Gate `pass_cap_hits` +35.106383% flag, not severe; goal `severe=true` `decided_passes` +255.434783%, `pass_cap_hits` +22.682268% flag, CLI exits 3, `make` exits 2 | Exactly reproduced (see above) | Fits |
| Config identity | Goal case digests change in all three worlds, plus `recruitment_paths.config`/`config_digest` and the gate's `effective_config_digest` | `inputs_changed` true; the identity diff confirms the digests differ (permitted) | Fits |
| Drift walk version and `supply_rule` | `drift-depth-v4`, string names the founder-pinned draw | All three worlds: `drift-depth-v4`, `"per-unit draw on the canonical V3Alpha1 founder's genome_size() 97 at per_unit_rate 0.005: Binomial(97, 0.005) events per walk and checkpoint birth"` | Fits |
| `by_requested_events`: zero-request share [0.58, 0.65], mean [0.44, 0.53] at every checkpoint/world | — | Zero share 0.6115–0.6235, mean 0.4790–0.4935 at every depth (0/22/250/1000/2000), identical across Orchards, Canyon, Confluence | Fits |
| Changed per all births, per checkpoint (tripwires, counts per 2,000) | Orchards/Confluence 245/147/30/12/1 at depth 0/22/250/1000/2000; Canyon 239/137/28/6/0 | Orchards/Confluence 305/244/48/12/5; Canyon 293/225/28/9/8 — every value at or above its tripwire | Fits (no escalation; depth-1000 Orchards/Confluence and depth-250 Canyon land exactly on their tripwire, not below it) |
| Dead per all births; executed nodes at 1,000/2,000 | Reported, no direction; pre-feature dead 0.0015/0.002 (O/C) and 0.0025 (Canyon-only reading was 0.0/0.0025 — see readings above); executed 7.42/6.98/7.42 (d1000), 9.60/10.06/9.60 (d2000) | Dead: O 0.0/0.0025 (depth 250: 0.0025), C 0.0/0.001 (depth 2000: 0.001); depth-1000 dead 0.0 both; executed nodes O 6.66 (d1000)/9.68 (d2000), C 7.06 (d1000)/9.66 (d2000) | Reported, no direction predeclared, so no fit/no-fit judgment applies |
| `recruitment_paths` block: `arms`/`pairs`/`opportunities`/`constructed`/`total_proposals` | 82,944 proposals unchanged, version unchanged, discard counts zero | `total_proposals=82944`, `version="recruitment-paths-v1"`, `selected_inapplicable_by_domain={}` and `discarded_selected_inapplicable_by_operator={}` (zero) | Fits |
| Wall: founder neighborhood < 10 s, evolved neighborhood (summed) < 180 s, goal run < 15 min investigated, `drift_depth_wall_clock_ms` < 30 s | Absolute budgets, any host | 240.45 ms, 3,681.43 ms, 467,862.48 ms total (~7.8 min), 14,950.68 ms | Fits |
| Wall (relative, T19.F06 host `Isaacs-MacBook-Pro-2.local`): `drift_depth_wall_clock_ms` ≤ 24,370.5 ms + 10% (26,807.55); `recruitment_paths_wall_clock_ms` within 10% of 12,609.0 ms (11,348.1–13,869.9) | — | Host matches (`Isaacs-MacBook-Pro-2.local`); 14,950.68 ms; 11,613.22 ms | Fits |
| Gate/goal wall per creature-tick | No direction | Gate 0.001671 ms; goal 0.010708 ms | Reported, no fit/no-fit judgment |

No reading falls outside its predeclaration; no tripwire is crossed; no
requested-events check misses; the identity diff shows no non-permitted path.

Series entries added: `t11-f20-per-birth-supply-rule-retirement.json`
appended to the `gate-v1` series `closed` list;
`t11-f20-per-birth-supply-rule-retirement-goal.json` appended to the
`goal-worlds-v1` series `closed` list. `epoch_baseline` unchanged in both
(`t19-f04-vote-based-action-selection.json` /
`...-goal.json`). No full report staged; both raws stay under the ignored
`.bench-artifacts/` in the main checkout.
