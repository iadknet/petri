# T14.F04 — population readings on the persistence checkpoints: measured readings

| Evidence record |
| --- |
| Closure pass, 2026-09-11, worktree `.claude/worktrees/t14-f04`, branch `worktree-t14-f04`, build commit `f771c30d`. Both reports record `git_revision` `f771c30dfc87baa74df2d7b8d8ae60423382674a`. Reports: [gate](../features/t14-f04-population-readings-on-the-persistence-checkpoints.json) (1,549 lines, 49.7 KB), [goal](../features/t14-f04-population-readings-on-the-persistence-checkpoints-goal.json) (74,887 lines, 2.98 MB — 300 lines above T14.F02's 74,587, the five readings on each of 60 checkpoint samples). |

## Verification commands

| Command | Outcome |
| --- | --- |
| `MUTANTS_ITERATE=0 make rust-mutants` | exit 0; `11 mutants tested in 82s: 8 caught, 3 unviable`; `rust-mutants: no survivors` |
| `make bench PROFILE=gate FEATURE=t14-f04-population-readings-on-the-persistence-checkpoints` | exit 0; `severe=false` against both references; every metric `level=ok`; wall total 540.86 ms |
| `make bench PROFILE=goal FEATURE=t14-f04-population-readings-on-the-persistence-checkpoints` | exit 0; `severe=false` against both references; wall total 460,451.08 ms (7.67 min); only non-`ok` level is the inherited `plasticity_updates` flag against T12.F04 |
| `make roadmap-check` | exit 0 |
| `make check-docs` | exit 0; roadmap, dependency, policy, quality, skill and script regression checks all pass, no dead link |

## Mutation gate transcript

Fresh run, diff base `b377b790` (merge base with `main`), output in
`~/.local/share/petri-tools/mutants/t14-f04/mutants.out`:

```
rust-mutants: fresh run; no prior mutant results reused
rust-mutants: diff against b377b790f6496027d1b800a87e68945ae3d275e7, output in /Users/istefanek/.local/share/petri-tools/mutants/t14-f04/mutants.out
Found 11 mutants to test
ok       Unmutated baseline in 23s build + 4s test
 INFO Auto-set build timeout to 185s
 INFO Auto-set test timeout to 120s
11 mutants tested in 82s: 8 caught, 3 unviable
rust-mutants: no survivors
```

No mutants missed, none timed out; no survivor triage was needed and no test or
production file changed for the gate.

## Goal comparison transcript (tail of log)

```
wrote docs/progress/features/t14-f04-population-readings-on-the-persistence-checkpoints-goal.json
compared against docs/progress/features/t12-f04-baseline-world-set-goal.json: severe=false
  mesh_hops: current=2.258369 reference=Some("2.247936") delta%=Some("0.464115") level=ok
  vm_steps: current=23.478709 reference=Some("23.339759") delta%=Some("0.595336") level=ok
  graph_relax_iters: current=1.024449 reference=Some("1.028540") delta%=Some("-0.397748") level=ok
  plasticity_updates: current=0.066183 reference=Some("0.046976") delta%=Some("40.886836") level=flag
  actions_applied: current=1.360568 reference=Some("1.371176") delta%=Some("-0.773642") level=ok
  births: current=0.019659 reference=Some("0.018609") delta%=Some("5.642431") level=ok
compared against docs/progress/features/t14-f01-closure-comparison-and-indicator-coverage-integrity-goal.json: severe=false
  mesh_hops: current=2.258369 reference=Some("2.258369") delta%=Some("0.000000") level=ok
  vm_steps: current=23.478709 reference=Some("23.478709") delta%=Some("0.000000") level=ok
  graph_relax_iters: current=1.024449 reference=Some("1.024449") delta%=Some("0.000000") level=ok
  plasticity_updates: current=0.066183 reference=Some("0.066183") delta%=Some("0.000000") level=ok
  actions_applied: current=1.360568 reference=Some("1.360568") delta%=Some("0.000000") level=ok
  births: current=0.019659 reference=Some("0.019659") delta%=Some("0.000000") level=ok
```

Every compute metric reads 0.000000 % against the T14.F01 goal reference, and the
readings are byte-identical to the values T14.F02's regenerated report records.
The `plasticity_updates` flag against T12.F04 is the same inherited 0.066183
T14.F01 and T14.F02 already store; it is not a new crossing.

The predeclared direction of **none** is checked on the indicators themselves,
not only on the compute metrics: deleting the five new keys from every checkpoint
sample of this report's `deterministic.goal_indicators` and diffing the sorted
block against T14.F02's stored goal report gives an empty diff, so lineage
diversity, memory sensitivity, temporal memory sensitivity, mutational
neighborhood, drift depth, population persistence and births per 100 ticks are
unchanged value for value.

```
jq -S 'del(.deterministic.goal_indicators.population_persistence.per_seed[].samples[]
       | .mean_genome_size, .mean_mesh_nodes, .mean_generation,
         .surviving_founder_clade_count, .shannon_entropy_nats)
       | .deterministic.goal_indicators' <f04-goal> > a.json
jq -S '.deterministic.goal_indicators' <f02-goal> > b.json
diff a.json b.json   # no output
```

## Caps and thresholds (goal report)

| Cap | Limit | Reading |
| --- | --- | --- |
| `environment.neighborhood_evolved_wall_clock_ms_total` | 180,000 ms | 426.49 ms |
| `environment.neighborhood_founder_wall_clock_ms` | 10,000 ms | 97.82 ms |
| Total goal-profile investigation | 15 min | 460.45 s (7.67 min) |
| `environment.drift_depth_wall_clock_ms` | no cap | 18,170.11 ms |
| Wall ms per creature-tick | normalized thresholds | gate 0.001265, goal 0.007552 |
| `severe` on any reference | must be false | false on all four comparisons |
| Epoch re-pin | none authorized | not re-pinned; both references unchanged |
| Non-`ok` levels in the stored `comparison` block | — | gate 14 `ok`, 0 other; goal 13 `ok` and the 1 inherited `plasticity_updates` `flag` |
| `deterministic.goal_indicators` vs T14.F02's stored goal report, with the five new sample keys deleted | predeclared direction: none | byte-identical, empty `diff` |

## Checkpoint readings in the stored goal report

Seed-to-world mapping from `deterministic.goal_indicators.cases[].case`:
11 = Orchards in grassland, 22 = Canyon country, 33 = Confluence.

Presence of all five readings on every checkpoint sample
(`has("mean_genome_size") and has("mean_mesh_nodes") and has("mean_generation")
and has("surviving_founder_clade_count") and has("shannon_entropy_nats")`):

| World case (seed) | Checkpoint samples | All five present on every sample | Samples with a null mean (extinction) |
| --- | --- | --- | --- |
| Orchards in grassland (11) | 20 | true | 0 |
| Canyon country (22) | 20 | true | 0 |
| Confluence (33) | 20 | true | 0 |

The gate report carries 1 checkpoint sample per seed, all five present on each.
Every one of the 60 goal samples has exactly 17 top-level keys
(`[.samples[]|keys|length]|unique` -> `[17]`).

First and last checkpoint of each case:

| Seed | Tick | Population | `mean_genome_size` | `mean_mesh_nodes` | `mean_generation` | `surviving_founder_clade_count` | `shannon_entropy_nats` |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 11 | 100 | 100,000 | 116.749420 | 2.154100 | 2.098710 | 9,564 | 8.853854 |
| 11 | 2000 | 11,313 | 213.125608 | 4.136215 | 47.819411 | 23 | 2.429221 |
| 22 | 100 | 45,014 | 116.760052 | 2.161883 | 2.236260 | 7,215 | 8.318394 |
| 22 | 2000 | 7,090 | 238.384344 | 5.053879 | 48.090832 | 25 | 2.901692 |
| 33 | 100 | 100,000 | 117.195070 | 2.163720 | 2.168060 | 8,672 | 8.728352 |
| 33 | 2000 | 9,995 | 228.936068 | 5.310055 | 52.037019 | 17 | 2.296008 |

### `WorldTracking` key set of a checkpoint sample vs T14.F02's stored report

Union of the sample key sets over all samples of all three cases in each report,
`diff` of the sorted lists (`<` = T14.F02, `>` = T14.F04):

```
3a4,6
> mean_generation
> mean_genome_size
> mean_mesh_nodes
10a14,15
> shannon_entropy_nats
> surviving_founder_clade_count
```

T14.F02's 12 keys are `births_total`, `food_density_total`, `mean_energy`,
`move_attempts_with_barrier_neighbor_by_reader_state`, `moves_attempted_total`,
`moves_blocked_avoidable_by_reader_state`, `moves_blocked_barrier_total`,
`moves_blocked_barrier_with_barrier_neighbor_by_reader_state`,
`moves_blocked_total_by_cause`, `population`, `tick`, `typed_eats_total`. All
twelve are present in T14.F04 with no key removed, renamed or re-nested: the
diff is additions only, the five new readings, 12 -> 17 keys.
