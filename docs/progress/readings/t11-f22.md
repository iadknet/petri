# T11.F22 — Meaning-Stable Input References readings

Tables and transcripts for the feature spec
[`docs/specs/roadmap/t11-f22-meaning-stable-input-references.md`](../../specs/roadmap/t11-f22-meaning-stable-input-references.md).

## Task 1: cross-kind fraction of pre-change `Swap` changes

Run before any operator change, at worktree HEAD `04b92491` plus the
`input_ref_kind` function only. Population: the live survey's 400 dumped
genomes and its `config.json` (`docs/strategy/live-survey-2026-09-16.md`,
Appendix A; the `config` key deserializes to `SimulationConfig`,
`food_type_count = 2`, so the three `Food` kinds are swappable in this
population). 20 `Swap` trials per genome under the live mutation config,
seeded and target-selected exactly as `neighborhood::operators::per_operator_rows`
(seed `1_000_000 * (i + 1) + INPUT_REF_SEED_BASE + trial`, input-ref
reachable bias and executed bias from the config), classified by the T11.F01
battery against each genome's own base signature.

Command (probe dropped into `crates/v3-core/tests/zz_probe_swap_kind.rs`,
not committed):

```sh
PETRI_GENOME_DIR=<survey>/genomes PETRI_CONFIG=<survey>/config.json \
PETRI_N=400 PETRI_TRIALS=20 \
cargo test --release -p v3-core --test zz_probe_swap_kind -- --nocapture
```

Self-check at `PETRI_N=100`: silent 0.7895, changed 0.208, dead 0.0025 —
the research note's cheap-proof `Swap` row (0.789 / 0.208 / 0.003), so the
seeds and selector match the note's probe.

Summary line at `PETRI_N=400`:

```json
{"B":0.3378961117861482,"applied":8000,"c":0.8219927095990279,"changed_frac":0.20575,"cross":{"changed":1353,"dead":15,"silent":5147},"dead_frac":0.002,"food_type_count":2,"genomes":400,"identical_redraw":236,"silent_frac":0.79225,"skipped":0,"trials_per_genome":20,"within":{"changed":293,"dead":1,"silent":955}}
```

- Applied 8,000 of 8,000 trials; 236 redrew the identical reference (silent,
  counted in `silent_frac`, excluded from the kind split).
- Changed trials: 1,646; cross-kind 1,353, within-kind 293.
- **`c` = 1353 / 1646 = 0.8220**; **`B` = 0.125 + 0.259 × 0.8220 = 0.3379.**
- Dead trials (not folded into `c`): 16, cross-kind 15, within-kind 1.
- `B` exceeds about 0.29, so per the spec's Performance section the T14.F12
  `neighborhood_read.changed_per_all_births` floors bind before `B`.

Per old kind (the entry `Swap` overwrote), within-kind versus cross-kind
outcome of the replacement:

| Old kind (class, width) | within silent | within changed | within dead | cross silent | cross changed | cross dead |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| ActionQueue,12 | 0 | 0 | 0 | 252 | 1 | 0 |
| Barrier,7 | 0 | 0 | 0 | 339 | 8 | 4 |
| Barrier,8 | 0 | 0 | 0 | 205 | 4 | 1 |
| Food,1 | 9 | 5 | 0 | 233 | 206 | 1 |
| Food,7 | 8 | 0 | 0 | 241 | 4 | 0 |
| Food,8 | 3 | 7 | 0 | 293 | 271 | 2 |
| Introspection,1 | 122 | 67 | 1 | 880 | 398 | 2 |
| Neighbor,12 | 0 | 0 | 0 | 233 | 7 | 0 |
| Neighbor,16 | 0 | 0 | 0 | 251 | 14 | 0 |
| Neighbor,8 | 0 | 0 | 0 | 267 | 1 | 0 |
| Occupancy,7 | 0 | 0 | 0 | 230 | 8 | 1 |
| Occupancy,8 | 0 | 0 | 0 | 226 | 14 | 0 |
| Upstream,1 | 813 | 214 | 0 | 1497 | 417 | 4 |

Kind pairs (old → new) with at least one changed or dead trial; pairs that
were silent on every trial are omitted:

| Old kind | New kind | silent | changed | dead |
| --- | --- | ---: | ---: | ---: |
| ActionQueue,12 | Introspection,1 | 40 | 1 | 0 |
| Barrier,7 | Food,8 | 18 | 2 | 0 |
| Barrier,7 | Introspection,1 | 52 | 6 | 3 |
| Barrier,7 | Upstream,1 | 125 | 0 | 1 |
| Barrier,8 | Introspection,1 | 29 | 3 | 1 |
| Barrier,8 | Occupancy,8 | 9 | 1 | 0 |
| Food,1 | ActionQueue,12 | 11 | 7 | 0 |
| Food,1 | Barrier,7 | 10 | 8 | 0 |
| Food,1 | Barrier,8 | 16 | 9 | 0 |
| Food,1 | Food,1 | 9 | 5 | 0 |
| Food,1 | Food,7 | 12 | 7 | 0 |
| Food,1 | Food,8 | 9 | 7 | 0 |
| Food,1 | Introspection,1 | 40 | 44 | 1 |
| Food,1 | Neighbor,12 | 14 | 7 | 0 |
| Food,1 | Neighbor,16 | 6 | 11 | 0 |
| Food,1 | Neighbor,8 | 16 | 11 | 0 |
| Food,1 | Occupancy,7 | 3 | 13 | 0 |
| Food,1 | Occupancy,8 | 9 | 6 | 0 |
| Food,1 | Upstream,1 | 87 | 76 | 0 |
| Food,7 | Food,8 | 7 | 2 | 0 |
| Food,7 | Upstream,1 | 88 | 2 | 0 |
| Food,8 | ActionQueue,12 | 15 | 10 | 0 |
| Food,8 | Barrier,7 | 9 | 12 | 0 |
| Food,8 | Barrier,8 | 8 | 11 | 0 |
| Food,8 | Food,1 | 19 | 8 | 0 |
| Food,8 | Food,7 | 10 | 9 | 0 |
| Food,8 | Food,8 | 3 | 7 | 0 |
| Food,8 | Introspection,1 | 66 | 55 | 2 |
| Food,8 | Neighbor,12 | 13 | 12 | 0 |
| Food,8 | Neighbor,16 | 8 | 15 | 0 |
| Food,8 | Neighbor,8 | 19 | 17 | 0 |
| Food,8 | Occupancy,7 | 10 | 20 | 0 |
| Food,8 | Occupancy,8 | 11 | 12 | 0 |
| Food,8 | Upstream,1 | 105 | 90 | 0 |
| Introspection,1 | ActionQueue,12 | 49 | 22 | 0 |
| Introspection,1 | Barrier,7 | 40 | 27 | 0 |
| Introspection,1 | Barrier,8 | 53 | 18 | 0 |
| Introspection,1 | Food,1 | 53 | 20 | 0 |
| Introspection,1 | Food,7 | 48 | 20 | 0 |
| Introspection,1 | Food,8 | 43 | 21 | 0 |
| Introspection,1 | Introspection,1 | 122 | 67 | 1 |
| Introspection,1 | Neighbor,12 | 30 | 18 | 0 |
| Introspection,1 | Neighbor,16 | 38 | 10 | 0 |
| Introspection,1 | Neighbor,8 | 47 | 20 | 0 |
| Introspection,1 | Occupancy,7 | 46 | 20 | 1 |
| Introspection,1 | Occupancy,8 | 46 | 20 | 0 |
| Introspection,1 | Upstream,1 | 387 | 182 | 1 |
| Neighbor,12 | Food,8 | 11 | 3 | 0 |
| Neighbor,12 | Introspection,1 | 47 | 3 | 0 |
| Neighbor,12 | Upstream,1 | 76 | 1 | 0 |
| Neighbor,16 | Food,1 | 9 | 2 | 0 |
| Neighbor,16 | Food,8 | 14 | 3 | 0 |
| Neighbor,16 | Introspection,1 | 42 | 5 | 0 |
| Neighbor,16 | Occupancy,8 | 9 | 2 | 0 |
| Neighbor,16 | Upstream,1 | 104 | 2 | 0 |
| Neighbor,8 | Occupancy,8 | 21 | 1 | 0 |
| Occupancy,7 | Food,1 | 14 | 2 | 0 |
| Occupancy,7 | Introspection,1 | 36 | 4 | 1 |
| Occupancy,7 | Occupancy,8 | 9 | 1 | 0 |
| Occupancy,7 | Upstream,1 | 102 | 1 | 0 |
| Occupancy,8 | Barrier,7 | 6 | 2 | 0 |
| Occupancy,8 | Introspection,1 | 41 | 5 | 0 |
| Occupancy,8 | Neighbor,16 | 8 | 2 | 0 |
| Occupancy,8 | Occupancy,7 | 9 | 2 | 0 |
| Occupancy,8 | Upstream,1 | 81 | 3 | 0 |
| Upstream,1 | ActionQueue,12 | 95 | 22 | 0 |
| Upstream,1 | Barrier,7 | 92 | 39 | 0 |
| Upstream,1 | Barrier,8 | 123 | 25 | 0 |
| Upstream,1 | Food,1 | 89 | 28 | 0 |
| Upstream,1 | Food,7 | 111 | 29 | 0 |
| Upstream,1 | Food,8 | 103 | 31 | 0 |
| Upstream,1 | Introspection,1 | 392 | 132 | 4 |
| Upstream,1 | Neighbor,12 | 97 | 30 | 0 |
| Upstream,1 | Neighbor,16 | 104 | 19 | 0 |
| Upstream,1 | Neighbor,8 | 97 | 24 | 0 |
| Upstream,1 | Occupancy,7 | 103 | 12 | 0 |
| Upstream,1 | Occupancy,8 | 91 | 26 | 0 |
| Upstream,1 | Upstream,1 | 813 | 214 | 0 |

## Tests for invariants 1–3 and the founder fixture

`crates/v3-core/src/mutation/input_ref/tests.rs`:

- `swap_alternatives_follow_the_kind_table`, `kind_is_class_and_width` (the
  kind partition);
- `swap_changes_one_entry_within_its_kind` (invariant 1, proptest, 48 cases
  over generated genomes at 1–3 food types);
- `prune_deletes_an_unreferenced_entry_and_keeps_every_consumer` (invariant
  2, proptest, 48 cases);
- `swap_on_the_graph_backend_keeps_every_edge` (replaces the pre-feature
  `swap_graph_input_ref_clamps_out_of_range_sub_indices`, whose premise no
  longer holds);
- founder fixture: `swap_applies_on_both_founder_nodes`,
  `prune_applies_to_founder_node_zero_only_and_is_silent` (Graph, battery
  signature identical), `prune_on_the_founder_vm_node_is_silent` (VM,
  battery signature identical), `prune_input_ref_decreases_count`,
  `prune_input_ref_on_empty_returns_no_applicable_target`.

`crates/v3-core/src/mutation/applicability_tests.rs` (invariant 3, T13.F03
invariants 1–3 and 5 extended to `Swap` and `Prune`):
`accepted_input_ref_node_always_applies`,
`empty_applicable_set_skips_and_pick_stays_inside_it`,
`a_biased_draw_stays_inside_the_applicable_set`,
`padding_never_shrinks_the_applicable_set`.

`crates/v3-cli/src/bench/tracking/tests.rs` (invariant 6):
`mutation_outcome_value_counters_are_optional_in_the_schema`,
`transferred_tracking_blocks_read_the_stats_counters_behind_them` (the existing end-of-run block test now carries the
three counters).

Re-pinned with the reason in the test (the new draw moves every lineage at
its first input-reference event):
`neighborhood::drift::tests::the_walk_dates_modules_from_their_birth_and_from_every_refresh`,
`recruitment_paths::experiment::tests::production_prepared_lineages_match_the_recorded_baseline_and_metadata`,
`recruitment_paths::experiment::tests::production_cost_selection_lineages_pin_the_first_reading`,
`crates/v3-core/tests/applied_trajectory.rs`
(`accounting_preserves_pre_feature_sampled_trajectories_and_actions`, digest
`0f21b62e…` to `35d2928667a5086b224a35089a66ddc1c669108b729a8ae2ae519f3bc89d954e`),
`crates/v3-core/tests/baseline_worlds.rs` (`legacy_default_short_run_identity`,
hash `11216971115431479696` to `3381210231612339474`); each new value was
observed on two separate `cargo test -p v3-core --test <file>` runs before
the edit and re-confirmed by the full workspace run after the simplify pass.

Probe source:

```rust
//! TEMPORARY PROBE (not for commit): T11.F22 Task 1. Reproduces the research
//! note's cheap-proof `InputRef.Swap` trials (same seeds and selector as
//! `neighborhood::operators::per_operator_rows`) on dumped live genomes under
//! the live mutation config, and records for every applied pre-change swap
//! whether the old and new entries share a kind (`input_ref_kind`).
//! `c` = cross-kind changed trials / all changed trials; `B = 0.125 + 0.259 c`.
use std::collections::BTreeMap;

use rand::rngs::SmallRng;
use rand::SeedableRng;
use v3_core::config::SimulationConfig;
use v3_core::creature::genome::analysis::mesh_reachable_nodes;
use v3_core::creature::genome::CreatureGenome;
use v3_core::mutation::input_ref::{input_ref_kind, InputRefMutator, InputRefOperator};
use v3_core::mutation::reachability::TargetSets;
use v3_core::neighborhood::operators::INPUT_REF_SEED_BASE;
use v3_core::neighborhood::{classify, Battery, Class, EvalContext};

#[derive(Default, Debug)]
struct Cell {
    silent: u32,
    changed: u32,
    dead: u32,
}

#[test]
fn probe_swap_kind() {
    let dir = std::env::var("PETRI_GENOME_DIR").expect("PETRI_GENOME_DIR");
    let config_path = std::env::var("PETRI_CONFIG").expect("PETRI_CONFIG");
    let n: usize = std::env::var("PETRI_N").ok().and_then(|v| v.parse().ok()).unwrap_or(400);
    let trials: u32 = std::env::var("PETRI_TRIALS").ok().and_then(|v| v.parse().ok()).unwrap_or(20);
    let cfg_json: serde_json::Value =
        serde_json::from_str(&std::fs::read_to_string(&config_path).unwrap()).unwrap();
    let config: SimulationConfig = serde_json::from_value(cfg_json["config"].clone()).unwrap();
    let food_type_count = config.world.food.types.len();
    let battery = Battery::generate(food_type_count);
    let ctx = EvalContext::from_config(&config);
    let mut files: Vec<_> = std::fs::read_dir(&dir)
        .unwrap()
        .filter_map(|e| e.ok())
        .map(|e| e.path())
        .filter(|p| p.extension().is_some_and(|x| x == "json"))
        .collect();
    files.sort();

    let mut applied = 0u32;
    let mut skipped = 0u32;
    let mut identical = 0u32; // redraw produced the very same reference
    let mut within = Cell::default();
    let mut cross = Cell::default();
    // (old kind, new kind) -> outcome cell
    let mut by_pair: BTreeMap<(String, String), Cell> = BTreeMap::new();

    for (i, path) in files.iter().take(n).enumerate() {
        let v: serde_json::Value = serde_json::from_str(&std::fs::read_to_string(path).unwrap()).unwrap();
        let genome: CreatureGenome = serde_json::from_value(v["genome"].clone()).unwrap();
        let base = battery.signature(&genome, ctx.runtime, ctx.shared_memory_decay_rate);
        let reachable = mesh_reachable_nodes(&genome);
        let executed = battery.executed_indices(&genome, ctx.runtime, ctx.shared_memory_decay_rate);
        let targets = TargetSets::new(&reachable, &executed);
        let seed_offset = 1_000_000 * (i as u64 + 1);
        for trial in 0..trials {
            let seed = seed_offset + INPUT_REF_SEED_BASE + u64::from(trial);
            let mut rng = SmallRng::seed_from_u64(seed);
            let mut selector = targets.selector(config.mutation.reachable_bias.input_ref, config.mutation.executed_bias);
            let mut mutated = genome.clone();
            let result = InputRefMutator::apply_with_food_type_count(
                &mut mutated,
                InputRefOperator::Swap,
                &mut selector,
                &mut rng,
                &config.mutation,
                food_type_count,
            );
            if result.is_err() {
                skipped += 1;
                continue;
            }
            applied += 1;
            let mut diff = None;
            for (node_before, node_after) in genome.nodes.iter().zip(&mutated.nodes) {
                for (old, new) in node_before.input_refs.iter().zip(&node_after.input_refs) {
                    if old != new {
                        assert!(diff.is_none(), "more than one entry changed");
                        diff = Some((old.clone(), new.clone()));
                    }
                }
            }
            let Some((old, new)) = diff else {
                identical += 1;
                continue;
            };
            let old_kind = input_ref_kind(&old, &config.mutation);
            let new_kind = input_ref_kind(&new, &config.mutation);
            let signature = battery.signature(&mutated, ctx.runtime, ctx.shared_memory_decay_rate);
            let class = classify(&base, &signature).class;
            let bucket = if old_kind == new_kind { &mut within } else { &mut cross };
            let key = (
                format!("{:?},{}", old_kind.class, old_kind.width),
                format!("{:?},{}", new_kind.class, new_kind.width),
            );
            let pair = by_pair.entry(key).or_default();
            match class {
                Class::Silent => { bucket.silent += 1; pair.silent += 1; }
                Class::Changed => { bucket.changed += 1; pair.changed += 1; }
                Class::Dead => { bucket.dead += 1; pair.dead += 1; }
            }
        }
    }
    let changed_total = within.changed + cross.changed;
    let c = f64::from(cross.changed) / f64::from(changed_total);
    let b = 0.125 + 0.259 * c;
    println!("{}", serde_json::json!({
        "genomes": n.min(files.len()), "trials_per_genome": trials, "food_type_count": food_type_count,
        "applied": applied, "skipped": skipped, "identical_redraw": identical,
        "within": {"silent": within.silent, "changed": within.changed, "dead": within.dead},
        "cross": {"silent": cross.silent, "changed": cross.changed, "dead": cross.dead},
        "silent_frac": f64::from(within.silent + cross.silent + identical) / f64::from(applied),
        "changed_frac": f64::from(changed_total) / f64::from(applied),
        "dead_frac": f64::from(within.dead + cross.dead) / f64::from(applied),
        "c": c, "B": b,
    }));
    for ((old, new), cell) in &by_pair {
        println!("{}", serde_json::json!({"old": old, "new": new, "silent": cell.silent, "changed": cell.changed, "dead": cell.dead}));
    }
}
```

## T13.F05 recruitment-path family after the within-kind `Swap`

`crates/v3-core/src/neighborhood/recruitment_paths/qualification.rs`: the
two unprepared forms opened with a cross-kind `InputRef.Swap` (`FoodHere` to
`NeighborFoodRing`) that invariant 1 forbids. Both now open with
`InputRef.Add` of `NeighborFoodRing { 0 }` beside the copied `FoodHere`
(table `[FoodHere, NeighborFoodRing]`, step `ring_added`, neutral like the
blank forms' `cue_added`), then each consumer moves onto entry 1 on its own:
graph `RetargetGraphEdge` of the sensing leaf edge to
`InputLeaf { ref_idx: 1, sub_idx: 2 }`; VM `VmInstructionRawFieldMutation`
nudging `ReadInput.ref_idx` 0 to 1 (`read_ref_idx_1`) before the two
existing `sub_idx` nudges. The stale `FoodHere` entry stays. `MAX_PATH_EVENTS`
stays 6 and the nine form names stay.

| Form | Length | Gap | Lengthening step |
| --- | --- | --- | --- |
| `graph_blank` | 7 | 7 | activation (route swap after the six-event build) |
| `graph_copy` | 2 | none | — |
| `graph_split` | 2 | none | — |
| `vm_blank` | 7 | 7 | activation (route swap after the six-event build) |
| `vm_copy` | 2 | none | — |
| `graph_unprepared` | 6 | none | — |
| `vm_unprepared` | 7 | 7 | `read_ref_idx_1` (the read's `ref_idx` nudge onto the added ring) |
| `graph_detour` | 6 | none | — |
| `vm_detour` | 6 | none | — |

No six-event `vm_unprepared` route exists with the existing operators:
`nudge_u16` moves a raw field by one (`sub_idx` 0 to 2 is two events; the
`ref_idx` 0 to 1 move is a third) and `apply_constant_mutation` adds a draw
from `[-1, 1]` (the direction constant 0.0 cannot reach `[1.5, 2.5)` in one
event), so ring + `ref_idx` + 2 × `sub_idx` + 2 × constant + swap = 7 is the
minimum. It is recorded as
`GrowthGap { length: 7, lengthening_step: "read_ref_idx_1: ..." }`, the shape
of the two blank forms' gaps.

Seeds re-pinned by the existing first-accepted search
(`recruitment_paths_seed_search_finds_the_pinned_seeds`): `ring_added` 68 on
both forms, `read_ref_idx_1` 94; every later seed on both forms is unchanged
(`graph_unprepared` `[68, 32, 64, 6718, 4, 0]`, `vm_unprepared`
`[68, 94, 72, 223, 13, 13, 0]`). Qualified forms drop from seven to six
(`vm_unprepared` leaves), asserted in `tests.rs`
(`recruitment_paths_qualified_outcomes_and_seeds_are_pinned`) and in
`crates/v3-core/tests/recruitment_paths.rs`
(`recruitment_paths_qualified_paths_replay_through_the_production_operators`).
`cargo test -p v3-core --lib recruitment_paths`: 38 passed.


## Verification on the simplified feature tree (uncommitted, 2026-09-16)

- `cargo test --workspace`: every binary ok (v3-core lib 1553 passed, 2
  ignored; integration binaries all ok, 0 failed).
- `cargo clippy --workspace --all-targets`: no warnings.
- `cargo fmt --all --check`: exit 0.
- `cargo check --workspace --all-targets`: Finished.
- `make roadmap-check`: validation passed.
