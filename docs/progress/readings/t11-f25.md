# T11.F25 — Meaningful Action-Parameter Targets readings

Tables and transcripts for the feature spec
[`docs/specs/roadmap/t11-f25-meaningful-action-parameter-targets.md`](../../specs/roadmap/t11-f25-meaningful-action-parameter-targets.md).

## Spec challenge (Codex Astra `xhigh`, read-only)

| Round | Job | Finding | Severity | Disposition |
| --- | --- | --- | --- | --- |
| 1 | `task-mufs8ekz-5p6an1` | Equivalence claim ignores `AddGraphEdge` eligibility: an undecoded-parameter-only graph becomes ineligible and resamples before any surface draw (`mutation/graph/mod.rs:217`) | blocking | Fixed: an Eligibility row states where eligibility changes (no fixed-catalog graph); equivalence now excludes births that select `AddGraphEdge` or meet changed eligibility, and attribution includes the downstream trajectory |
| 1 | `task-mufs8ekz-5p6an1` | "Gate and goal one-edge census" is not a report field (`neighborhood/census.rs:179` is a standalone ignored test; founder half is `bench/indicators.rs:603`) | blocking | Fixed: the row is replaced by the actual founder-half fields (`mesh_execution`, `steering`, `reachable_node_count` unchanged) |
| 1 | `task-mufs8ekz-5p6an1` | Founder readings grouped too broadly; `operator_rows` move through direct trials; memory, temporal memory, structure size and `pass_cap_hits` missing | advisory | Adopted: `operator_rows` row (only `AddGraphEdge` moves, per-operator seeds), births row split, all nine `COUNTER_NAMES` listed, omitted goal indicators added |
| 1 | `task-mufs8ekz-5p6an1` | "Catalog order" is ambiguous for partial defs; `SinkInput(i)` indexes the actual vector | advisory | Adopted: filter in existing vector order, keep the original index, no RNG consumed with no candidate; tests cover partial and mixed lists |
| 1 | `task-mufs8ekz-5p6an1` | Raw-field Non-Goal says the nudge from a decoded slot always changes behavior | advisory | Adopted: "can remove that write's contribution to a decoded parameter" |
| 1 | `task-mufs8ekz-5p6an1` | Track Goal line does not name the natural analog | advisory | Dropped: the row is a correctness repair to the mutation operators, not a new mechanism, and the sibling repair T11.F24 closed with an analog-free row; the spec names the analog |
| 2 | `task-mufsfi00-n1un8h` | Round-1 findings 1–5 confirmed resolved; the drop of finding 6 accepted (the track note classifies F25 as a correctness repair) | — | Closed |

Round 2 verdict: `ready`. No blocking finding remains open.

## Build verification (2026-09-24)

Viability before the change: `cargo test -p v3-core --test viability` gave
`28 passed; 0 failed`.

Red run. With the catalog constant added and both draws still on the
pre-feature code, `cargo test -p v3-core --lib -- action_param undecoded
surface can_add_edge decoded_action` and `-- add_edge_wires_only` gave five
failures:

| Test | Pre-feature result |
| --- | --- |
| `pick_random_surface_draws_uniformly_over_compute_nodes_and_drawable_sinks` | a seed's surface differed from the one-draw pick over the 94-sink candidate list |
| `add_edge_wires_only_decoded_parameter_sinks` | an undecoded `ActionParam` sink was wired |
| `can_add_edge_agrees_with_the_surface_draw_on_partial_sink_lists` | `can_add_edge` was true on the undecoded-parameter-only def |
| `random_vm_instruction_write_action_param_reaches_every_decoded_slot` | drew an undecoded slot |
| `random_vm_instruction_never_draws_an_undecoded_action_param_slot` (proptest) | drew an undecoded slot |

The catalog tests (`decoded_action_params_flatten_to_slots_zero_five_and_seven`,
`decoded_action_param_catalog_matches_decode_commit`,
`undecoded_action_params_never_change_the_commit`) and the pruning
regressions (`remove_edge_prunes_an_edge_on_an_undecoded_parameter_sink`,
`vm_delete_instruction_removes_a_write_to_an_undecoded_param_slot`) pass on
both sides: they tie the catalog to the unchanged decoder and pin unchanged
pruning.

Green run. The same filters gave `31 passed; 0 failed`;
`cargo test -p v3-core --test viability` gave `28 passed; 0 failed`;
`cargo test -p v3-core --lib mutation` gave `424 passed; 0 failed; 1 ignored`.

### Re-pinned values

Attribution method: restoring the pre-feature graph surface draw alone
(`is_fresh_edge_sink` returning `true`, which restores the old candidate
count, draw and eligibility) with the VM slot draw kept restores every old
value below, so each move comes from births that make an `AddGraphEdge`
surface draw and their downstream trajectory.

| Test | Old | New |
| --- | --- | --- |
| `accounting_preserves_pre_feature_sampled_trajectories_and_actions` | `c04e289e…eafc95` | `dd28049c…0bee19` |
| `mutation_on_applied_trajectory_guard_is_pinned` | `03761998…9fb431` | `e0f75b16…29f37` |
| `legacy_default_short_run_identity` (`tests/baseline_worlds.rs`) | 7548337837478651677 | 7552184245034446484 |
| Recruitment paths `vote_edge_added` seed (`graph_blank`, `graph_detour`) | 57 | 102 |
| Recruitment paths `vote_edge_added` seed (`graph_copy`, `graph_split`, `graph_unprepared`) | 103 | 743 |

The recruitment seeds are the first accepted seed of the unchanged
`search_seeds`; the VM seeds did not move. The new digests reproduced on two
runs. No predicate changed.

### Gate severity

`gate_profile_has_no_severe_regression_against_series_references`
(`crates/v3-cli/tests/bench.rs`) fails: `pass_cap_hits` current 0.001364
against the T19.F04 epoch reference 0.000752 (+81.4%, severe). The same test
passes at `2a2371fa` and with only the graph surface draw restored, so the
move is attributable to the `AddGraphEdge` draw. Under the Performance
blocker rule this is a user decision.

### Benchmark specialist run (2026-09-24)

Commands run once each from the feature worktree with
`PATH=$HOME/.local/share/petri-tools/bin:$HOME/.local/share/aquaproj-aqua/bin:$PATH`;
`scripts/bench-wait` preflight reported no blockers on either run.

| Profile | Command | CLI exit | Outer (`make`) exit | Severe |
| --- | --- | --- | --- | --- |
| gate | `make bench PROFILE=gate FEATURE=t11-f25-meaningful-action-parameter-targets` | 3 (`crates/v3-cli/src/main.rs:574`, "severe work-counter regression against a stored reference") | `make: *** [bench] Error 3` (propagated from the recipe) | true |
| goal | `make bench PROFILE=goal FEATURE=t11-f25-meaningful-action-parameter-targets` | 3 (same source) | `make: *** [bench] Error 3` | true |

Raw reports (main checkout, ignored, not staged):
`/Users/istefanek/projects/petri/.bench-artifacts/t11-f25-meaningful-action-parameter-targets/gate.json`
(102,693 bytes, sha256 `86bef81d…664c3f9c2`) and
`.../goal.json` (654,640,601 bytes, sha256 `ca0e3add…9982263a0a15550`).

Committed summaries:
`docs/progress/features/t11-f25-meaningful-action-parameter-targets.json`
(105,634 bytes, sha256 `ccff5a23…86210a7b59`, `verified_at`
2026-09-24T17:55:43Z, `effective_config_digest`
`sha256:f351830c…49c32d184`) and
`docs/progress/features/t11-f25-meaningful-action-parameter-targets-goal.json`
(7,814,311 bytes, sha256 `56671259…56498cbdd`, `verified_at`
2026-09-24T18:12:39Z).

**Gate comparison.** Against T19.F04 epoch: `pass_cap_hits` 0.001364 vs
0.000752 (+81.38%, severe); `mesh_hops` +0.98%, `vm_steps` -14.86%,
`graph_relax_iters` +0.99%, `plasticity_updates` -21.38%, `actions_applied`
+0.25%, `births` -0.17%, `passes` +0.15%, `decided_passes` 0→0 — all `ok`.
Against T11.F24 latest closure: `pass_cap_hits` +34.25% (flag); all other
eight counters `ok`.

**Goal comparison.** Against T19.F04 epoch (goal-worlds-v1): `vm_steps`
2.403726 vs 1.024805 (+134.55%, severe), `decided_passes` 0.000148 vs
0.000092 (+60.87%, severe), `mesh_hops` +10.68% (flag), `plasticity_updates`
+42.41% (flag), `graph_relax_iters` +8.13%, `actions_applied` +2.18%,
`births` +1.15%, `pass_cap_hits` -14.40%, `passes` +1.76% — remainder `ok`.
Against T11.F24 latest closure: `vm_steps` +202.71% (severe), `decided_passes`
+34.55% (flag), `mesh_hops` +26.74% (flag), `graph_relax_iters` +20.19%
(flag), `plasticity_updates` +21.18% (flag), `actions_applied` +26.34%
(flag), `passes` +16.51% (flag); `births` +3.29% and `pass_cap_hits` -20.62%
`ok`. No extinction: `final_population` 4877 / 3221 / 1219 across the three
goal-worlds-v1 seeds, `extinction_tick` null on all three.
`neighborhood_evolved_wall_clock_ms_total` 5317.7255 ms (180 s cap, not
exceeded); `neighborhood_founder_wall_clock_ms` 280.056374 ms (10 s cap, not
exceeded); `wall_clock_ms_total` 464,452.27 ms across three seeds.

The goal severes (`vm_steps` and `decided_passes` against the T19.F04 epoch)
are accepted. User decision (verbatim, 2026-09-24): "Accept, keep epoch". The
goal series `epoch_baseline` stays at T19.F04.

**Series re-pin.** `docs/progress/benchmark-series.json` gate
`epoch_baseline` changed from
`docs/progress/features/t19-f04-vote-based-action-selection.json` to
`docs/progress/features/t11-f25-meaningful-action-parameter-targets.json`
(new summary also appended to the gate `closed` list); `goal_worlds`
`epoch_baseline` left at
`docs/progress/features/t19-f04-vote-based-action-selection-goal.json`
(new goal summary appended to its `closed` list only). Confirmation:
`cargo test -p v3-cli --test bench
gate_profile_has_no_severe_regression_against_series_references` — `ok`,
1 passed, 0 failed (6.87s).

### Workspace checks

`cargo test --workspace --no-fail-fast` and `make -k check` fail only on
the gate severity above; `cargo clippy --workspace --all-targets -- -D
warnings`, `cargo check --workspace --all-targets`, frontend checks,
dependency audit and `make roadmap-check` pass.
