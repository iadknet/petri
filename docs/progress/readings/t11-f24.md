# T11.F24 — Stable Parent Membership During Mutation readings

Tables and transcripts for the feature spec
[`docs/specs/roadmap/t11-f24-stable-parent-membership-during-mutation.md`](../../specs/roadmap/t11-f24-stable-parent-membership-during-mutation.md).

## Spec challenge (Codex Astra `xhigh`, read-only)

| Round | Job | Finding | Severity | Disposition |
| --- | --- | --- | --- | --- |
| 1 | `task-mufl19bo-mb1hsq` | "RNG and equivalence" promised identical draws after a removal, but corrected membership can move a draw onto the no-roll short-circuit (`reachability.rs` `select`) | blocking | Fixed: membership upkeep consumes no RNG and the selector algorithm is unchanged; post-removal draws may consume different RNG; equivalence kept for draws before the first removal; short-circuit transition added to the regressions |
| 1 | `task-mufl19bo-mb1hsq` | Rollback invariant not exercised (discard/retry and parseability rejection after a removal) | advisory | Adopted: regression task covers a discarded or rejected attempt after a removal |
| 1 | `task-mufl19bo-mb1hsq` | `next_node_id` probes from max+1, so reuse is conditional; a birth-wide allocator from parent max+1 would not change addition-only births | advisory | Adopted: code claim corrected, allocator row re-grounded (genome id change out of scope), sparse-id fixture added |
| 2 | `task-mufl6lkl-zleh02` | Round-1 findings 1–3 confirmed resolved | — | Closed |
| 2 | `task-mufl6lkl-zleh02` | `docs/reference/v3-mutation-spec.md` "Executed layer" item 2 documents the stale-index behavior and exempts the drift harness | advisory | Adopted: Implementation Tasks now replace that sentence with the within-birth membership rule |

Round 2 verdict: `ready`. No blocking finding remains open.
