# T11.F24 — Stable Parent Membership During Mutation readings

Tables and transcripts for the feature spec
[`docs/specs/roadmap/t11-f24-stable-parent-membership-during-mutation.md`](../../specs/roadmap/t11-f24-stable-parent-membership-during-mutation.md).

## Spec challenge (Codex Astra `xhigh`, read-only)

| Round | Job | Finding | Severity | Disposition |
| --- | --- | --- | --- | --- |
| 1 | `task-mufl19bo-mb1hsq` | "RNG and equivalence" promised identical draws after a removal, but corrected membership can move a draw onto the no-roll short-circuit (`reachability.rs` `select`) | blocking | Fixed: membership upkeep consumes no RNG and the selector algorithm is unchanged; post-removal draws may consume different RNG; equivalence kept for draws before the first removal; short-circuit transition added to the regressions |
| 1 | `task-mufl19bo-mb1hsq` | Rollback invariant not exercised (discard/retry and parseability rejection after a removal) | advisory | Adopted: regression task covers a discarded or rejected attempt after a removal |
| 1 | `task-mufl19bo-mb1hsq` | `next_node_id` probes from max+1, so reuse is conditional; a birth-wide allocator from parent max+1 would not change addition-only births | advisory | Adopted: code claim corrected, allocator row re-grounded (genome id change out of scope), sparse-id fixture added |
| 2 | `task-mufl6lkl-zleh02` | Round-1 findings 1–3 confirmed resolved | — | Closed |
| 2 | `task-mufl6lkl-zleh02` | `docs/reference/v3-mutation-spec.md` "Executed layer" item 2 documents the stale-index behavior and exempts the drift harness | advisory | Adopted: Implementation Tasks now replace that sentence with the within-birth membership rule |

Round 2 verdict: `ready`. No blocking finding remains open.

## Build verification (2026-09-24)

Red run. The six regressions in `crates/v3-core/src/mutation/engine/membership_tests.rs`
were run with the `membership.observe_event` call disabled, which reproduces
the pre-feature engine exactly (index sets fixed for the whole birth):
`cargo test -p v3-core --lib mutation::engine::membership_tests` gave
`0 passed; 6 failed`.

| Test | Pre-feature result |
| --- | --- |
| `a_carried_node_after_the_removed_index_keeps_its_membership` | seed 175: C after B's removal recorded `Applied(Unreachable)`, expected `Applied(Reachable)` |
| `sparse_ids_keep_membership_through_a_removal` | seed 175: `Applied(Unreachable)`, expected `Applied(Reachable)` |
| `a_node_created_on_a_removed_id_and_index_is_never_a_member` | node created on C's id and index recorded `Applied(Reachable)`, expected `Applied(Unreachable)` |
| `a_discarded_or_skipped_attempt_after_a_removal_leaves_membership_unchanged` | C recorded `Some(Unreachable)`, expected `Some(Reachable)` |
| `an_all_executed_eligible_set_after_a_removal_takes_the_no_roll_short_circuit` | seed 53: executed-bias-1.0 birth differs from the executed-layer-free birth (the executed roll fired) |
| `applied_targets_follow_the_birth_membership_rule` (proptest) | minimal case `genome_seed = 5947878855455386925, growth = 4, events = 5`: executed-target count 3, rule predicts 2 |

Green run, in order:

| Command | Result |
| --- | --- |
| `cargo test -p v3-core --test viability` | 28 passed, 0 failed |
| `cargo test -p v3-core --lib mutation` | 417 passed, 0 failed, 1 ignored |
| `cargo check --workspace --all-targets` | exit 0 |
| `cargo clippy --workspace --all-targets -- -D warnings` | exit 0 |
| `cargo test --workspace` | every suite ok, 0 failed |

Re-pins: none. No pinned trajectory, replay, recruitment-paths, or drift value
changed under `cargo test --workspace`, so no old/new values are listed.

Written with the implementation, green only: the `reachability.rs` proptest
`birth_membership_follows_provenance_not_position` (tagged-provenance oracle
over sparse shuffled ids and removal/append/unchanged scripts), the
equivalence proptest `births_without_a_draw_after_a_removal_are_unchanged`
(against the test-only `FROZEN_MEMBERSHIP` pre-feature oracle), and
`every_topology_event_removes_one_node_or_appends_fresh_ones`, which pins the
within-event identity for all 13 topology operators.
