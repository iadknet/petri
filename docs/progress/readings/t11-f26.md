# T11.F26 readings

## Spec challenge (Codex Astra `gpt-6-astra` `xhigh`, read-only)

| Round | Job | Severity | Finding (short) | Disposition |
| --- | --- | --- | --- | --- |
| 1 | `task-mug3jfef-s946n6` | blocking | Computation/state comparison omits applied output/parameter values and graph runtime state; `unresolved` could hide them | Fixed: two named records (computation incl. applied sink/parameter values; state-and-cost incl. graph runtime state aligned by node id); fixtures for graph-state-only and decoded-parameter masking |
| 1 | `task-mug3jfef-s946n6` | blocking | Recorded contexts must assemble typed local food unconditionally too; `neighborhood-v1` already populates typed local food | Fixed: both families assembled unconditionally with a donor/tested-genome fixture; authored groups corrected to channels `neighborhood-v1` holds at zero |
| 1 | `task-mug3jfef-s946n6` | blocking | Closure rule forces "minor" on short/missing evidence; fixed 5-parent plurality; actionless-on-both needs parents evaluated on the extension | Fixed: per-candidate denominators, "insufficient evidence" state, plurality as 25% of evaluated parents, every cohort parent evaluated on the extension |
| 1 | `task-mug3jfef-s946n6` | advisory | Authored contexts and sequence construction not frozen | Adopted: deterministic authored rule and sequence index rule, version bump on change, pinned in tests |
| 1 | `task-mug3jfef-s946n6` | advisory | 616 s is simulation time, not end-to-end goal time | Adopted: 15-minute check on end-to-end process time; strata cost stays in drift/read timers |
| 1 | `task-mug3jfef-s946n6` | advisory | Page does not require operator/target breakdown | Adopted: per-cohort operator and target-class detail table |
| 2 | `task-mug3qkp5-l9dgpl` | confirmed | Round-1 blocking findings 1–3 | Confirmed resolved by Codex |
| 2 | `task-mug3qkp5-l9dgpl` | blocking | "State or cost without action" counted only `state_or_cost_only`, missing silent proposals whose computation and state both change | Fixed: independent `silent_with_state_or_cost` count over action-silent applied proposals drives the closure row and page; fixture for a computation-and-state pair |
| 2 | `task-mug3qkp5-l9dgpl` | advisory | Operator rows need an accepted-operator rule and a `domain_exhausted` bucket for `operator: None` | Adopted, with reconciliation to proposals and skips |
| 3 | `task-mug3vot7-dz3lej` | confirmed | Round-1 blocking findings 1–3 and round-2 blocking finding | Confirmed resolved; no new findings; `verdict: ready` |

## Implementation verification (2026-09-24)

| Check | Command | Result |
| --- | --- | --- |
| Core unit tests | `cargo test -p v3-core --lib neighborhood`; `cargo test -p v3-core --lib full_sensor` | 208 passed, 3 ignored; 1 passed; 0 failed |
| CLI tests | `cargo test -p v3-cli` | lib 130, main 12, `bench` 20, `bench_artifacts` 25 (1 ignored), `cli` 12; 0 failed |
| Build, clippy and format | `cargo check --workspace --all-targets`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo fmt --all` | clean |
| Page fixtures | `node --test scripts/benchmark-artifacts.test.mjs` | 5 passed, 0 failed |
| Completion gate | `make check` | exit 0 (2,183 Rust tests, 0 failed) |
| Browser, block absent | page served from `docs/progress/` at `http://127.0.0.1:8793/`, inspected with `agent-browser` | the seven new cards render in section 4 with "not measured" rows, since no stored summary carries the block yet: [`t11-f26-page-unmeasured.png`](t11-f26-page-unmeasured.png). Inspection of the measured view waits for the goal summary |

Viability was not run first: no production default, founder, or tick-loop
mechanic changed (`assemble_sensor_inputs` keeps its predicates through a
shared helper).

## Focused tests

Core tests are in `crates/v3-core/src/neighborhood/mutation_effects/` unless
named otherwise.

| Requirement | Test |
| --- | --- |
| Strata reconcile with the tallies they sit beside | `births::tests::exposure_reconciles_with_the_birth_result_it_is_folded_beside`; proptest `births::tests::exposure_partitions_births_and_reconciles_with_the_fold`; `drift::tests::checkpoints_replay_the_production_walk_and_fixed_birth_subset`; CLI `bench::mutation_effects::tests::world_set_cases_carry_reconciled_mutation_effects_on_the_existing_readings` |
| Category partition and row reconciliation | `tests::a_world_reading_reconciles_and_is_identical_across_thread_counts`; proptest `tests::effect_counts_fold_is_a_partition_under_any_grouping` |
| One pair per category | `tests::action_changes_and_deaths_take_precedence`, `an_identical_child_is_genome_identical`, `a_nan_payload_edit_is_genome_identical`, `an_edit_to_a_never_dispatched_node_is_unexecuted`, `a_changed_vote_with_the_same_action_is_masked_before_selection`, `a_graph_state_only_change_is_state_or_cost_only`, `a_dispatched_edit_nothing_records_is_unresolved` |
| Graph parameter decoded to the same action | `tests::a_graph_parameter_decoded_to_the_same_action_is_masked_before_selection` |
| Computation and state both differ with identical actions | `tests::computation_and_state_changes_count_masked_and_silent_with_state` |
| Genome content excluded from the computation record | `tests::copied_genome_content_is_not_part_of_the_computation_record` |
| State record: bits, one-sided nodes, placeholders, node-id alignment | `records::tests::empty_placeholders_hold_no_state_but_a_stored_zero_does`, `a_node_present_on_one_side_differs_exactly_when_it_stores_a_scalar`, `graph_state_aligns_by_node_id_not_mesh_index` |
| Operator and `domain_exhausted` rows; first qualifying pairs | `tests::exhausted_domains_count_as_skips_and_pairs_are_the_first_qualifying_proposals` |
| Traced actions equal `Battery::signature` | `tests::traced_battery_actions_equal_the_battery_signature` |
| The three controls | `tests::controls_pass_on_the_authored_extension` (the integrator differs only in ticks 5–32) |
| Coverage union and state-only counts | `tests::a_pair_counts_once_in_the_union_and_state_only_needs_no_action_difference` |
| Unconditional recorded-context assembly | `simulation::tick::tests::full_sensor_inputs::full_assembly_matches_production_for_a_genome_reading_every_input`; `tests::recorded_contexts_assemble_perception_whatever_the_donor_reads` |
| Genome identity | `tests::genome_identity_distinguishes_negative_zero_and_ignores_nan_payloads_and_birth_weights`; `tests::an_entry_change_marks_both_entries_edited` |
| Zero denominators and extinct worlds | `tests::plurality_is_undefined_without_applied_proposals_and_breaks_ties_by_precedence`; `tests::an_extinct_world_leaves_the_selected_cohort_and_recorded_group_undefined` |
| Byte identity across thread counts | `tests::a_world_reading_reconciles_and_is_identical_across_thread_counts` (1 vs 4 threads); CLI `bench::mutation_effects::tests::world_set_blocks_are_byte_identical_across_thread_counts` (1 vs 3) |
| Pinned contexts | `tests::the_recorded_sample_is_a_seeded_draw_in_draw_order` (positions `[4, 2, 6, 9]` of 10 at world seed 11); `tests::authored_contexts_set_only_their_channel_group_in_production_ranges` (context 0 barriers, context 1 outcome bits); `tests::sequences_cycle_recorded_contexts_or_fall_back_to_authored` |
| Summary projection and historical reports | `tests/bench_artifacts.rs::world_mutation_effects_is_projected_whole`; CLI `bench::mutation_effects::tests::a_report_without_the_block_reads_back_unmeasured` |
| Page view: counts, ratios, historical, zero-denominator, short-sample | `scripts/benchmark-artifacts.test.mjs`: "mutation-effects view shows counts and ratios over their stored denominators", "mutation-effects view reads historical, zero-denominator and short-sample blocks honestly" |

## Benchmark specialist (2026-09-24/25)

Worktree `.claude/worktrees/t11-f26`, commit `01e9c277`. Both profiles run
once, sequentially, no competing builds/tests/servers.

| Profile | Command | CLI exit | `severe` | References |
| --- | --- | --- | --- | --- |
| gate | `make bench PROFILE=gate FEATURE=t11-f26-mutation-effect-attribution-and-observation-coverage` | 0 | false (vs T11.F25 gate epoch and T11.F27 previous closure) | T11.F25, T11.F27 |
| goal | `make bench PROFILE=goal FEATURE=t11-f26-mutation-effect-attribution-and-observation-coverage` | 0 | false (vs T11.F27, previous closure and goal-worlds epoch) | T11.F27 |

All counters (`mesh_hops`, `vm_steps`, `graph_relax_iters`,
`plasticity_updates`, `actions_applied`, `births`, `pass_cap_hits`,
`passes`, `decided_passes`) show delta% 0.000000 in both profiles.

Deterministic-block equality (predeclared): gate `deterministic` byte-for-byte
identical to T11.F27's (`jq -S '.deterministic'` diff empty). Goal
`deterministic` identical to T11.F27's after
`.cases[] |= del(.mutation_effects)` (`jq -S` diff empty).

Timers (`environment` block of the goal raw/summary report):

| Timer | T11.F26 | T11.F27 | Delta | Cap/band |
| --- | --- | --- | --- | --- |
| `mutation_effects_wall_clock_ms_total` | 7,864.65 ms | n/a (new) | — | expected <20 s, cap 60 s — met |
| `drift_depth_wall_clock_ms` | 17,395.64 ms | 18,242.30 ms | −4.6% | within ±10% |
| `neighborhood_read_wall_clock_ms_total` | 1,622.90 ms | 2,079.80 ms | −22.0% | **outside ±10%** (faster); hard cap 10 s still met |
| `neighborhood_founder_wall_clock_ms` | 324.96 ms | 339.45 ms | — | cap 10 s, met |
| `neighborhood_evolved_wall_clock_ms_total` | 5,288.63 ms | 7,008.79 ms | — | cap 180 s, met |

End-to-end goal process time: 684 s wall (command start to exit, including a
0.15 s cached build), against the 900 s (15-minute) threshold. Internal
simulation `wall_clock_ms_total` 630,602.69 ms + timed observations
(drift 17.4 s, founder 0.32 s, evolved 5.29 s, read 1.62 s, mutation_effects
7.86 s, final_state 0.85 s ≈ 33.3 s) ≈ 664 s.

Summary size: goal summary 8,087,659 bytes vs T11.F27's 7,810,874 bytes,
+276,785 bytes (≈270.3 KB), within the ≤400 KB cap. Gate summary 105,830
bytes (raw 102,830 bytes).

Byte counts and hashes (local raw, `.bench-artifacts/`, ignored; committed
summaries under `docs/progress/features/`):

| File | Bytes | SHA-256 |
| --- | --- | --- |
| raw gate | 102,830 | `23074b6d...78fb7ca` |
| summary gate | 105,830 | `324a5851...a73c6ffab` |
| raw goal | 642,824,715 | `811feaa2...ff336cb7` |
| summary goal | 8,087,659 | `1f0c66e4...db85301` |

Verification time: 2026-09-25T00:27:39Z (goal `generated_at`, matches
`git_revision` `01e9c27769df267a44999dab6317a227e66f5e48`).

Sanity readings (predeclared, informational): drift@2000
`parents_all_noop` 11/20 in all three worlds (55%, "about half"); action
effects (`from_acting.changed` / `applied_events_total`) at drift@2000:
Orchards 3/985 (0.3%), Canyon 4/985 (0.4%), Confluence 3/985 (0.3%) — same
order of magnitude as the 1% pilot figure, on the low side. Selected cohort
(`selected-read`) has fewer/no `parents_all_noop` (4/50, 0/50, 1/50) and more
action effects (18.2%, 20.3%, 17.0%) than drift@2000, consistent with the
predeclared expectation. A large `unresolved` share was not separately
tallied here; it belongs to the closure reading's bottleneck classification,
not this benchmark record.

Series entries added: `docs/progress/benchmark-series.json`
`gate.closed` and `goal_worlds.closed` each append this feature's summary
path. No new full report staged; local raw artifacts remain under
`.bench-artifacts/` only.

**Exception reported, not resolved.** The read timer moved −22.0% against
T11.F27, outside the predeclared ±10% band (faster, not slower). Every hard
cap (founder 10 s, evolved 180 s, read 10 s, `mutation_effects` 60 s) is met
with wide margin, and no threshold, baseline, or code was changed to produce
or absorb this reading.
