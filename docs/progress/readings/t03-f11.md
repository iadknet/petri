# T03.F11 — Genome Replication Cost readings

Verification results for the feature spec
[`docs/specs/roadmap/t03-f11-genome-replication-cost.md`](../../specs/roadmap/t03-f11-genome-replication-cost.md).
Build pass run from the worktree `.claude/worktrees/t03-f11` on 2026-09-14.

## Build-pass commands

| Command | Result |
| --- | --- |
| `cargo test -p v3-core --test viability` (run first) | 25 passed, 0 failed |
| `cargo test -p v3-core --lib replication_multiplier` | 7 passed (4 unit, 3 proptest) |
| `cargo test -p v3-core --lib -- replication_factor rate_zero_charges founder_reproduce_charge` | 3 passed |
| `cargo test -p v3-core` | lib 1479 passed / 2 ignored; every integration target ok after the `applied_trajectory` re-pin |
| `cargo test -p v3-core --test applied_trajectory` (twice) | digest `96497a4f…2fa9` on both runs, re-pinned |
| `cargo test -p v3-core --test baseline_worlds` | 19 passed; `legacy_default_short_run_identity` hash unchanged |
| `cargo test -p v3-cli --test bench_artifacts checked_in_goal_recipe_identities` | ok after the three digests were re-pinned |
| `cargo test -p v3-cli -p v3-server` | all targets ok (105 / 96 / 46 / 21 / 20 / 11 / 11 / 2 passed) |
| `cargo fmt --all -- --check` | clean |
| `cargo check --workspace --all-targets` | clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | clean |
| `frontend`: `npx tsc --noEmit -p .` | exit 0 |
| `frontend`: `npx biome check src` | 4 pre-existing warnings, none in touched files |
| `frontend`: `npx vitest run src/components/ControlBar.test.tsx` | 11 passed |

## Focused tests

| Test | Location | Checks |
| --- | --- | --- |
| `the_canonical_founder_genome_is_one_hundred_eleven_units` | `crates/v3-core/src/creature/state.rs` | founder `genome_size()` is 111 and equals `FOUNDER_GENOME_SIZE_UNITS` |
| `replication_multiplier_is_exactly_one_for_the_founder` | `crates/v3-core/src/simulation/actions/reproduction.rs` | factor 1.0 at 111 units |
| `replication_multiplier_is_exactly_one_below_the_founder_anchor` | same | factor 1.0 at 0, 1, 7, 110 units |
| `replication_multiplier_is_exactly_one_at_rate_zero` | same | factor 1.0 at rate 0.0 for 0, 111, 386, 10,000 units |
| `replication_multiplier_charges_the_reference_genome_28_5_times` | same | 386 units at rate 0.1 gives 28.5 |
| `replication_multiplier_is_at_least_one` (proptest) | same | factor >= 1 for every rate in [0, 10) and every `u32` size |
| `replication_multiplier_is_non_decreasing_in_size` (proptest) | same | factor(lo) <= factor(hi) for every ordered pair of sizes |
| `replication_multiplier_is_exactly_one_at_or_below_the_anchor` (proptest) | same | factor == 1.0 for every size in 0..=111 |
| `parent_above_the_founder_anchor_pays_the_replication_factor_and_child_gets_the_transfer` | `crates/v3-core/src/simulation/tick/tests/actions.rs` | 300-`Noop` VM parent pays `base × factor`, `action_charges.reproduce` records it, child receives the unchanged transfer |
| `rate_zero_charges_an_oversized_parent_exactly_the_base_charge` | same | rate 0.0 leaves the oversized parent's energy bit-identical to the base charge path |
| `founder_reproduce_charge_is_bit_identical_with_and_without_the_replication_rate` | same | founder parent energy, charge, and child energy identical at rate 0.1 and 0.0 |

## Re-pins

| Pin | Old | New | Reason |
| --- | --- | --- | --- |
| `applied_trajectory.rs` digest | `86eee62c…7cec` | `96497a4f…2fa9` | forced-mutation 64-tick run has parents above 111 units reproducing; two runs agreed |
| `bench_artifacts.rs` orchards-in-grassland | `sha256:f381535d…0e64e` | `sha256:f1b9fe5a…295ab` | new config field enters `config_digest` |
| `bench_artifacts.rs` canyon-country | `sha256:197b1647…a8370` | `sha256:9c8fcf58…87e58` | same |
| `bench_artifacts.rs` confluence | `sha256:0922635c…f0933` | `sha256:a870b08a…24250` | same |
| `baseline_worlds.rs` short-run identity | `1397923697343438469` | unchanged | no child reaches `min_reproduce_age` inside the 30-tick window |

## Self-review pass

The `simplify` pass (2026-09-14) hoisted the two function-local founder
imports in `crates/v3-core/src/simulation/tick/tests/actions.rs` to the
module imports and dropped a redundant creature-count assert after the
`Spawned` check in `charged_reproduction`; production code, the multiplier
arithmetic, and every pinned digest are untouched.

| Command | Result |
| --- | --- |
| `cargo fmt --all -- --check` | clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | clean |
| `cargo test -p v3-core` | lib 1479 passed / 2 ignored; `applied_trajectory` 1, `baseline_worlds` 19, `viability` 25, every other integration target ok |

## Benchmark specialist readings (2026-09-15)

Worktree `.claude/worktrees/t03-f11`, HEAD `6a8879cb`, sequential, nothing
concurrent. First attempt blocked on a live user `scripts/dev.sh` session
(`v3-server`/vite preview, PID 50123 tree); resumed once the coordinator
confirmed the dev stack was stopped and `pgrep` found no `v3-server`,
`v3-cli`, or `cargo-mutants` on the host. First gate invocation used
`--feature t03-f11` (wrong, non-standard filename); cleaned up before
recording and rerun with the full slug `t03-f11-genome-replication-cost`
matching the directory convention.

### Gate profile

`make bench PROFILE=gate FEATURE=t03-f11-genome-replication-cost` — exit 0
(`v3-cli bench` inner process, `cargo run --release` wrapper, `make`
outer status all 0; source: captured stdout/stderr of the backgrounded
command).

- Raw: `/Users/istefanek/projects/petri/.bench-artifacts/t03-f11-genome-replication-cost/gate.json`, 94,169 bytes.
- Summary: `docs/progress/features/t03-f11-genome-replication-cost.json`, 96,978 bytes.
- `severe=false` against both references.

| Counter | vs `remove-complementary-nutrition` (gate epoch) | vs `t11-f19-per-unit-mutation-supply` | Level |
| --- | --- | --- | --- |
| mesh_hops | +0.065014% | +0.063484% | ok |
| vm_steps | -1.504229% | -0.138560% | ok |
| graph_relax_iters | -0.008541% | +0.038602% | ok |
| plasticity_updates | -15.495479% | -6.793049% | ok |
| actions_applied | -0.405088% | -0.104978% | ok |
| births | +0.451830% | +0.466836% | ok |

No flag, no severe on either comparison.

### Goal profile

`make bench PROFILE=goal FEATURE=t03-f11-genome-replication-cost` — exit 0
(same three-layer status chain, source as above). Ran once, per the
workflow's second-goal-run policy (cross-process reproducibility already
covered by `crates/v3-core/tests/reproducibility.rs` in `make check`).

- Raw: `/Users/istefanek/projects/petri/.bench-artifacts/t03-f11-genome-replication-cost/goal.json`, 511,836,405 bytes (local only, not staged).
- Summary: `docs/progress/features/t03-f11-genome-replication-cost-goal.json`, 6,790,293 bytes.
- `severe=false` against `t11-f19-per-unit-mutation-supply-goal`.

| Counter | vs T11.F19 goal | Level |
| --- | --- | --- |
| mesh_hops | -2.314486% | ok |
| vm_steps | -0.154664% | ok |
| graph_relax_iters | -2.656709% | ok |
| plasticity_updates | -57.029453% | ok |
| actions_applied | -1.930031% | ok |
| births | -9.816509% | ok |

All decreases; the comparator's flag/severe thresholds (+10%/+50%) are a
ceiling on increases, so none trip. No direction was predeclared for the
work counters; `plasticity_updates` at -57% is the largest move and is
reported per the predeclaration (composition, not compute — not
investigated further here).

Observation budgets (all pass):

| Budget | Cap | Measured |
| --- | --- | --- |
| Founder neighborhood | < 10 s | 128.188752 ms |
| Evolved neighborhood, summed | < 180 s | 880.845708 ms |
| Neighborhood read, summed | < 10 s | 342.829166 ms |
| Drift walk | < 30 s | 22,716.368584 ms |
| Goal run total | < 15 min (900 s) | 711,927.462084 ms (≈ 11.87 min) |

### Persistence re-read (goal profile)

| World (seed) | Final | Minimum | Plateau | Extinct? |
| --- | --- | --- | --- | --- |
| Orchards (11) | 9,772 | 1,194 | 6,307.646 | No |
| Canyon (22) | 11,534 | 4,152 | 10,254.156 | No |
| Confluence (33) | 14,110 | 1,010 | 11,324.096 | No |

Reference (no direction predeclared): T11.F19 final 13,808 / 10,546 / 13,789,
plateau 10,244.8 / 9,985.4 / 13,547.8; T11.F04 `w1600` final 11,610 / 10,398 /
11,093. All three worlds persist; every final and plateau reading is at or
below T11.F19's (reported, not a threshold).

### Neighborhood-read guard (goal profile)

| World | `changed_per_all_births` | floor (T14.F12) | `dead_per_all_births` | `mean_executed_nodes` | `mean_reachable_nodes` | `mean_total_nodes` | `mean_genome_size` (neighborhood) | `mean_generation` |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Orchards | 0.207200 | 0.146400 | 0.000200 | 2.960000 | 4.040000 | 4.920000 | 176.820000 | 46.620000 |
| Canyon | 0.196400 | 0.130000 | 0.008000 | 3.260000 | 4.100000 | 5.100000 | 193.240000 | 46.560000 |
| Confluence | 0.201400 | 0.143400 | 0.001200 | 2.720000 | 3.360000 | 4.500000 | 197.160000 | 48.740000 |

Verdicts: `changed_per_all_births` above its floor on every world (strict
pass). `mean_executed_nodes` above 2.5 on every world (Confluence lowest at
2.72, still above the floor and above T11.F19's floor-check condition since
it never drops below 2.5). `dead_per_all_births` below the 0.0150
investigation threshold on every world (max 0.0080, Canyon).

Against T11.F19 (0.278000 / 0.243600 / 0.274000 changed; 3.10 / 3.34 / 3.02
executed nodes; 0.0066 / 0.0032 / 0.0104 dead), all three worlds read lower —
reported with no direction per the predeclaration (fewer carried units under
the per-unit supply mean fewer events per birth; the brake, not a delivery
failure).

### Goal terminal `mean_genome_size` / `mean_mesh_nodes` (tick 2,000, population-persistence samples)

| World | Terminal `mean_genome_size` | vs T11.F19 (340.9/300.4/309.0) | Terminal `mean_mesh_nodes` | vs T11.F19 (6.91/6.26/7.06) |
| --- | --- | --- | --- | --- |
| Orchards | 180.782542 | 0.530 (down) | 4.958862 | 0.718 (down) |
| Canyon | 195.926131 | 0.652 (down) | 5.063291 | 0.809 (down) |
| Confluence | 193.837066 | 0.627 (down) | 4.423600 | 0.627 (down) |

All three down, none above the 1.10x investigation ceiling — brake reads as
biting harder than T11.F19 alone did, as expected (direction down or flat was
predeclared).

### Goal `reachable_structure_size_distribution` (reported, no direction)

| World | min | p25 | median | p75 | max | mean |
| --- | --- | --- | --- | --- | --- | --- |
| Orchards | 42 | 71 | 82 | 99 | 761 | 89.430209 |
| Canyon | 1 | 71 | 78 | 93 | 361 | 87.450754 |
| Confluence | 1 | 71 | 78 | 92 | 404 | 87.435932 |

### Goal `action_charges.reproduce` per world

| World | Value | vs T11.F19 (67,120.8/78,802.8/90,566.9) |
| --- | --- | --- |
| Orchards | 226,705.415108 | 3.38x |
| Canyon | 393,093.039754 | 4.99x |
| Confluence | 355,780.381365 | 3.93x |

Up on every world — the predeclared applied-behavior evidence that the
charge is active in all three standard worlds.

### Founder neighborhood, drift-walk, `recruitment_paths` blocks vs T11.F19 goal

Compared field-for-field against the local T11.F19 goal raw report
(`/Users/istefanek/projects/petri/.bench-artifacts/t11-f19-per-unit-mutation-supply/goal.json`):

- `mutational_neighborhood` (founder): byte-identical.
- `drift_depth` (all three cases): byte-identical.
- `recruitment_paths`: byte-identical excluding the `config`/`config_digest`
  echo fields, which differ only because the applied recipe now carries
  `genome_replication_cost_per_unit` (0.1 vs absent in T11.F19); no
  reproduction action runs inside this block, so this is the expected config
  echo, not a behavior difference.

### Paired 12,000-tick Orchards run, seed 11 — truncated per the 2026-09-15 user decision

The control arm was stopped by the orchestrator at 11:59:02 local time,
2026-09-15, on the user's decision recorded in the spec (the pair exceeded
its 3-hour budget; population/genome growth on this substrate, not the
host). **Every reading below that cites the control arm is truncated at
tick 4,500 (the control's last sample); the cost arm is read in full to
tick 12,000.**

Artifacts under `docs/progress/sweeps/t03-f11/`: `cost.ndjson` (103,024
bytes, 24 tick samples to 12,000), `control.ndjson` (37,972 bytes, 9 tick
samples to 4,500), `control-recipe.json` (2,075 bytes), `cost-config.json`
(6,647 bytes), `control-config.json` (6,647 bytes), `timing.txt` (108
bytes), `run.sh` (1,052 bytes, POSIX `sh`).

`timing.txt`:
```
cost wall_seconds=8506
control wall_seconds=10674 (stopped at tick 4500 by user decision, 2026-09-15 11:59)
```

The control line was written by the orchestrator at the stop, not by
`run.sh`; the cost line is `run.sh`'s.

Applied-config diff: `cost-config.json` vs `control-config.json` differ in
exactly one field, `energy.lifecycle.genome_replication_cost_per_unit`
(0.1 -> 0.0); confirmed by a full flattened-key diff.

Neither arm extinct at any sample (population > 0 throughout both).

Cost-vs-control comparison at the common last control tick (4,500,
**truncated**):

| Arm | `mean_genome_size` | `mean_mesh_nodes` | `mean_generation` | population |
| --- | --- | --- | --- | --- |
| Cost (rate 0.1) | 502.882161 | 11.650231 | 103.855639 | 82,952 |
| Control (rate 0.0) | 2,074.590127 | 59.780320 | 90.548680 | 78,883 |

The cost arm's `mean_genome_size` and `mean_mesh_nodes` are strictly below
the control's at the common tick (4.12x and 5.13x smaller respectively),
consistent with the charge acting as a brake; the cost arm is also at a
higher `mean_generation` (103.9 vs 90.5) at that tick, i.e. more
generations turned over per unit of carried structure under the charge.

Cost-arm full per-sample table (ticks 500-12,000, complete, not truncated):

| tick | population | mean_genome_size | mean_mesh_nodes | mean_generation | units/gen `(size-111)/gen` | node slope `(nodes-2)/gen` |
| --- | --- | --- | --- | --- | --- | --- |
| 500 | 15,294 | 133.3186 | 2.7961 | 11.7782 | 1.8949 | 0.0676 |
| 1,000 | 2,477 | 164.2071 | 3.5075 | 24.6887 | 2.1551 | 0.0611 |
| 1,500 | 4,562 | 182.2567 | 4.1320 | 34.8490 | 2.0447 | 0.0612 |
| 2,000 | 9,772 | 180.7825 | 4.9589 | 46.3067 | 1.5070 | 0.0639 |
| 2,500 | 26,306 | 181.7813 | 5.2675 | 58.3725 | 1.2126 | 0.0560 |
| 3,000 | 25,121 | 205.4578 | 5.9973 | 67.9507 | 1.3901 | 0.0588 |
| 3,500 | 34,918 | 340.9669 | 8.0825 | 79.3468 | 2.8982 | 0.0767 |
| 4,000 | 80,131 | 473.5279 | 10.4444 | 93.3290 | 3.8844 | 0.0905 |
| 4,500 | 82,952 | 502.8822 | 11.6502 | 103.8556 | 3.7733 | 0.0929 |
| 5,000 | 84,114 | 506.2827 | 13.3475 | 114.7887 | 3.4436 | 0.0989 |
| 5,500 | 84,451 | 484.6972 | 13.4710 | 125.5690 | 2.9760 | 0.0914 |
| 6,000 | 81,720 | 477.0411 | 14.2901 | 136.0491 | 2.6905 | 0.0903 |
| 6,500 | 81,562 | 491.1448 | 15.7468 | 146.9846 | 2.5863 | 0.0935 |
| 7,000 | 79,702 | 491.9102 | 16.1565 | 157.5375 | 2.4179 | 0.0899 |
| 7,500 | 78,036 | 481.5727 | 16.0788 | 167.3262 | 2.2147 | 0.0841 |
| 8,000 | 74,874 | 477.5461 | 16.4210 | 176.6232 | 2.0753 | 0.0816 |
| 8,500 | 71,066 | 479.4729 | 18.2401 | 187.9424 | 1.9606 | 0.0864 |
| 9,000 | 73,580 | 495.2649 | 19.8599 | 198.0391 | 1.9403 | 0.0902 |
| 9,500 | 68,882 | 482.9199 | 20.5913 | 208.5213 | 1.7836 | 0.0892 |
| 10,000 | 65,774 | 473.0736 | 21.4074 | 219.2308 | 1.6516 | 0.0885 |
| 10,500 | 63,487 | 462.1411 | 22.3461 | 229.4383 | 1.5304 | 0.0887 |
| 11,000 | 63,344 | 471.5135 | 23.2618 | 239.6649 | 1.5042 | 0.0887 |
| 11,500 | 61,674 | 491.4406 | 24.4938 | 248.3846 | 1.5317 | 0.0906 |
| 12,000 | 59,852 | 486.7528 | 25.2873 | 258.6173 | 1.4529 | 0.0900 |

Control-arm per-sample table (ticks 500-4,500, **truncated at the stop**):

| tick | population | mean_genome_size | mean_mesh_nodes | mean_generation |
| --- | --- | --- | --- | --- |
| 500 | 16,476 | 139.7745 | 2.7329 | 11.6184 |
| 1,000 | 2,611 | 237.6496 | 4.8958 | 23.5741 |
| 1,500 | 7,267 | 315.9777 | 6.4127 | 34.5230 |
| 2,000 | 13,808 | 340.9446 | 6.9125 | 44.7985 |
| 2,500 | 14,987 | 358.6360 | 8.2241 | 55.9466 |
| 3,000 | 33,681 | 679.2672 | 17.8483 | 63.0727 |
| 3,500 | 34,351 | 888.1495 | 25.1624 | 72.6631 |
| 4,000 | 68,592 | 1,501.3355 | 43.1909 | 80.9724 |
| 4,500 | 78,883 | 2,074.5901 | 59.7803 | 90.5486 |

**Closure question (levelling-off, cost arm alone, not affected by the
truncation):** increment over ticks 4,000-8,000 = 477.5461 - 473.5279 =
4.0181 units; increment over ticks 8,000-12,000 = 486.7528 - 477.5461 =
9.2067 units. Ratio (8k-12k)/(4k-8k) = 2.29, **above** one quarter — **not
levelling off**. The cost arm's `mean_genome_size` oscillates in a 462-506
unit band from tick 4,500 to 12,000 without a downward trend (the two
increments are 0.85% and 1.9% of the base), and the later increment is larger
than the earlier one, so the brake has not produced a plateau over this run's
depth. Over the same span `mean_mesh_nodes` rises 10.44 -> 25.29 with no
plateau (one 0.08-node dip at tick 7,500), so units per node fall from ~45
to ~19: the brake holds size while node count keeps growing.

Wall time: cost 8,506 s (2.36 h); control 10,674 s to its tick-4,500 stop
(2.97 h, truncated). Pair total 19,180 s (5.33 h), over the spec's 3-hour
budget — reported per the recorded 2026-09-15 user decision, not a fresh
finding.
