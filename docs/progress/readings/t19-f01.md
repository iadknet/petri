# T19.F01 — Per-Tick Hop Ramp readings

Tables and transcripts for the feature spec
[`docs/specs/roadmap/t19-f01-per-tick-hop-ramp.md`](../../specs/roadmap/t19-f01-per-tick-hop-ramp.md).

## Spec-time chain-length measurement (allowance calibration)

Method: a temporary histogram of `work_counters.mesh_hops` per creature-tick,
recorded in `TickComputeStats::record` (`crates/v3-core/src/simulation/tick.rs`)
and read every 500 ticks by a throwaway `v3-cli` integration test that built
each goal world exactly as the goal profile does (`resolve_config` over the
checked-in recipe, then `build_config(goal_profile_params())`) and ran it 2,000
ticks with `run_tick`. Code at `437e3358`, release build, host Apple M1 Pro,
2026-09-21. The probe was reverted after the run; nothing of it is committed.
Command: `cargo test --release -p v3-cli --test hop_probe_tmp -- --ignored --nocapture`.

Identity check: every case's `config_digest`, final population, and
creature-tick total equal the stored T13.F07 goal report
(`docs/progress/features/t13-f07-current-policy-recruitment-transitions-goal.json`):
5,849 / 3,506 / 9,084 creatures and 38,938,170 creature-ticks.

| World (seed) | Creature-ticks | Max hops in one creature-tick | Max by 500-tick window (1–500, 501–1000, 1001–1500, 1501–2000) | Creature-ticks with hops > 6 / > 8 |
| --- | ---: | ---: | --- | ---: |
| Orchards in grassland (11) | 11,410,064 | 7 | 5, 5, 5, 7 | 73 / 0 |
| Canyon country (22) | 11,858,968 | 9 | 5, 7, 9, 7 | 5,774 / 53 |
| Confluence (33) | 15,669,138 | 7 | 5, 6, 6, 7 | 1,415 / 0 |

Histogram of hops per creature-tick (count of creature-ticks):

| Hops | Orchards (11) | Canyon (22) | Confluence (33) |
| ---: | ---: | ---: | ---: |
| 1 | 8,376 | 7,567 | 10,663 |
| 2 | 10,072,517 | 8,380,579 | 11,912,337 |
| 3 | 1,158,645 | 2,420,020 | 3,037,602 |
| 4 | 157,885 | 696,961 | 561,587 |
| 5 | 12,319 | 268,472 | 132,141 |
| 6 | 249 | 79,595 | 13,393 |
| 7 | 73 | 5,266 | 1,415 |
| 8 | 0 | 455 | 0 |
| 9 | 0 | 53 | 0 |
| > 9 | 0 | 0 | 0 |

Other chain-length references, for the margin above the goal maximum:

| Source | Reading |
| --- | --- |
| Live survey 2026-09-16 (user world, generation ~400, 400 creatures, 1,036 runs each) | per-creature median chain: min 1, p25 5, median 7, p75 9, max 19; executed nodes over the battery max 22; hop-cap hits 0 |
| T13.F07 goal drift walk, depth 2,000 (50 lineages, 4,000 battery executions) | executed nodes 220 graph + 208 VM over 50 lineages (mean 8.56 per lineage); hop-cap hits 0 |
| V3Alpha1 founder | 2 hops per tick |

## Ramp cost at the chosen constants (`hop_ramp_allowance` 32, `hop_ramp_cost` 1e-4)

Per tick, `n` hops cost `1e-4 * m * (m + 1) / 2` with `m = max(0, n - 32)`.

| Hops in one tick | Ramp energy | Against `initial_energy` 20 and goal mean energy ~32 |
| ---: | ---: | --- |
| 9 (goal maximum) | 0 | nothing paid |
| 32 | 0 | nothing paid |
| 48 | 0.0136 | one long pass, survivable |
| 64 | 0.0528 | one capped pass at a 64-hop cap, survivable |
| 128 | 0.4656 | 1.5% of initial energy |
| 352 (11 passes of 32) | 5.136 | lethal within about four to six ticks of sustained cycling |
| 704 (11 passes of 64) | 22.628 | lethal in one to two ticks |
| 1,024 (`max_mesh_hops` today) | 49.253 | lethal in one tick; T03.F10's VM ramp costs about 49 at its own cap |

## Benchmark (roadmap-benchmark-specialist, 2026-09-21/22)

Commands, sequentially from the worktree at `1f64e7c3` (final feature code),
nothing else running (no `v3-*`/`cargo-mutants` processes before either run);
logs in the session scratchpad (`t19-f01-gate.log`, `t19-f01-goal.log`).

1. `make bench PROFILE=gate FEATURE=t19-f01-per-tick-hop-ramp` — CLI
   arguments `bench --profile gate --feature t19-f01-per-tick-hop-ramp`;
   `make` exit 0 (observed, shell `EXIT:0`); `measurement_evidence.cli_exit`
   `{"code":0,"source":"v3-cli status on successful artifact-pair
   completion; output errors instead exit 1"}`; `outer_exit` null; `dirty`
   false. `comparison.severe` false against both references; every gate
   counter and the wall reading `ok`.
2. `make bench PROFILE=goal FEATURE=t19-f01-per-tick-hop-ramp` — CLI
   arguments `bench --profile goal --feature t19-f01-per-tick-hop-ramp`;
   `make` exit 0 (observed, shell `EXIT:0`); `measurement_evidence.cli_exit`
   `{"code":0,"source":"v3-cli status on successful artifact-pair
   completion; output errors instead exit 1"}`; `outer_exit` null; `dirty`
   true (worktree state at run time: tracked files clean, the step-1 gate
   summary present as an untracked file, same pattern as T17.F02). Run once
   per the 2026-09-05 user decision; no second determinism run taken.
   `comparison.severe` false against both references; every counter `ok`.

Reports and sizes:

| Profile | Summary path | Summary bytes | Summary sha256 | Raw path (main checkout, local, not staged) | Raw bytes | Raw sha256 |
| --- | --- | --- | --- | --- | --- | --- |
| gate | `docs/progress/features/t19-f01-per-tick-hop-ramp.json` | 97,650 | `3c49a2f9989913be766a7994d605a61a2aad1c9aecc18ff418e0609922d54816` | `.bench-artifacts/t19-f01-per-tick-hop-ramp/gate.json` | 94,860 | `e18ce59cf756f9e74ea2ad8dc1ad6d81c0a5740cec785c304e6512ad481e4cee` |
| goal | `docs/progress/features/t19-f01-per-tick-hop-ramp-goal.json` | 7,662,146 | `0f792cca5ae7a6e40df9778d3f55987991d2629a2e9044b7a174c3c97f23d8f6` | `.bench-artifacts/t19-f01-per-tick-hop-ramp/goal.json` | 591,975,125 | `aeacfa049ea332489eb58d82226c6924e159c9dacff1fc12b486a4220364a201` |

Byte counts and hashes re-checked locally with `ls -l` and `shasum -a 256`
after both runs, matching the values above (verification time
2026-09-22T02:48:34Z). Thresholds recorded in `measurement_evidence`: work
flag 10% / severe 50%, wall flag 25% / severe 100%, wall not fatal; caps
founder 10 s per world, evolved 180 s summed, drift 30 s per world,
recruitment 120 s per report, goal investigation 900 s.

Gate work-counter deltas (per creature-tick; `severe=false`, every level
`ok`; vs `t13-f07-current-policy-recruitment-transitions.json`, the latest
closure, all six read 0.000000%; vs `remove-complementary-nutrition.json`,
the gate epoch, largest move is `plasticity_updates` −37.821144%, all
`ok`): `mesh_hops` 2.040249, `vm_steps` 22.532262, `graph_relax_iters`
0.999842, `plasticity_updates` 0.006946, `actions_applied` 1.352060,
`births` 0.025954. Gate wall 0.0013997856 ms per creature-tick, −10.260431%
vs epoch (`ok`), −7.145903% vs latest closure (`ok`).
`neighborhood_founder_wall_clock_ms` 47.557834 against the 10,000 ms cap;
`wall_clock_ms_total` 381.897916 ms.

Goal work-counter deltas (per creature-tick; vs
`t13-f07-current-policy-recruitment-transitions-goal.json`, the latest
closure, all six read 0.000000%; vs
`t17-f02-unit-scale-introspection-goal.json`, the goal-worlds epoch, all
`ok`, largest move `plasticity_updates` −46.011157%): `mesh_hops`
2.284326, `vm_steps` 23.014350, `graph_relax_iters` 1.020063,
`plasticity_updates` 0.047907, `actions_applied` 1.328119, `births`
0.015790. Goal wall 0.0115435350 ms per creature-tick, −8.895251% vs epoch
(`ok`), −2.375998% vs latest closure (`ok`).
`wall_clock_ms_total` 449,484.126791 ms (~7.5 minutes);
`neighborhood_evolved_wall_clock_ms_total` 605.908541 ms against the
180,000 ms summed cap; `neighborhood_founder_wall_clock_ms` 105.819876 ms
against the 10,000 ms cap.

`mesh_ramp` energy flow and mortality, per world (`allowance` 32 exceeds
the measured goal maximum of 9 hops in a tick, so no charge is expected):

| World | `energy_flows.mesh_ramp` | `mortality.by_cause.mesh_ramp` |
| --- | --- | --- |
| Orchards in grassland | 0.000000 | 0 |
| Canyon country | 0.000000 | 0 |
| Confluence | 0.000000 | 0 |

`config_digest` per goal case: all three changed from the T13.F07 goal
reference's stored digests (Orchards
`sha256:d4b8c057eb…06878a34` → `sha256:88fe9d19be…8118d3e`, Canyon
`sha256:3c42bd6c56…9f722f6d` → `sha256:f86798707b…5e1ae3`, Confluence
`sha256:aa5b67350a…78f72b8c` → `sha256:e41d334669…002b5838e`, abbreviated);
every case carries `comparison.references[].cases[].inputs_changed: true`
against both references, matching the predeclaration (the two new
hop-ramp fields serialize into the resolved config).

Drift walk (`deterministic.goal_indicators.drift_depth`), population
persistence, lineage diversity, memory sensitivity, and
`comparison_inputs.case_readings` are byte-identical to the T13.F07 goal
report (structural diff, order-insensitive, 0 differences in each
subtree). The full `deterministic.goal_indicators` block is **not**
otherwise byte-identical: `mutational_neighborhood.evolved.per_seed[0]`
carries `sampled_genomes` (full per-genome mesh-execution and routing
detail) in the current run where the T13.F07 stored reference carries a
`mesh_summary` aggregate, and `recruitment_paths.arms[].lineages[]`
carries `checkpoints`/`proposals`/`task`/`first_successful_path` in the
current run where the stored reference carries `proposal_count`. This is
a report-schema difference between the current tool and the T13.F07
summary's original schema, not a numeric counter change: every counter
this feature predeclared (work counters, `mesh_ramp`, `config_digest`
identity/change behavior, persistence, diversity, memory sensitivity,
drift walk) reads as predeclared. No production or bench-crate diff in
this feature (`1f64e7c3`) touches these field names, so the schema
difference predates this feature; flagged here as a literal deviation
from the "byte-identical apart from the digests and the two new zero
fields" wording, not investigated or remediated by this specialist.

The gate `deterministic` block (no `config_digest`/per-world `cases` field
in the gate profile) is byte-identical to the T13.F07 gate reference with
no differences at all (line diff empty).

Predeclaration table against the measurements (verdict only; no
interpretation):

| Row | Predeclared | Measured | Verdict |
| --- | --- | --- | --- |
| Gate/goal `mesh_hops`, `vm_steps`, `graph_relax_iters`, `plasticity_updates`, `actions_applied`, `births` vs T13.F07 | 0.000000% | 0.000000% on all twelve readings (6 gate + 6 goal) | met |
| Goal persistence, lineage diversity, memory sensitivity, drift walk (changed/dead per birth, depths 0–2,000) | identical to T13.F07 | structural diff 0 in each subtree | met |
| Energy flow/mortality `mesh_ramp` per goal world | `0.000000` / `0` in every world | confirmed in all three worlds | met |
| Goal case `config_digest` | changes in all three, `inputs_changed: true` | confirmed | met |
| Gate and goal `deterministic` blocks | byte-identical apart from digests and the two new zero fields | gate: byte-identical, no diff at all; goal: digests + two new `mesh_ramp` fields as predicted, plus a pre-existing report-schema difference (`sampled_genomes`/`mesh_summary`, `checkpoints`/`proposal_count`) unrelated to any counter | deviation from literal wording, reported as fact, no counter affected |
| Wall time, both profiles | flat; flag tolerated, severe investigated | gate ok (−7.1%/−10.3%), goal ok (−8.9%/−2.4%); no flag or severe on either profile | met |
| `comparison.severe` | false expected (no justified cost predeclared beyond neutrality) | false on both profiles, both references | met |

## Focused tests (spec Verification)

| Test | Where | Claim |
| --- | --- | --- |
| `hop_charge_is_zero_inside_the_allowance_and_linear_past_it` (proptest) | `crates/v3-core/src/runtime/mesh.rs` | hop `k` pays 0 for `k <= allowance` and `cost * (k - allowance)` past it |
| `hop_ramp_total_over_a_tick_is_the_closed_form` (proptest) | `crates/v3-core/src/runtime/mesh.rs` | a tick of `n` hops totals `cost * m * (m + 1) / 2`, `m = max(0, n - allowance)` |
| `hop_ramp_charges_only_past_the_allowance` | `crates/v3-core/src/runtime/mesh.rs` | a chain at the allowance pays nothing |
| `founder_pays_no_hop_ramp_at_the_production_defaults` | `crates/v3-core/src/runtime/mesh.rs` | V3Alpha1 runs exactly two hops and pays zero ramp |
| `hop_ramp_exhaustion_ends_the_tick_as_noop_before_dispatch` | `crates/v3-core/src/runtime/mesh.rs` | an unaffordable hop ends as `NoOp`/`EnergyExhausted`, counted, never dispatched |
| `hop_ramp_agrees_across_execution_modes` | `crates/v3-core/src/runtime/mesh.rs` | production, observed, and traced modes agree on energy, counters, observation, `mesh_ramp_cost` |
| `a_node_reads_its_own_hop_ramp_charge_as_consumed_this_tick` | `crates/v3-core/src/runtime/mesh.rs` | at allowance 0, a node reading `EnergyConsumedThisTick` on hop 2 sees hop 1's charge plus hop 2's (differential against a zero-cost run) |
| `mesh_ramp` cause key, schema slot, flow accumulation | `crates/v3-core/src/simulation/energy_accounting.rs` | key list and `record_cognition` carry `mesh_ramp` |
| stored JSON key | `crates/v3-cli/src/bench/tracking/tests.rs` | `EnergyFlowTracking.mesh_ramp` serializes beside `vm_compute` |
| `dispatch_float_totals_follow_priority_queue_order_without_preaggregation` | `crates/v3-core/src/simulation/tick/tests/energy_accounting.rs` | flow totals still summed in dispatch order |
| four hop-ramp config tests | `crates/v3-core/src/config/simulation.rs` | defaults, serde defaults when absent, invalid-cost fallback, zero cost and zero allowance preserved |

## Mutation gate (roadmap-mutation-specialist, 2026-09-21)

Command: `MUTANTS_ITERATE=0 make rust-mutants` (fresh; `run-mode.txt` = `fresh`).
Summary line: `37 mutants tested in 9m: 4 missed, 31 caught, 2 unviable`.
Output: `~/.local/share/petri-tools/mutants/t19-f01/mutants.out`.
One fresh run; no second fresh run — triage added one test and changed no
production content, test selection, or tool configuration.

The later `MUTANTS_ITERATE=1` remediation pass reuses that same output
directory, so `run-mode.txt` there now reads `incremental` and `missed.txt` is
empty. The fresh run's own evidence is its redirected log and the four
`missed.txt` lines quoted in the table below, copied while the fresh run's
output was in place.

| Survivor (from `missed.txt`) | Resolution | Killing test |
| --- | --- | --- |
| `crates/v3-core/src/simulation/tick.rs:574:34: replace + with - in TickComputeStats::record` | killed | `tick_compute_total_sums_vm_graph_and_mesh_ramp_costs` |
| `crates/v3-core/src/simulation/tick.rs:574:34: replace + with * in TickComputeStats::record` | killed | `tick_compute_total_sums_vm_graph_and_mesh_ramp_costs` |
| `crates/v3-core/src/simulation/tick.rs:574:60: replace + with - in TickComputeStats::record` | killed | `tick_compute_total_sums_vm_graph_and_mesh_ramp_costs` |
| `crates/v3-core/src/simulation/tick.rs:574:60: replace + with * in TickComputeStats::record` | killed | `tick_compute_total_sums_vm_graph_and_mesh_ramp_costs` |

`timeout.txt` was empty: no timed-out survivors. All four survivors are the two
`+` operators in `total_cost = vm_cost + graph_cost + mesh_ramp_cost`; the new
test in `crates/v3-core/src/simulation/tick/tests/energy_accounting.rs` records
one `MeshOutput` with costs 1.0 / 2.0 / 4.0, so the pinned total 7.0 separates
the plain sum from every mutated difference or product (3.0, 6.0, −1.0, 12.0).

Post-remediation checks: `MUTANTS_ITERATE=1 make rust-mutants` — `4 mutants
tested in 3m: 4 caught`, no survivors (feedback only, not closure evidence);
`cargo test -p v3-core` 1637 lib + 91 passed, 0 failed; `cargo clippy
--workspace --all-targets` and `cargo fmt --all --check` clean.
