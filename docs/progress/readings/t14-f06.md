# T14.F06 — reproductive success by cognitive class

| Build record | Value |
| --- | --- |
| Date | 2026-09-12 |
| Worktree / branch | `.worktrees/t14-f06` / `codex/t14-f06` |
| State | Complete; tested commit `4f116b4cad727f9d29e6b7cefe520e2faee05f84` passed full `make check`; closure documentation is final. |

## Build verification

| Command | Result |
| --- | --- |
| `cargo test -p v3-core --lib reproductive_success` | TDD red before implementation: expected missing class/types, counter field and genome-layer analyzer export; exit 101. |
| `cargo check --workspace --all-targets` | Passed after implementation and again after the reproducibility test extension; no warnings. |
| `cargo test -p v3-core --lib` | 1,384 passed, 2 existing ignored, 0 failed. All new classification, removal and analyzer tests passed. |
| `cargo test -p v3-cli` | 96 library, 11 main, 20 bench integration and 11 CLI integration tests passed; documentation tests passed. |
| `cargo test -p v3-core --test reproducibility` | 3 passed; existing accounting fingerprint now includes all twelve integers for independent seeded runs and one/four-thread comparisons. |
| `cargo test -p v3-core --lib cognitive_class_keys_match_the_report_contract` | 1 passed; the exact four-key public report contract is pinned after mutation triage. |
| `cargo fmt --all` / `git diff --check` | Passed. |
| `make roadmap-check` | Passed after the implementation/spec/readings edits. Aqua's package-timestamp sandbox warning was non-fatal. |
| `make check` | First sandboxed attempt exited 2 only because seven server WebSocket tests could not bind `127.0.0.1:0` (`PermissionDenied`); all preceding checks passed. After advisor consultation, the complete unchanged suite reran with local-listener permission and exited 0 at tested commit `4f116b4cad727f9d29e6b7cefe520e2faee05f84`. Standalone viability-first was inapplicable because no defaults, founder behavior or tick mechanics changed. |

## Focused coverage

All tests below passed in the package commands above. Property assertions do not
depend on which cases were drawn; no regression files were generated.

| Module | Exact tests / coverage |
| --- | --- |
| `simulation::reproductive_success::tests` | `all_sixteen_structural_combinations_follow_class_precedence`; `runtime_storage_is_exactly_twelve_u64_totals`; `cognitive_class_keys_match_the_report_contract`; `cognitive_classes_partition_every_structural_flag_combination`; `reproductive_success_totals_are_additive_and_permutation_invariant` |
| `simulation::tick::tests::reproductive_success` | `reproductive_success_records_every_class_cause_and_generation_once`: four classes, all 17 causes, founders/descendants, with/without birth mutation operators, repeated absent removal, cumulative reset protection and mortality reconciliation. |
| `simulation::tick::tests::reproductive_success` | `reproductive_success_ignores_unreachable_cognitive_structure`; `reproductive_success_preserves_phase_zero_and_predation_removal_ages` |
| `simulation::tick::tests::reproductive_success` | `reproductive_success_counts_successful_births_when_the_parent_is_removed`: production success and rejection, surviving-parent exclusion, actual newborn exit at age zero and parent offspring/age contributions. |
| `simulation::tick::tests::reproductive_success` | `reproductive_success_is_unchanged_by_observational_reexecution` |
| `creature::genome::companions::tests` | `genome_and_both_neighborhood_paths_share_structural_companions`; `all_vm_memory_forms_remain_structural_even_after_halt`; all nine relocated existing VM/graph analyzer tests. |
| `bench::tests` | `reproductive_success_report_transfers_all_twelve_integers_and_stable_keys`: distinct nonzero values, exact definition/key/field shape, deterministic key order, integer above floating-point exact range, and round trip. |
| `bench::tests` | `reproductive_success_report_keeps_all_empty_cohorts_as_integer_zeros`; `checkpoint_tracking_omits_every_transferred_block`; `transferred_tracking_blocks_are_absent_not_zero_in_a_historical_report` |
| `bench::tests` | `world_set_case_tracking_matches_a_replayed_run`: each world's terminal values equal its replay, class counts reconcile to mortality, and the block occupies the flattened case path. |
| `tests/reproducibility.rs` | `two_simulations_with_the_same_seed_are_byte_identical`; `terrain_is_identical_across_independent_initialization_and_thread_counts`; `fixed_map_seed_isolates_maps_from_run_seeded_placement` |

## Self-review and consultations

| Item | Outcome |
| --- | --- |
| Production self-review | Reviewed every production diff for reuse, simplification and efficiency. The analyzer body and type are unchanged; both neighborhood paths re-export the shared implementation. Fixed enum/array storage and terminal `BTreeMap` transfer reuse existing patterns. No production rewrite was needed. |
| Verification refinement | Extended the existing reproducibility fingerprint to include the new totals; workspace check and the affected integration suite passed afterward. |
| Runtime boundary | One analyzer call inside each successful removal, beside mortality and outside mutation filters; exactly 96 bytes of fixed totals; no per-tick scan, per-creature cache, ledger, configuration, dependency or RNG draw added. |
| Advisor 1: pre-approach | Accepted the fixed four-row design, successful-removal-only placement, lifetime and structural boundaries, terminal absence/zero semantics, and the inapplicability of standalone viability-first. No scope expansion or exception. |
| Advisor 2: pre-handoff | Confirmed implementation and verification are sufficient for handoff with no correctness/spec blocker or production change. Accepted the wording clarification: one of the twelve distinct transfer values exceeds 2^53. |
| Final review | `gpt-6-astra` / `xhigh`: P1=0, P2=0, P3=1. The stale opening status was corrected in documentation. |
| Consultations / remediation | 3 completed consultations; all required guidance accepted, none rejected; 1 documentation-only remediation pass. |
| Verification exception | Advisor consultation 3 classified the sandboxed listener-bind failures as environmental. The full unchanged suite passed with permission; no test was filtered or waived. |
| Requirement corrections | None during implementation. Planning clarified the roadmap's twelve-integer constraint as four exhaustive, mutually exclusive classes with documented precedence. |
| User interventions | One: add a durable Codex benchmark-specialist instruction to request process-inspection permission on the first attempt; recorded in `docs/workflow-codex.md`. |
| Model / effort ledger | Orchestrator: `gpt-5.6-sol` / `medium` goal configuration (active runtime metadata unavailable); spec owner/advisor: `gpt-6-astra` / `xhigh`; implementer: `gpt-6-astra` / `xhigh`; benchmark: `gpt-5.6-terra` / `high`; reviewer: `gpt-6-astra` / `xhigh`; mutation: `gpt-5.6-sol` / `medium`. |
| Task usage | Native goal telemetry immediately before completion: 642,353 tokens and 3,535 seconds elapsed. |

## Closure measurements

| Evidence | State |
| --- | --- |
| Mutation gate | Fresh mode: `21 mutants tested in 4m: 2 missed, 16 caught, 3 unviable`; both survivors were killed by one exact-key core test and an incremental pass reported `2 mutants tested in 2m: 2 caught`. No second fresh run was required under the unchanged-production contract. |
| Gate / goal reports | Completed sequentially by the benchmark specialist on 2026-09-12 and stored at the required paths. Both reports have `severe=false`; see the measured record below. |
| Orchards / Canyon / Confluence raw class totals | Recorded from the standard goal report with the applied configuration digest for every case. |
| Performance and goal verdict | Passed the severe threshold. Gate comparisons are all `ok`; goal has one non-severe advisory flag against the older goal epoch (`plasticity_updates`, +40.886836%). |

### Mutation record

The closure gate ran once in fresh mode with
`MUTANTS_ITERATE=0 make rust-mutants`. Its exact output directory is
`/Users/istefanek/.local/share/petri-tools/mutants/t14-f06/mutants.out`; the
summary was `21 mutants tested in 4m: 2 missed, 16 caught, 3 unviable`.

| Fresh survivor | Resolution |
| --- | --- |
| `crates/v3-core/src/simulation/reproductive_success.rs:37:9: replace CognitiveClass::as_key -> &'static str with ""` | **Killed.** `cognitive_class_keys_match_the_report_contract` pins all four exact report keys. |
| `crates/v3-core/src/simulation/reproductive_success.rs:37:9: replace CognitiveClass::as_key -> &'static str with "xyzzy"` | **Killed.** The same exact-key contract assertion rejects an arbitrary replacement. |

The permitted incremental feedback run used
`MUTANTS_ITERATE=1 make rust-mutants` in incremental mode and reported
`2 mutants tested in 2m: 2 caught`, with no survivors. No second fresh run was
required because remediation added one test only: production code, test
selection and tool configuration were unchanged, and no test was deleted or
weakened.

### Benchmark record

After the initial sandbox-denied preflights, the repository preflight was
authorized to inspect host processes and each actual payload ran once,
sequentially. No profile parameter, recipe, threshold, stored baseline, or
production/test code was changed.

| Profile | Command / result | Measured duration | `severe` / threshold verdict | Stored report |
| --- | --- | --- | --- | --- |
| Gate | `make bench PROFILE=gate FEATURE=t14-f06-reproductive-success-by-cognitive-class`; exit 0 | 679.280 ms total; 0.001588788 ms/creature-tick | `false`; pass. All normalized counters and both reference wall-clock comparisons are `ok`. | `docs/progress/features/t14-f06-reproductive-success-by-cognitive-class.json` |
| Goal | `make bench PROFILE=goal FEATURE=t14-f06-reproductive-success-by-cognitive-class`; exit 0 | 491,134.413 ms total; 0.008055520 ms/creature-tick | `false`; pass. Against T12.F04, `plasticity_updates` is advisory `flag` at +40.886836%; no counter is severe and wall clock is `ok` (+8.760495%). The matching T14.F03 comparison is all `ok`. | `docs/progress/features/t14-f06-reproductive-success-by-cognitive-class-goal.json` |

Gate observation budget: founder neighborhood 56.232 ms (cap 10,000 ms) and
evolved neighborhood 0 ms (not sampled by the gate profile).

Goal observation budgets were within the declared caps: founder neighborhood
100.303 ms (cap 10,000 ms) and evolved neighborhood 454.842 ms total across
seeds 11/22/33 (161.018, 160.790, 133.034 ms; cap 180,000 ms). The report's
final-state observation total is 839.124 ms and drift-depth observation is
19,368.050 ms. Both reports record revision
`698510b4a233643f9934fc232375c0f78d939483` with eight threads.

The following terminal blocks use
`reproductive-success-by-cognitive-class-v1`. Each class is
`removed creatures / offspring spawned / survival ticks`.

| Standard goal case / applied config identity | none | plasticity | shared memory | stateful |
| --- | --- | --- | --- | --- |
| Orchards in grassland (`sha256:8141056a33bc30445fd29340fa40498372835e08459b7c04c347f9724b445423`) | 249,949 / 249,667 / 14,763,879 | 16,128 / 14,784 / 959,736 | 99,426 / 94,379 / 5,582,578 | 3,924 / 2,993 / 220,980 |
| Canyon country (`sha256:ee72c5532cb947fad7349a3a4d3c5a5b5bef2c501ebe5b299b5de12ad26bd99d`) | 230,318 / 228,872 / 11,711,761 | 12,097 / 10,389 / 634,383 | 132,188 / 123,259 / 5,338,797 | 5,993 / 5,153 / 318,919 |
| Confluence (`sha256:25fb4d0baf34719c0f1e657c98b4e8a7932216510d69651c6fd58d4f6d318676`) | 266,542 / 262,685 / 12,923,480 | 8,095 / 6,170 / 428,687 | 168,729 / 158,726 / 7,430,852 | 6,789 / 5,568 / 293,602 |

Comparison boundary: this observation-only feature adds no environmental
pressure. All three current case digests match the T12.F04 goal-world epoch,
so their existing recipes and pressures were applied unchanged. The comparison
reports establish compute-threshold context only; these class totals are first
readings for removed creatures, with no historical-zero reference. They remain
confounded correlations and do not establish cognition payoff, causal selection
or an independent trait effect.

Present zero-count rows describe empty removed-creature cohorts with undefined
means; historical absence is unmeasured. These are correlations among removed
creatures, confounded by carrying cost, genotype, environment, class precedence
and survivor exclusion. They do not establish useful cognition, causal selection
or independent trait effects.
