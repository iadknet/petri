# T14.F02 — existing counter transfer: measured readings

| Evidence record |
| --- |
| Self-review, benchmark, and verification pass, 2026-09-11, worktree `.claude/worktrees/t14-f02`, branch `worktree-t14-f02`, build commit `7579a4a7` (the self-review edit was test-only and uncommitted at benchmark time; both reports record revision `7579a4a7`). Reports: [gate](../features/t14-f02-existing-counter-transfer.json), [goal](../features/t14-f02-existing-counter-transfer-goal.json). |

## Verification commands

| Command | Outcome |
| --- | --- |
| `cargo fmt --all` | clean |
| `cargo check --workspace --all-targets` | clean, 0 warnings |
| `cargo test -p v3-core --test viability` | 24 passed; 0 failed |
| `cargo test -p v3-core` (inside `make check`) | lib 1307 passed / 2 ignored, creature_workflow_e2e 7, temporal_fixtures 13, priority_bid_reachability 2, terrain 10, baseline_worlds 19 / 1 ignored, reproducibility 3, vm_all_opcodes_e2e 1; 0 failed |
| `cargo test -p v3-cli` (inside `make check`) | lib 77 passed, main 11, tests/bench.rs 20, tests/cli.rs 11; 0 failed |
| `cargo test -p v3-server` (inside `make check`) | lib 46 passed, bin 2, tests/server.rs 95; 0 failed |
| `make check` | exit 0 (rust format/clippy/tests, frontend lint, tsc, tests, build, dependency audit, roadmap and policy checks) |
| `make roadmap-check` | exit 0 |
| `make bench PROFILE=gate FEATURE=t14-f02-existing-counter-transfer` | exit 0; severe=false against both references; wall total 0.58 s |
| `make bench PROFILE=goal FEATURE=t14-f02-existing-counter-transfer` | exit 0; severe=false against both references; wall total 492.0 s; founder neighborhood 0.11 s, evolved neighborhood 0.48 s, drift depth 19.3 s |

## Self-review (simplify pass)

| Finding | Fix |
| --- | --- |
| `world_set_case_tracking_matches_a_replayed_run` re-asserted every transferred field against `sim.stats` field by field, ~90 lines that duplicate the production mapping in test form and are already implied by the `assert_eq!(case.tracking, WorldTracking::observe(&sim))` two lines above them | Deleted the block, keeping the one assertion the equality does not imply: `typed_eats_failed_total.len() == case.case.food_type_count`. The mapping itself is covered field for field by `transferred_tracking_blocks_read_the_stats_counters_behind_them`, which sets `SimStats` by hand. |

No reuse or efficiency findings: the `From<&MutationValueTotals>` conversion, the shared `by_food_type` closure at the single `observe` site, and the `termination_reason`-on-`MeshOutput` change (which removed a parameter from every `MeshExecutionMode::finish`) are already the general forms.

## Gate transcript (tail of log)

```
     Running `target/release/v3-cli bench --profile gate --feature t14-f02-existing-counter-transfer --out docs/progress/features/t14-f02-existing-counter-transfer.json`
wrote docs/progress/features/t14-f02-existing-counter-transfer.json
compared against docs/progress/features/remove-complementary-nutrition.json: severe=false
  mesh_hops: current=2.028954 reference=Some("2.027261") delta%=Some("0.083512") level=ok
  vm_steps: current=22.425973 reference=Some("22.751316") delta%=Some("-1.429996") level=ok
  graph_relax_iters: current=0.994920 reference=Some("0.995234") delta%=Some("-0.031550") level=ok
  plasticity_updates: current=0.009880 reference=Some("0.011171") delta%=Some("-11.556709") level=ok
  actions_applied: current=1.272860 reference=Some("1.275525") delta%=Some("-0.208934") level=ok
  births: current=0.026902 reference=Some("0.026780") delta%=Some("0.455564") level=ok
compared against docs/progress/features/t14-f01-closure-comparison-and-indicator-coverage-integrity.json: severe=false
  mesh_hops: current=2.028954 reference=Some("2.028954") delta%=Some("0.000000") level=ok
  vm_steps: current=22.425973 reference=Some("22.425973") delta%=Some("0.000000") level=ok
  graph_relax_iters: current=0.994920 reference=Some("0.994920") delta%=Some("0.000000") level=ok
  plasticity_updates: current=0.009880 reference=Some("0.009880") delta%=Some("0.000000") level=ok
  actions_applied: current=1.272860 reference=Some("1.272860") delta%=Some("0.000000") level=ok
  births: current=0.026902 reference=Some("0.026902") delta%=Some("0.000000") level=ok
exit=0
```

## Goal transcript (tail of log)

```
     Running `target/release/v3-cli bench --profile goal --feature t14-f02-existing-counter-transfer --out docs/progress/features/t14-f02-existing-counter-transfer-goal.json`
wrote docs/progress/features/t14-f02-existing-counter-transfer-goal.json
compared against docs/progress/features/t12-f04-baseline-world-set-goal.json: severe=false
  mesh_hops: current=2.258369 reference=Some("2.247936") delta%=Some("0.464115") level=ok
  vm_steps: current=23.478709 reference=Some("23.339759") delta%=Some("0.595336") level=ok
  graph_relax_iters: current=1.024449 reference=Some("1.028540") delta%=Some("-0.397748") level=ok
  plasticity_updates: current=0.066183 reference=Some("0.046976") delta%=Some("40.886836") level=flag
  actions_applied: current=1.360568 reference=Some("1.371176") delta%=Some("-0.773642") level=ok
  births: current=0.019659 reference=Some("0.018609") delta%=Some("5.642431") level=ok
compared against docs/progress/features/t14-f01-closure-comparison-and-indicator-coverage-integrity-goal.json: severe=false
  mesh_hops: current=2.258369 reference=Some("2.258369") delta%=Some("0.000000") level=ok
  vm_steps: current=23.478709 reference=Some("23.478709") delta%=Some("0.000000") level=ok
  graph_relax_iters: current=1.024449 reference=Some("1.024449") delta%=Some("0.000000") level=ok
  plasticity_updates: current=0.066183 reference=Some("0.066183") delta%=Some("0.000000") level=ok
  actions_applied: current=1.360568 reference=Some("1.360568") delta%=Some("0.000000") level=ok
  births: current=0.019659 reference=Some("0.019659") delta%=Some("0.000000") level=ok
exit=0
```

The `plasticity_updates` flag against T12.F04 is inherited: T14.F01's stored goal
report records the same 0.066183 against the same reference, and this run reads
0.000000 % against T14.F01 on every metric.

## Caps and thresholds (goal report)

| Cap | Limit | Reading |
| --- | --- | --- |
| `environment.neighborhood_evolved_wall_clock_ms_total` | 180,000 ms | 478.71 ms |
| `environment.neighborhood_founder_wall_clock_ms` | 10,000 ms | 108.97 ms |
| Total goal-profile investigation | 15 min | 492.01 s (8.2 min) |
| `environment.drift_depth_wall_clock_ms` | no cap | 19,254.97 ms |
| Wall ms per creature-tick | normalized thresholds | gate 0.001353, goal 0.008070 |
| `severe` on any reference | must be false | false on all four comparisons |
| Non-`ok` reading levels in either report | — | none (the only non-`ok` compute level is the inherited `plasticity_updates` flag above) |
| Per-case and top-level readings vs the T14.F01 goal reference | predeclared direction: none | every reading identical (`current == reference`) |

## Transferred blocks in the stored goal report

Every block is present and non-null in all three world cases, which is what the
feature exists to produce.

| Case | `typed_eats_total` | `typed_eats_failed_total` | `mesh_dispatches_energy_exhausted_total` | mutation attempted / applied / skipped | executed / reachable / unreachable / not-applicable target | `mutation_outcome_summary.carriers_observed_total` | operators in `mutation_value_totals_by_operator` | predation attempted / transferred / rejected / kills |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Orchards in grassland | 7,126,353 / 1,518 | 1,214,058 / 1,090 | 707 | 204,646 / 204,646 / 0 | 181,214 / 182,610 / 1,087 / 20,949 | 158,256 | 53 | 2,269 / 207 / 2,062 / 66 |
| Canyon country | 4,990,021 | 586,761 | 752 | 208,200 / 208,197 / 3 | 182,233 / 185,508 / 1,564 / 21,125 | 163,524 | 52 | 1,184 / 64 / 1,120 / 28 |
| Confluence | 5,876,268 / 100 | 1,969,961 / 111 | 1,342 | 247,552 / 247,550 / 2 | 219,108 / 220,878 / 1,642 / 25,030 | 193,559 | 53 | 2,191 / 147 / 2,044 / 80 |

Per-result predation keys carried: `RejectedNoVictim`, `Transferred`,
`TransferredAndKilled` in all three cases.
